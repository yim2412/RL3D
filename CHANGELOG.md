# 변경 이력

버전은 `main.py`의 `__version__` 이 기준이다. 형식은 `주.부.수` (기능 추가는 부, 수정은 수).

## [1.16.0] - 2026-09-12

Phase 12 ⑬번(P12-4 — 발사 궤적 근사선). 보류된 Phase 4(실시간 텔레메트리)의
**실현 가능한 대체**다. 무료 텔레메트리 소스가 없으니 실제 비행 경로는 못 그린다.

### 추가
- **발사 궤적 근사선** — 상세 패널을 열면 상승 경로를 점선으로. 닫으면 같이 지워진다
- **가정을 숫자로 적는 안내** — `가정: 경사 98.0° (태양동기) · 방위 203° 남서쪽`
- `web/js/trajectory.js` (신규) — `ascentAzimuth` · `ascentPath` · `ascentNote`

### 설계 메모
- **방위각은 구면 삼각법으로 푼다**: `sin A = cos i / cos φ`. 순행은 북동쪽 해,
  **역행(i ≥ 90)은 `180 − A`(남쪽 해)** — 반덴버그의 태양동기 발사가 남서로 나가는 이유다.
  실제 값과 대조: 케네디→ISS 45°(실제 44~45°), 바이코누르→ISS 63°(실제 61~65°)
- **`i < |φ|` 는 물리적으로 불가능하다** → 정동(90°)으로 클램프한다. 예외를 던지면 선만 조용히 사라진다
- **실측(캐시 441건 전수)**: `Low Earth Orbit` 이 250건(57%)인데 LEO 경사는 28~97° 어디든 되므로
  **이름만으로는 정할 수 없다.** 최소 에너지(=발사장 위도)로 두되 **그 사실을 화면에 적는다**.
  "안 그린다"도 검토했으나 절반 이상의 발사에서 기능이 사라진다 — 모른다는 사실은 문구로 전할 수 있다
- **Suborbital 은 그리지 않는다.** 궤도 진입이 없는데 그리면 없는 궤도를 지어내는 셈이다
- **점선으로 그린다.** 실선은 실측처럼 보인다 — 문구와 같은 말을 선 모양으로도 한다
- **안내 문구는 패널 위쪽에.** 처음엔 맨 아래에 붙였는데, 캡처해 보니 긴 패널에서
  **스크롤 끝까지 내려야** 보였다. 그건 "명시했다"고 할 수 없다(P12-16 과 같은 자리)

### 고침
- 하네스 스텁 엘리먼트가 `innerHTML` 을 덮어써도 `children` 이 남아 있었다 —
  실제 DOM 과 달라 **"안 그렸는데 그려졌다"로 잘못 통과**했다(이번에 실제로 겪음)

### 검증
- `tests/test_frontend.js` 394항목 — 외부 정답표가 없어 **물리 불변식**으로 잰다
  (경사=위도면 정동 · 경사가 커지면 북쪽으로 단조 이동 · 역행은 남쪽 · 끝점까지 2,200km ·
  경도 정규화 · 날짜변경선 분할). 변이 9종 전부 검출
- 캡처 3회(SSO·LEO). 패널을 여는 것은 클릭이 필요해 **임시 스크립트로 열고 되돌렸다**
  (P11-7 에서 타일 URL 을 잠시 바꾼 것과 같은 방식)

## [1.15.0] - 2026-09-12

Phase 12 ⑫번(P12-15 — 발사 밀도 히트맵).
**마커·클러스터는 "어디서 몇 번"을 못 보여준다** — 같은 발사장에 완전히 겹친 점은
1건이나 300건이나 똑같이 보이고, 클러스터는 줌에 따라 뭉치는 기준이 바뀐다.

### 추가
- **발사 밀도 히트맵** — 툴바 `🔥 밀도` 체크박스, 단축키 `7`, `settings.json` 에 저장
- **범례** — 색 눈금(`1건 ~ 한 발사장 최다 N건`)과 **모수**(불러온 건수·기간·부분 표본 연도)

### 설계 메모
- **소스를 따로 판다.** 기존 `launches` 소스는 `cluster: true` 라, 히트맵을 그 위에 얹으면
  **클러스터 하나가 점 하나로** 세어져 밀도가 통째로 왜곡된다 — 오류 없이 조용히 틀린다
- **필터 결과는 한 곳(`applyFilters`)에서 나눈다.** 마커와 히트맵이 갈라지면
  한 지도의 두 표현이 서로 다른 말을 한다
- **가중치를 고정하면 기능이 목적을 잃는다.** `heatmap-weight: 1` 로 첫 캡처를 떠 보니
  **1건짜리 발사장과 21건짜리가 똑같이 빨강**이었다(점 하나로 이미 색이 포화).
  → `heatScale()` 이 지금 보고 있는 집합의 최다 건수에 맞춰 매번 다시 잡는다
- **MapLibre 커널 계수(GAUSS_COEF ≈ 0.399)를 반영해야 한다.** 점마다 얹히는 값은 weight 가
  아니라 `weight × 0.399` 라, `1/max` 로는 최다 발사장이 램프의 40% 에서 멈춘다 —
  두 번째 캡처에서 21건짜리가 파랑이었다. **세 캡처 모두 테스트는 전부 초록이었다**
- **`heatmap-intensity` 를 줌에 따라 바꾸지 않는다.** 강도가 줌마다 달라지면 같은 색이
  줌마다 다른 건수를 뜻하게 되어 범례의 눈금이 거짓말이 된다
- **클러스터 원은 숨기지 않고 흐린다**(0.92 → 0.3). 클러스터를 숨기면 그 안에 든 발사는
  개별 점으로도 안 나와 **저줌에서 클릭할 대상이 통째로 사라진다**
- **범례는 지도에 찍힌 것만 센다.** 좌표 없는 발사까지 세면 범례 자체가 거짓말이 된다
  (마커·히트맵·범례가 `hasCoords` 한 술어를 공유한다)

### 검증
- `tests/test_frontend.js` 에 히트맵 항목 추가(총 358) — 변이 10종을 모두 검출
- 하네스 지도 스텁이 `setPaintProperty`/`setLayoutProperty` 값을 남기게 했다.
  "켜면 다른 레이어가 같이 흐려지는" 배선은 `setData` 로는 안 보인다

## [1.14.0] - 2026-09-12

Phase 12 ⑪번(P12-12 — 업데이트 확인). 선행 조건이던 P12-22(밀린 릴리스)는 전날 해소했다.
**새 버전이 나와도 알 방법이 없었다.**

### 추가
- **새 버전 안내 배지** — 시작할 때 GitHub 최신 릴리스를 확인하고, 지금 버전보다 높으면
  지도 좌하단에 `⬆ 새 버전 v1.15.0 가 있습니다 (현재 v1.14.0) — 눌러서 받기`.
  본문을 누르면 릴리스 페이지가 열리고, `✕` 는 **그 버전만** 닫는다(다음 버전이 나오면 다시 뜬다)
- `api_client.check_update()` · `Api.check_update()` — 비교까지 파이썬에서 끝내고 JS 는 표시만 한다

### 설계 메모
- **`update_available` 을 캐시에 저장하지 않는다.** bool 을 굳혀 두면 사용자가 새 버전으로
  바꾼 뒤에도 TTL(하루) 동안 계속 "새 버전이 있다"고 말한다 — **예외도 안 나고 화면만
  조용히 틀린다.** 캐시에는 `latest`·`url`·`name` 만 넣고, 비교는 **매번 지금의 버전**으로 다시 한다
- **버전 비교는 문자열이 아니라 숫자 튜플로.** 문자열 비교면 `"1.9.0" > "1.13.0"` 이 참이 된다 —
  이 앱이 실제로 지나온 구간이다
- **읽을 수 없는 태그는 "업데이트 없음"으로 본다.** `nightly` 같은 태그 때문에 **없는 새 버전을
  알리는 쪽**이 놓치는 쪽보다 나쁘다
- **확인 실패는 화면에 띄우지 않는다.** 업데이트 확인은 앱의 기능이 아니라 부가물이라,
  실패하면 배지가 안 뜰 뿐이어야 한다. 오프라인이면 오래된 캐시로라도 알던 최신 버전을 쓴다
- **TTL 하루.** 비인증 GitHub API 는 IP 당 시간당 60회다 — 넉넉하다
- **닫은 버전은 `settings.json` 에 남긴다.** 세션 한정으로 두면 실행할 때마다 같은 배지를 다시 닫아야 한다
- 스모크는 **소스 도달 여부만** 잰다. 현재 버전은 앱이 넘기는 것이라 `api_client` 가 읽지 않는다 —
  `main.py` 를 import 하면 스모크가 webview(GUI)에 딸려간다

### 검증
- 파이썬 회귀 28 → **46**(버전 비교 8 · 확인/캐시/폴백 10). 변이 **6건 전부 잡힘** —
  문자열 비교 · **판정을 캐시에 굳히기** · 못 읽는 버전을 업데이트로 · TTL 반전 ·
  폴백 제거 · prerelease 꼬리
- 프론트 회귀 293 → **319**. 변이 **10건 전부 잡힘**(판정 7 + **배선 3**)
- **하네스가 부트 경로 전체를 탈 수 있게 됐다** — `maplibregl` 스텁이 없어 `initMap` 이
  던지는 바람에 `pywebviewready` 를 재현할 수 없었고, 그래서 처음 쓴 배선 테스트는
  **boot.js 의 호출 한 줄을 지워도 통과**했다(변이가 잡아냈다). 스텁을 `harness.js` 에 넣어
  `win.fire("pywebviewready")` 로 실제 부트를 재게 했다
- 화면 확인: 캐시에 더 높은 버전을 심어 실제 경로(캐시 → 비교 → 배지)를 그대로 태우고 캡처.
  **처음엔 배지가 안 떴는데 원인이 BOM 이었다** — PowerShell 5.1 의 `-Encoding utf8` 이 BOM 을
  붙여 `json.load` 가 실패했고, 캐시 미스로 판정돼 실제 API 를 다시 불러 덮어썼다(재현 절차의 실수)

## [1.13.0] - 2026-09-11

Phase 12 ⑨번(P12-16 — 궤적 다중 주기 · 미래 위치 스크럽).
**"3시간 뒤 어디"를 물어볼 방법이 없었다.**

### 추가
- **앞으로 볼 시간 슬라이더**(위성 컨트롤, 0~360분) — 지상궤적선이 앞으로 그만큼 이어지고,
  그 시각 위치에 `2시간 뒤` 점선 배지가 선다. 슬라이더를 끌면 배지가 궤적을 따라 움직인다
- 컨트롤 라벨에 **그때의 좌표**도 적는다 — `2시간 뒤 · -37.1°, -95.5°`

### 설계 메모
- **앞쪽만 늘린다.** 뒤쪽(지나온 길)까지 늘리면 화면이 선으로 덮여 지금 어디인지가 묻힌다
- **점 간격을 범위에 맞춰 성기게 잡는다**(`ceil(span/240)`). 6시간을 1분 간격으로 그리면
  400점이 넘고 30초마다 다시 계산된다 — 그 해상도는 화면에서 구분되지도 않는다.
  처음 `round` 로 썼다가 **정지궤도+6시간에서 257점**이 나와 상한을 넘었다(테스트가 잡았다)
- **라벨은 DOM 마커로 그린다.** MapLibre 의 symbol text 는 쓰지 않는다 — WebView2 에서
  온라인 glyphs 요청이 지도 전체 렌더를 막은 적이 있다
- **슬라이더 값은 저장하지 않는다.** 일시적으로 미래를 들여다보는 상태라, 다음 실행에
  복원되면 "왜 궤적이 이상하게 길지"가 된다

### 검증
- 프론트 회귀 259 → **293**. 순수 함수(`trackWindow`·`aheadLabel`·`aheadStatus`)와
  실제 SGP4 불변식(1주기 뒤 위도가 거의 같다 · 경도는 서쪽으로 밀린다)을 함께 잰다
- 변이 **11건 전부 잡힘** — 뒤쪽도 늘리기 · 음수 방어 제거 · 간격 고정 · 미래 오프셋 무시 ·
  라벨 2종 · 궤적이 연장 무시 · **슬라이더 배선 제거** · 마커 그리기 제거 · 선택 해제 시 마커 방치
- **화면 확인이 실제 문제를 하나 찾았다**: 확대된 상태에서는 2시간 뒤 위치가 **화면 밖**이라
  슬라이더를 올려도 아무 일도 안 일어난 것처럼 보였다 → 컨트롤에 좌표를 함께 적었다.
  *테스트로는 안 나왔을 종류의 결함이다*

## [1.12.0] - 2026-09-11

Phase 12 ⑧번(P12-20 파이썬 테스트를 파싱 밖으로 + P12-6 아카이브 잘림).
**요청이 두 배로 늘어도 화면은 똑같이 보인다 — 429 가 뜨고 나서야 안다.**

### 추가
- **`tests/test_cache.py` (28건)** — HTTP 를 주입해 **언제 부르고 실패하면 무엇을 돌려주는지**를 잰다:
  TTL 만료·경계·`force` · 오래된 캐시 폴백(오프라인) · 깨진 캐시 파일 · 아카이브 영구 캐시와
  올해 갱신 · 페이지 상한 · 페이지 사이 딜레이 횟수 · 설정 얕은 병합 · 위성 그룹 중복 제거.
  네트워크 없이 0.06초에 돈다 → CI 에 그대로 올렸다
- **아카이브 잘림 표시(P12-6)** — 연도당 페이지 상한에 걸리면 `truncated` 로 알리고, 화면에
  `⚠ 2022년 341건 추가됨 — 연도당 상한에 걸려 일부만 받았습니다`. 로그에도 남는다

### 설계 메모
- **잘린 해는 통계에서 완전한 해로 세지 않는다.** v1.11.0 이 "✓ 연도 전체를 불러온 해"를
  표시하기 시작했는데, 잘린 해를 거기 넣으면 **그 표시 자체가 새 거짓말**이 된다.
  `completeYears()` = 불러온 해 ∖ 잘린 해
- **잘림 여부를 캐시에 함께 저장한다.** 안 그러면 두 번째 실행부터 조용해져
  "처음 열었을 때만 경고가 뜨는" 더 나쁜 모양이 된다
- **옛 캐시(리스트)를 그대로 읽는다.** `%APPDATA%` 에 이미 깔려 있는 파일이라, 형식을 바꾸면서
  못 읽으면 사용자가 멀쩡한 아카이브를 다시 받게 된다. 실측 확인: `archive_2025.json`(리스트)
  → 341건, 재요청 없음
- 테스트는 모듈 전역(`CACHE_DIR`·`SETTINGS_PATH`)을 임시 디렉터리로 **모듈 경유 재대입**한다
  (전역 규칙 8번 — 이름 import 였으면 재대입이 안 보인다)

### 검증
- 파이썬 회귀 63 + **28** · 프론트 회귀 248 → **259**
- 변이 **12건 전부 잡힘** — TTL 판정 제거 · TTL 0 · 페이지 상한 무력화 · 잘림 표시 제거 ·
  캐시에서 잘림 표시 잃기 · stale 폴백 제거 · 설정 병합 제거 · 위성 중복 제거 무력화 ·
  잘린 해를 완전으로 세기 · 잘린 해 기록 제거 · 잘림 안내 제거
- **변이가 구멍을 하나 잡아냈다**: `loadArchive` 가 잘린 해를 기록하는 **한 줄을 지워도 통과**했다.
  `truncatedYears` 를 테스트가 직접 채워 재고 있었기 때문이다 — 배선 테스트를 더해 막았다
  (v1.11.0 의 `keydown` 배선과 같은 교훈이 이틀 연속 나왔다)
- 화면은 잘린 캐시를 심어 띄우고 캡처로 확인(확인 후 가짜 캐시 삭제). 창 좌표 `L=2880` ∈ 디스플레이 2

## [1.11.0] - 2026-09-11

Phase 12 ⑦번(P12-7 통계 모수 + P12-14 키보드) — 둘 다 작아서 한 묶음.

### 추가
- **통계의 모수를 밝힌다(P12-7)** — 포함된 기간(`2025년 1월 4일 ~ 2026년 12월 31일`),
  성공률의 분모(`확정 391건 중`), 그리고 **어느 해가 부분 표본인지**(`2026 (일부)` + 경고 박스).
  연도 전체를 불러온 해는 따로 `✓` 로 밝힌다
- **키보드 단축키(P12-14)** — `/` 검색 · `S` 사이드바 · `R` 새로고침 · `1`~`4` 결과 필터 ·
  `5` 위성 · `6` 낮/밤 · `Esc` 닫기 · `?` 도움말. 검색창 placeholder 에 `( / )` 힌트를 넣었다

### 설계 메모
- **모수 문구 자체는 이미 있었다.** 계획서에는 "화면엔 그 말이 없다"고 적혀 있었지만
  `현재 불러온 N건 기준` 은 v1.x 부터 있었다 — 실제로 없던 것은 **어느 연도가 잘린 표본인가**다.
  실측(2026-09-11): 라이브 98건이 전부 2026년인데 **가장 이른 것이 7-14**다.
  `previous` 50건뿐이라 올해 1~6월이 통째로 빠졌는데도 막대는 `2026: 98` 로 선다
- **Esc 는 위에 덮인 것부터 하나씩** 닫는다(도움말 → 상세 → 통과 → 통계 → 그룹 → 위성 컨트롤 → 사이드바).
  한꺼번에 닫으면 "상세만 닫고 목록은 두기"가 안 되고, 우선순위가 없으면 뒤쪽이 먼저 닫혀
  **아무 반응이 없는 것처럼** 보인다
- **숫자키 토글은 체크박스의 `change` 경로를 그대로 탄다.** 상태만 바꾸면 설정 저장이 빠져
  **다음 실행에 조용히 사라진다** — 같은 동작에 경로가 둘이면 한쪽만 고치게 된다
- 판정(`keyAction`·`escapeTarget`)과 실행을 나눠 `web/js/keys.js` 로 뒀다

### 수정
- **또 하나의 시각 의존 테스트를 고쳤다(잠복 flaky).** "정지궤도 가시 구간은 통과 전체보다 짧다"는
  단언이 **저녁에 실패**했다 — 가시 구간은 `[visStart, visEnd]` **하나**라 중간의 낮을 표현하지
  못하고, 창의 시작과 끝이 모두 밤이면 구간이 창 전체와 같아진다. **주장 자체가 틀린 자리였다.**
  태양표를 주입해("내내 밝음" → 가시 0건, "내내 어두움" → 전부 가시) 시각과 무관하게 배선만 잰다

### 검증
- 프론트 회귀 **+46건**(202 → 248). 변이 **11건 전부 잡힘**(입력 중 단축키 차단 제거 ·
  조합키 가로채기 · 대소문자 정규화 · Esc 우선순위 2종 · `change` 경로 생략 ·
  연도 판정 전부 완전 · `(일부)` 표시 제거 · 경고 제거 · 어두움 조건 제거 · **keydown 배선 제거**)
- **배선까지 잰다** — 하네스가 `document` 핸들러를 노출하게 해서 `bindUI()` 가 `keydown` 을
  실제로 붙이는지 확인한다. 로직이 다 맞아도 그 한 줄이 없으면 앱에서는 아무 키도 안 먹는다
- 화면은 임시 스크립트로 통계·도움말을 띄워 캡처로 확인(클릭 자동화 없음, 확인 후 제거).
  창 좌표 `L=2880` ∈ 디스플레이 2

## [1.10.0] - 2026-09-11

Phase 12 ⑥번(P12-3 — 발사 카운트다운 집중 화면).
**티커는 5초마다 흘러가서 "지금 곧 발사"라는 사실이 약하다.**

### 추가
- **발사 임박 집중 화면** — T-60분 ~ T+30분 사이의 발사가 있으면 지도 하단 중앙에 카드를 띄운다.
  큰 카운트다운(초당 갱신) + 미션 패치 + 로켓·발사장 + 중계 버튼(생중계 중이면 ● 배지) +
  `발사장 보기`(상세 패널). 처음 뜰 때 지도를 발사장으로 옮긴다 — 카드만 보고는 어디인지 알 수 없다
- `✕` 로 닫으면 그 발사는 이 실행 동안 다시 뜨지 않고, **다음 임박 발사가 있으면 바로 이어 띄운다**

### 설계 메모
- **`net_precision` 이 Second/Minute 인 발사만 띄운다.** 날짜만 확정된 발사(`Day`·`Month`)에
  `T-00:47:12` 를 붙이면 **없는 정밀도를 지어내는 것**이다. 실측 캐시 97건 중 초/분 확정은 58건뿐이다
- 대상이 바뀔 때만 카드를 다시 그리고, 매초는 숫자만 갈아 끼운다(1초마다 innerHTML 재생성 회피)
- 자리는 하단 중앙 — 좌측 사이드바(300px)·우측 상세 패널(320px)·하단 타임라인을 피한 유일한 빈 곳
- 선정 규칙을 `pickFocusLaunch(list, now, dismissed)` **순수 함수**로 떼어냈다.
  화면엔 카드가 하나 떠 있을 뿐이라 **잘못된 발사를 골라도 눈으로는 똑같다**

### 수정
- **`net_precision` 경고가 일부 값에서 조용히 안 뜨고 있었다.** LL2 는 `"Quarter 4"`·`"Year Half 2"`
  처럼 뒤에 숫자를 붙여 보내는데 표에는 `"Quarter"` 만 있어 조회가 빗나갔다(캐시 97건 중 8건).
  `netPrecisionNote()` 로 앞 단어를 맞추고, **모르는 값도 원문으로 알린다** — 넘어가면 정밀도를 속이게 된다

### 검증
- 프론트 회귀 테스트 **+34건**(180 → 202). 대상은 고르는 규칙: 창 경계(T-61/T-59/T+29/T+31) ·
  정밀도 거름 · 임박 순 · 닫기 기억 · 깨진 `net` · 이스케이프
- **변이 7건을 돌려 전부 잡히는 것을 확인**했다(정밀도 완화 · 창 확대 2종 · 닫기 기억 제거 ·
  정렬 뒤집기 · 이스케이프 제거 · Year Half 처리 제거)
- **배선 테스트가 처음에 시각 의존이었다** — 고정 `NOW` 로 만든 `net` 을 실제 `Date.now()` 를 보는
  `updateFocus()` 에 넘겨, 실행 시각에 따라 결과가 달라졌다. ⑤번에서 고친 flaky 와 같은 모양이라
  같은 방식으로 갈랐다(순수 함수는 고정 시각, 배선은 실시간 상대값)
- 화면 확인은 캐시의 임박 발사 1건을 조작해 띄우고 캡처로 확인했다(클릭 자동화 없음).
  창이 보조 모니터 `Bounds` 안인지 좌표로 확인 — `L=2880` ∈ 디스플레이 2

## [1.9.0] - 2026-09-11

Phase 12 ⑤번(P12-5b — P12-5 에서 범위 밖으로 미뤄 둔 절반).
**"밝게 보이는 위성"의 58%가 위성이 아니었다.**

### 추가
- **종류·소유국 필터** — 그룹 드롭다운에 `종류`(위성체/로켓 몸체/잔해)와 `소유국` 구역.
  개수를 함께 보여주고 **많은 순**으로 세운다(동수는 한국어 이름순 — 순서가 매번 바뀌면
  체크박스가 튄다). 설정에 저장되고, 대역 필터와 **함께(AND)** 걸린다
- 근거는 실측이다(2026-09-11): `visual` 159개 중 위성체는 **66개뿐이고 92개가 다 쓴
  로켓 몸체**, 1개가 잔해다. `stations` 21개에도 잔해가 2개 섞여 있다.
  실제 확인: 로켓 몸체를 끄니 툴바가 `위성 (90/174)` 로 바뀐다

### 설계 메모
- **켠 것이 아니라 "끈 것"만 저장한다.** 켠 목록으로 두면 새로 나타난 종류·나라가 목록에
  없어서 **조용히 숨겨진다** — SATCAT 의 나라 코드는 130종이고 그룹마다 달라진다.
  값이 없으면 보인다 = 기본은 전부 켜짐
- **필터 목록은 고정 표가 아니라 불러온 위성에서 뽑는다**(`satFacet`). 기본 그룹은
  소유국 13종, `geo` 를 켜면 53종이라 고정 목록은 반드시 어긋난다
- **메타가 없는 위성은 숨기지 않는다.** SATCAT 은 비동기로 늦게 오므로, 분류를 모르는 것을
  숨기면 위성이 사라졌다 나타난다
- 드롭다운에 **높이 제한과 스크롤**을 넣었다 — 소유국 53종이면 화면을 넘쳐 아래쪽을
  아예 누를 수 없었다

### 수정
- **시각에 따라 실패하던 회귀 테스트(잠복 flaky)** — 지상궤적선 테스트가
  "1주기면 반드시 날짜변경선을 넘는다"고 단언했는데 **그 전제가 틀렸다.** 실측으로
  경도가 156.4° → -177.9° 로 **연속 감소만 하고 넘지 않는** 구간이 있다. 오늘 아침엔
  통과했고 지금은 실패한다 — 언젠가 CI 를 빨갛게 만들었을 것이다. 분할 규칙을
  `splitAtDateline()` 순수 함수로 떼어내 **합성 입력으로 결정론적으로** 재고, 실제 SGP4
  쪽은 "가짜 선이 없다"만 확인한다. 경계(정확히 180°는 안 끊는다)도 함께 잰다
- **위성 설정 저장이 세 곳에 흩어져 있던 것** — 그룹·대역·토글이 각자 객체를 만들어,
  새 필드를 늘리면 **한 곳만 빠뜨려도 그 경로로 저장할 때 조용히 사라졌다.**
  실제로 이번에 그럴 뻔했다. `saveSatSettings()` 한 곳으로 모았다(전역 규칙 6번)

## [1.8.0] - 2026-09-11

Phase 12 ④번(P12-5). 위성 상세가 그동안 **TLE 에서 나오는 것뿐**이었다 —
이름·NORAD·궤도요소. 그 물체가 **무엇인지**는 알 수 없었다.

### 추가
- **위성 메타데이터(Celestrak SATCAT)** — 상세 패널에 타입(위성체/로켓 몸체/잔해)·소유국·
  운용 상태·발사일·발사장·국제 식별번호·크기·재진입일. 재진입했거나 위성체가 아닌 물체는
  **배지로 먼저** 알린다(표 안의 한 줄은 눈에 안 들어온다)
- 새 파일 `satcat_codes.py` — 코드→사람 말 표. `api_client.py` 가 더 커지지 않게 떼어 놨고,
  아무것도 import 하지 않는다. 터미널 확인 `python satcat_codes.py`
- `python api_client.py` 스모크에 `[OK] satcat` 한 줄 추가(새 데이터 소스의 프로젝트 규칙)

### 설계 메모
- **TLE 와 같은 GROUP 단위로 받는다.** 전체 `satcat.csv` 는 6.7MB 지만 그룹별은
  2KB~1MB 다(2026-09-11 실측: stations 2KB · visual 14KB · geo 54KB · starlink 983KB).
  캐시 구조도 TLE 와 같게 맞췄다 — 조인은 프론트에서 NORAD 로
- **TTL 24시간.** 발사일·소유국은 거의 안 변하고, Celestrak 은 같은 데이터를 짧은 간격으로
  거듭 받으면 403 으로 막는다(v1.5.1 위성 상한의 근거와 같은 제약)
- **메타가 없어도 위성은 그대로 뜬다.** 로드를 위성 표시와 분리했고(await 하지 않는다),
  값이 없는 행은 **아예 그리지 않는다** — 없는 것을 "알 수 없음"으로 채우면 화면이 거짓을 말한다
- **모르는 코드는 원문 그대로.** OWNER 는 130종이라 전부 적지 않기로 했고(실측), 표에 없으면
  코드라도 보여준다. 뭉개면 정보가 사라진다
- 표의 범위는 실측이다 — `satcat.csv` 전체 70,649행에서 OBJECT_TYPE 은 4종, OPS_STATUS 는
  8종뿐이라 **전부** 덮었고, LAUNCH_SITE 37종 중 주요 발사장을 덮었다
- 회귀 테스트 63(파이썬) + 140(프론트). **변이 4건을 돌려 전부 잡히는 것을 확인**했다
  (RCS 경계 부등호 · 빈 RCS 를 0 으로 읽기 · 모르는 코드 뭉개기 · 깨진 행에서 전체 중단).
  등가 변이 1건은 걸러냈다

## [1.7.1] - 2026-09-11

CARTO 가 API 키를 요구하기 시작해 지도 배경이 워터마크로 덮였다. 배경을 교체하고,
그 과정에서 개발 도구 버그를 하나 찾았다.

### 수정
- **다크 배경 지도를 Esri Dark Gray Canvas 로 교체** — CARTO(`basemaps.cartocdn.com/dark_all`)가
  **HTTP 200 으로 `API KEY REQUIRED` 워터마크가 찍힌 타일**을 보낸다. 오류가 아니라 정상
  응답이라 오프라인 배지(P11-7)도 못 잡는 **조용한 실패**다. Esri 는 위성사진 배경으로 이미
  쓰는 호스트라 외부 의존이 늘지 않는다. 소스·레이어 id 도 `carto` → `darkbase` 로 바꿨다
  (이름이 거짓이 되면 다음 사람이 엉뚱한 곳을 찾는다)
- **`RL3D_DEV_MONITOR=2` 가 주 모니터에 창을 띄우던 것** — `Screen.AllScreens` 의 순서는
  사용자가 보는 "디스플레이 1/2"와 **무관하다**. 이 PC 실측: `AllScreens[0]` 이 보조,
  `[1]` 이 주였다. 그래서 `=2` 가 주 모니터를 가리켰고, **"보조에서만 테스트한다"는
  프로젝트 규칙이 조용히 지켜지지 않고 있었다.** `order_screens()` 로 주 모니터를 1번으로
  정렬한다. 회귀 테스트 5건 추가(48 → 53)

### 문서
- **README 스크린샷 갱신** — v1.5.1 화면은 이제 존재하지 않는 배경(CARTO)을 보여줬다.
  `settings.json` 을 미리 써두고 재실행 → PrintWindow 로 캡처(P11 검증 메모의 방법 그대로)

## [1.7.0] - 2026-09-11

Phase 12 ③번 묶음(P12-1 · P12-2). **"지나간다"와 "눈에 보인다"를 구분한다.**

### 추가
- **가시 통과 판정** (P12-1) — 통과 예측이 그동안 **기하학적 통과를 전부** 냈다.
  `visual`(밝게 보이는 위성) 그룹의 존재 이유가 "실제로 보이는 것"인데 그 판정이 없었다.
  두 조건을 함께 본다: ① 관측지의 태양 고도 **−6° 미만**(시민박명 종료) ② 위성이
  지구 그림자 밖(원통 근사). 통과 목록에 ☀ 로 표시하고, 가시 통과는 **조명이 시작되는
  시각·그 구간의 최대고도**를 앞세운다 — 알고 싶은 값이 통과 시작이 아니라 그것이다
- **`🌙 오늘 밤` 사이드바 탭** (P12-2) — 지금까지는 위성을 **1개 골라야** 통과를 볼 수
  있어 무엇을 고를지 모르면 못 썼다. 로드된 위성 전체를 훑어 24시간 가시 통과를
  **최대고도 순**으로 세운다. 행을 누르면 그 위성이 선택된다
- **`눈에 보이는 통과만` 체크박스** (기본 켜짐, 설정에 저장) — 끄면 낮 통과까지 나온다.
  전파 수신·안테나 목적이면 낮 통과도 의미가 있어 버리지 않았다

### 설계 메모
- **외부 API 를 쓰지 않는다.** 태양 위치는 터미네이터(P6-3)가 쓰던 `map.js` 의
  `sunEquatorial`·`gmstHours` 를 그대로 재사용한다 — 같은 계산을 두 벌 만들지 않는다
- **측정이 구현을 지웠다** — 계획서에 "거친 스캔 → 후보 구간 정밀화" 2단계를 적어 뒀으나,
  재보니 `visual` 157개 × 24시간 전수 스캔이 **30초 간격 413ms**(전파 45만 회)였다.
  2단계는 불필요. 대신 태양 위치는 시각당 한 번만 계산해 위성들이 공유하고, 관측자가
  밝은 시간대는 조명 판정 자체를 건너뛴다. P11-6 과 같은 결론이다
- 회귀 테스트 102 → 123. **변이 실험으로 세 번 고쳐 썼다** — 아래 참조

### 수정
- **테스트 하네스: vm realm 의 `Date`** — `tests/harness.js` 가 `date(ms)` 를 노출한다.
  vm 컨텍스트는 별도 realm 이라 Node 쪽 `Date` 를 satellite.js 에 넘기면 `instanceof Date`
  가 실패해 **예외 없이 NaN 좌표**가 나온다. P12-1 테스트를 쓰다 실제로 당했다

## [미출시] — 2026-09-11

Phase 12 ②번 묶음(P12-21 · P12-13). **앱 코드는 건드리지 않았다** — 버전을 올리지 않는다.

### 추가
- **CI** (P12-13) — `.github/workflows/tests.yml`. push·PR 마다 **windows-latest** 에서
  파싱 회귀(48) + 프론트 회귀(102) + 로그 스모크. 이 앱은 Windows exe 이고 `startup.py` 는
  `winreg` 를 만지므로 리눅스에서 도는 초록은 실제를 대변하지 못한다.
  네트워크를 쓰는 `api_client.py` 스모크와, 러너에 WebView2 가 없어 반드시 실패하는
  `startup.py` 스모크는 **일부러 뺐다**

### 수정
- **스모크가 영문 로캘 콘솔에서 죽던 것** — CI 첫 실행이 바로 여기서 깨졌다.
  한글 `print` 가 `cp1252` 에서 `UnicodeEncodeError` 를 낸다. **이 PC 는 UTF-8 로캘이고
  한국어 PC 는 cp949 라 한글이 넘어가서, 개발 PC 에서는 재현되지 않는 버그였다.**
  세 스모크(`applog`·`startup`·`api_client`)가 stdout 을 `utf-8`/`errors="replace"` 로
  재설정하게 하고, 워크플로에도 `PYTHONIOENCODING: utf-8` 을 넣었다(이중 방어).
  로그 파일 자체는 `encoding="utf-8"` 명시라 멀쩡했다 — 깨진 건 표준출력뿐이다.
  회귀 테스트 2건 추가(46 → 48): `PYTHONIOENCODING=cp1252` 로 자식 stdio 만 되돌려
  재현하고, **보호를 뜯으면 실제로 깨지는지**까지 함께 단언한다

### 문서
- **PLAN.md 사후 정리** (P12-21) — 로드맵에 없던 작업 6건(v1.5.2 버그 3건, 개발 노트 2편,
  README 노트 절, GitHub Pages, 언어 표기 진단, 페이지 가독성)을 **11.5절**로 정리했다.
  적어두지 않으면 다음 세션이 "이건 왜 있지"를 다시 조사한다
- **계획서가 실제와 어긋난 곳 셋을 고쳤다** — 5절 파일 구조에 `applog.py`·`startup.py`·
  `tests/`·`docs/` 누락, 10절 리스크의 캐싱 "30분"(실제 15분), WebView2 안내(이제 코드에 있다)
- **README 상단 버전이 v1.5.2 로 남아 있던 것** — v1.6.0 커밋에서 놓쳤다. CI 배지와 함께 갱신

## [1.6.0] - 2026-09-11

Phase 12 의 첫 덩어리(P12-9/10/11). **앱이 겪은 실패의 신호를 만드는 일** — 화면과 기능은 그대로다.
이전까지는 `logging` 이 코드에 한 줄도 없었고, exe 는 `pythonw` 로 도는 셈이라 stderr 도 사라져
사용자가 겪은 실패를 뒤에서 확인할 방법이 **아예 없었다.**

### 추가
- **로그 파일** (P12-9) - `%APPDATA%\RL3D\logs\rl3d.log` 에 회전 로그(512KB x 4, `encoding="utf-8"` 명시).
  그동안 조용히 삼켜지던 자리(캐시 읽기/쓰기 실패, 설정 저장 실패, 발사 1건 파싱 실패, 네트워크 조회
  실패, 창 상태 저장 실패)에 사유를 남긴다. **동작은 바꾸지 않았다** - 실패해도 화면이 비지 않는 기존
  폴백은 그대로다. 잡히지 않은 예외도 `sys.excepthook` 으로 남긴다
- **시작 실패 안내** (P12-10) - 시작 중 예외가 나면 창이 그냥 안 떴다. 예외 요약과 **로그 파일 경로**를
  담은 안내 대화상자를 띄운다
- **WebView2 부재 안내** (P12-11) - 런타임이 없으면 흰 화면만 떴다(문서엔 "안내"라고 적혀 있었지만
  코드에 없었다). EdgeUpdate 레지스트리의 `pv` 값으로 감지해 설치 경로를 안내한다. 안내 후에도 앱은
  계속 뜬다 - 사용자가 상황을 보게

### 설계 메모
- **순환 import 를 피하려고 경로 상수를 옮기지 않았다.** 모듈은 `logging.getLogger(__name__)` 만 쓰고
  핸들러 설정은 진입점(`main.py`)에서 1회만 한다 -> `applog` 가 `api_client` 를 import 할 일이 없다.
  경로 상수는 `api_client.py` 상단에 그대로 남고, `tests/test_parsing.py` 의 `CACHE_DIR` 재대입 방식도
  손대지 않았다. (계획서가 "먼저 정하라"고 남긴 결정 - 옮기는 쪽은 테스트까지 깨졌을 것이다)
- **레지스트리 읽기와 대화상자는 주입 가능하게** 떼어 놨다. 마우스 자동화 없이 판정만 잰다(프로젝트 규칙)
- 회귀 테스트 12건 추가(34 -> 46). 그중 하나가 실제 구멍을 잡았다 - 빈 문자열 판정이 레지스트리 리더
  안에만 있어 **주입한 리더는 그 규칙을 비껴갔다.** 판정을 `webview2_version` 한 곳으로 옮겼다

## [1.5.2] — 2026-08-20

정기 점검에서 찾은 버그 3건. 화면과 기능은 그대로다.

### 수정
- **타임아웃을 "인터넷 연결을 확인하세요" 로 잘못 안내하던 것** — `urlopen` 은 타임아웃을 그대로
  던지지 않고 `URLError(reason=timeout)` 으로 감싼다. `URLError` 검사가 위에 있어 서버가 느릴 뿐인
  상황에서도 연결 문제라고 안내했고, `isinstance(e, TimeoutError)` 분기는 **한 번도 실행되지 않는
  죽은 코드**였다. 실제 타임아웃을 유발해 확인한 뒤 `reason` 까지 보도록 고쳤다
- **403 전용 안내 문구 추가** — 그동안 `서버 응답 오류(403).` 로만 보였다. 403 은 이 앱이 실제로
  겪는 실패다(Celestrak 은 대형 그룹을 짧은 간격으로 거듭 받으면 막는다 — v1.5.1 위성 상한의 근거).
  상태코드→문구를 `HTTP_ERROR_MESSAGES` 표 한 곳으로 모으고 404·500·502·503·504 도 함께 채웠다
- **창을 화면 밖으로 복원하던 것** — 보조 모니터에서 창을 옮긴 뒤 그 모니터를 떼면 저장된 좌표가
  어느 화면에도 없는 자리가 된다. 그대로 복원하면 창이 보이지 않는 곳에 떠서 **앱이 안 뜬 것처럼
  보인다.** 복원 좌표가 어느 모니터든 최소 120×40px 이상 걸치는지 확인하고, 아니면 OS 기본 위치로 넘긴다
- **쓰지 않는 옛 캐시 정리** — 그룹별 TLE 캐시로 바꾸기 전의 `tle.json` 이 `%APPDATA%` 에 남아
  있었다(36KB). 시작할 때 한 번 지운다

### 검증
- 파싱 회귀 17 → **34항목**(에러 문구·창 위치 판정·캐시 정리 추가). 뮤테이션 5종
  (타임아웃 `reason` 검사 제거 · 403 문구 제거 · 캐시 미삭제 · 최소 노출폭 0 · 창 방어 무력화)을
  모두 검출하는지 확인했다. 경계 테스트는 **좌표를 직접 적는다** — 처음엔 `MIN_VISIBLE_W` 로
  계산했더니 그 상수를 0 으로 바꿔도 기대값이 같이 움직여 통과해 버렸다

## [1.5.1] — 2026-07-26

### 수정
- **위성 개수 상한 조정** — 정지궤도(GEO)는 상한을 없애 568개 전부, Starlink 는 300 → 2,000.
  P11-6(렌더 성능)을 검토하며 실제로 측정한 결과 **위성 8,000개에서도 초당 갱신이 동기 28ms ·
  화면 반영 33ms** 로 렌더가 병목이 아니었다. 반면 GEO 는 실제 568개인데 200개로 자르고 있었다.
  Starlink 만 상한을 남긴 이유는 성능이 아니라 **Celestrak 쪽 제약** — 전체(10,776개 · 1.7MB)를
  짧은 간격으로 거듭 받으면 403 으로 막힌다. 상한은 지도에 그릴 개수만 줄이고 다운로드는 줄이지
  않는다는 점도 코드 주석에 남겼다

## [1.5.0] — 2026-07-26

로드맵 P11-7.

### 추가
- **오프라인 안내 배지** — 배경 타일만 온라인 전용이라, 오프라인이면 발사·위성은 저장된 캐시로 뜨는데
  지도만 비어 보인다. 이 상태를 앱 고장으로 오해하지 않게 지도 좌하단에 안내 배지를 띄운다.
  타일이 **연속 3회** 실패했을 때만 뜨고(타일 한두 개 실패는 흔하다), 타일이 다시 받아지면 스스로 사라진다.
  브라우저 `online`/`offline` 이벤트에도 반응하며, 눌러서 닫으면 그 세션에선 다시 뜨지 않는다

## [1.4.0] — 2026-07-26

로드맵 P11-3. 이미 받아둔 TLE만 걸러내는 기능이라 API 요청은 늘지 않는다.

### 추가
- **궤도 대역 필터** — 툴바 `그룹 ▾` 안에 저궤도(LEO)·중궤도(MEO)·정지궤도(GEO) 체크박스를 뒀다.
  그룹 선택이 "무엇을 받을지"였다면 이쪽은 "받은 것 중 무엇을 볼지"라, 끄고 켜도 재요청 없이 즉시 반영된다.
  대역은 평균운동에서 얻은 **평균 고도**로 나눈다 — 실시간 고도(점 색)와 달리 타원 궤도도 한 대역에 머문다.
  툴바 위성 개수는 필터가 걸리면 `(보이는 수/전체)`로 바뀌고, 사이드바 위성 목록에도 대역이 함께 표시된다.
  관심 위성은 필터와 무관하게 남긴다 — 관심 탭에서 골랐는데 지도에 점이 없으면 고장으로 보이기 때문
- 선택된 대역은 `settings.json`에 저장된다. 단 **세 대역이 모두 꺼진 설정은 무시**한다(위성이 하나도 안 보여 앱이 고장난 것처럼 된다)

## [1.3.0] — 2026-07-26

로드맵 Phase 11 착수분(P11-1·P11-2·P11-4). 셋 다 이미 가진 데이터·설정 파일만 쓰므로 API 요청은 늘지 않는다.

### 추가
- **관점 화면 확장 (기관 · 로켓)** — 1.2.0의 발사장 화면을 발사장/기관/로켓 공용으로 일반화했다.
  상세 패널의 기관명·로켓명도 클릭하면 그 대상만의 총 발사·성공률과 구성 막대를 본다
  (발사장은 로켓·기관·패드별, 기관/로켓은 로켓·발사장·연도별). 패드가 하나뿐인 발사장에선
  "패드별" 막대를 생략한다 — 총합과 같아 정보가 없다
- **관심 발사 · 위성** — 상세 패널의 `☆ 관심`으로 담아두면 지도에서 **금색 테두리로 강조**되고
  (발사 마커는 굵은 테두리, 위성 점은 크게) 사이드바에 **⭐ 관심** 탭으로 모인다.
  `settings.json`에 저장돼 다음 실행에도 남는다
- **지도 위치 복원** — 마지막으로 보던 지도 중심·줌을 저장해 다음 실행에 그대로 연다.
  지도를 움직일 때마다 파일을 쓰지 않도록 1초 디바운스를 두고, 추적 모드 중 위치는 저장하지 않는다

### 수정
- 위성 상세 패널이 매 초 통째로 다시 그려져 그 위의 버튼 클릭이 씹힐 수 있던 문제 —
  실시간 값(고도·속도·위치) 부분만 교체하도록 분리

## [1.2.0] — 2026-07-26

### 추가
- **위성 검색·목록** — 목록 사이드바에 `🚀 발사 / 🛰 위성` 탭. 위성 탭에서 이름·NORAD로 검색하고
  행을 클릭하면 그 위성을 선택·추적하고 지도가 이동한다(수백 개 중 ISS 찾기가 목적).
  위성 레이어가 꺼져 있으면 그 사실을 안내
- **발사장 관점 화면** — 상세 패널의 발사장 이름을 클릭하면 그 발사장만의 성적을 본다.
  총 발사·성공률·예정, 결과별/주요 로켓/기관/패드별 막대, 예정·최근 발사 목록(클릭 시 상세로).
  패드 이름은 `Space Launch Complex 4E` → `SLC 4E` 로 축약해 좁은 라벨에서도 구분된다
- **배경 지도 전환** — 툴바에서 다크(CARTO) ↔ 위성사진(Esri World Imagery) 전환. 설정에 저장.
  두 레이어를 함께 두고 표시만 바꾸므로 마커·궤적·터미네이터가 다시 만들어지지 않는다
- **위성 고도별 색상** — 점 색이 궤도 대역을 나타낸다: 저궤도(하늘색) → 중궤도(보라) →
  GPS 대역(분홍) → 정지궤도(주황)

## [1.1.0] — 2026-07-25

발사 상세 패널 확장. **이미 받고 있던(`mode=detailed`) 데이터를 화면에 푸는 작업이라 API 요청은 늘지 않는다.**

### 추가
- **중계 링크** — 발사별 유튜브·X 중계를 버튼으로. 클릭 시 기본 브라우저에서 열림(앱 창 안에서 열지 않음).
  `webcast_live` 면 "● 생중계 중" 배지
- **발사 소식 타임라인** — 지연·재조정·리프트오프 등 최신 6건과 원문 링크
  (예: "Rescheduled to July 24 due to launch site weather" → "Liftoff" → "Ship has splashed down")
- **미션 패치 이미지**, **프로그램 태그**(Starlink, SpaceX Starship 등)
- **발사 윈도우** — "오전 07:45 ~ 오전 09:15 (1시간 30분)". 순간 발사면 표시하지 않음
- **시각 확정도 경고** — `net_precision` 이 초·분 단위가 아니면 "날짜만 확정 (시각 미정)" 등을 알려
  카운트다운을 곧이곧대로 믿지 않게 함
- **기록 맥락** — "이 발사대 285번째 · SpaceX 올해 90번째"
- **발사 확률 / 기상 우려** — 값이 있을 때만(주로 유인·NASA 발사)

### 기반
- `Api.open_url` 브릿지 — http/https 만 허용(file://·javascript: 등 거부)
- 회귀 테스트 5개 추가(중계 링크 상한·소식 정렬·확장 필드 부재 시 기본값 등), 총 17개

## [1.0.0] — 2026-07-25

첫 배포 버전. Phase 0~9 완료.

### 기능
- **발사 지도** — Launch Library 2 의 전 세계 발사를 MapLibre 2D 지도에 마커로 표시(예정/성공/실패/부분).
  클러스터링, 상세 패널(로켓·기관·미션·궤도·이미지·실패/지연 사유), 검색·결과별 필터, 타임라인 슬라이더
- **발사 목록 사이드바** — 필터·검색·타임라인이 적용된 목록. 행 클릭 시 해당 발사장으로 이동
- **과거 발사 아카이브** — 연도(최근 5년) 단위로 불러오기. 지난 연도는 영구 캐시
- **발사 통계 패널** — 총 발사·성공률·예정 + 결과별/연도별/기관/국가 막대(외부 차트 라이브러리 없이)
- **속보 티커** — 임박한 예정 발사 카운트다운과 최근 발사 결과를 섞어 순환
- **위성 실시간 추적** — Celestrak TLE + satellite.js(SGP4). 그룹 선택(stations·visual·Starlink·GPS 등),
  위성 상세(고도·속도·주기·경사각·이심률), 지상궤적선, 추적 모드
- **통과 예측** — 지도 클릭으로 관측 위치 지정 → 향후 24시간 통과(시각·방위·최대고도). 외부 API 없이 계산
- **낮/밤 터미네이터 오버레이** — 태양 위치로 야간 반구 음영. 분 단위 갱신
- **자동 갱신 / 설정 저장** — 5분 폴링 + "N분 전 갱신" 표시. 필터·토글·위성 그룹·관측 위치·창 상태 복원

### 기반
- 디스크 캐싱(`%APPDATA%\RL3D\cache\`) — 발사 15분 / TLE 2시간 / 아카이브 지난 연도 영구.
  API 실패 시 오래된 캐시라도 반환해 화면이 비지 않음
- 파싱 회귀 테스트(`tests/`) — 실제 응답 픽스처 기반, 네트워크 없이 실행
- PyInstaller onefile exe. MapLibre GL·satellite.js 는 오프라인 번들
  (지도 배경 타일과 발사/위성 데이터는 온라인 필요)

### 참고 — 만들었다가 뺀 것
- **발사 T-10분 Windows 토스트 알림**: 구현했으나 실사용에서 불필요하다고 판단해 1.0.0 전에 제거
  (`winotify` 의존성 포함). 위성 통과 알림도 같은 이유로 도입하지 않음
- **발사 실시간 텔레메트리**: 무료 공개 API 부재로 범위 밖
