# RL3D — 프로젝트 규칙

> 로켓 발사 & 위성 추적 데스크톱 앱. 세부 설계는 `PLAN.md` 참조.
>
> **공통 규칙(보고·작업 방식·검증·Git·문서·상수 위치·외부 API·Windows 앱·YAGNI)은
> 전역 `~/.claude/CLAUDE.md` 에 있다.** 여기에는 **이 앱에서 실제로 당한 것**만 적는다.
> 지금은 혼자 쓰는 초기 단계 → **일부러 가볍게** 둔다. 무거운 절차(버전체계·회귀·감사)는 필요가 실제로 생기면 도입.

## 프로젝트 개요

pywebview + PyInstaller 로 만든 Windows exe. 내부는 웹 UI(HTML/JS + MapLibre GL 2D 지도).
전 세계 로켓 발사(Launch Library 2)를 지도에 표시하고, 위성(Celestrak TLE)을 실시간으로 움직이게 한다.

| 파일 | 역할 |
|------|------|
| `main.py` | pywebview 창 생성 + `Api` 브릿지 클래스(JS→파이썬 호출 창구) |
| `api_client.py` | 외부 API **호출·캐싱**과 설정 저장. **엔드포인트 URL 상수는 전부 이 파일 상단에**. 테스트가 `CACHE_DIR`·`_http_get` 을 **재대입해서** 격리하므로, 여기 것을 다른 모듈로 옮길 때는 전역 8번을 먼저 본다 |
| `api_parsing.py` | 응답·문자열 → 값 (순수). `_parse_launch(es)`·`_parse_tle`·`_parse_satcat`·`parse_version`·`is_newer`. 네트워크도 캐시도 없다 |
| `api_errors.py` | 예외 → 사람이 읽는 말. 상태코드별 문구 표를 **한 곳에** |
| `applog.py` | 회전 로그 설정(P12-9). 핸들러는 여기서만 붙인다 — 다른 모듈은 `log` 로 기록만 |
| `startup.py` | WebView2 런타임 확인·크래시 안내(P12-10·P12-11) |
| `satcat_codes.py` | SATCAT 코드표(소유국·궤도·타입) — 숫자·약어를 한국어로 |
| `RL3D.spec` | PyInstaller 산출물. `build.bat` 이 플래그로 빌드하므로 **이 파일은 쓰이지 않는다**(생성물) |
| `web/index.html` · `style.css` | 레이아웃과 테마. `<script>` 순서가 곧 JS 의존 관계다 |
| `web/js/*.js` | UI 로직 23개 파일: `errors` → `state` → `utils` → `map` → `launches` → `sequence` → `focus` → `sats` → `satfilter` → `sattrack` → `satpass` → `observer` → `trajectory` → `favorites` → `sidebar` → `panels` → `satpanel` → `stats` → `keys` → `settings` → `update` → `firstrun` → `boot`. **ES 모듈이 아니라 클래식 스크립트** — 최상위 `let`·`function` 이 파일 간에 공유되므로 로드 순서를 지켜야 한다(`file://` 로 열려 모듈을 못 쓴다) |
| `web/lib/` | MapLibre GL JS, satellite.js (오프라인 번들) |
| `build.bat` | exe 빌드 (venv→설치→pyinstaller) |
| `tools/build_ne_land.py` | 오프라인 배경용 Natural Earth 육지 폴리곤 생성(P17-2). 네트워크가 필요하고, 결과(`web/lib/ne_land.js`)는 커밋한다 |
| `tests/` | 파싱 회귀(`test_parsing.py`) + 실제 응답 픽스처·골든, **캐시·폴백·아카이브·설정 회귀(`test_cache.py`)**, 프론트 회귀(`test_frontend.js` + `harness.js`) |
| `PLAN.md` | 개발 계획·마일스톤 |

```powershell
python main.py     # 개발 실행
build.bat          # exe 빌드 → dist\RL3D.exe
```

---

## API 규칙 — 이 앱의 실제 수치

> 외부 API 앱의 일반 규칙(캐싱 우선 · `.get()` 방어 · 에러는 사람 말로 · 실패 시 오래된
> 캐시 반환 · 터미널 스모크 유지)은 전역 규칙 7번. 아래는 **이 앱에만 해당하는 값**이다.

- **한도: Launch Library 2 는 시간당 약 15회**(초과 시 429). 이게 캐싱 설계의 출발점이다.
- **TTL: 발사 15분 / TLE 2시간.** 프론트는 5분마다 폴링하지만 실제 호출은 캐시 TTL 이
  제어한다 → **시간당 약 8요청**으로 한도 안에 든다. 폴링 주기를 줄이려면 TTL 을 먼저 본다.
- **캐시 위치: `%APPDATA%\RL3D\cache\`.** onefile exe 는 실행 폴더가 임시라 영구 위치 필수.
- **엔드포인트 URL 상수는 `api_client.py` 상단에.** (전역 6번의 이 앱 적용 지점)
- **정규화 결과의 모양을 바꾸면 `CACHE_SCHEMA` 를 올린다**(필드 추가 포함). 안 올리면
  **지난 연도 아카이브는 TTL 이 없어 영구**라 새 필드가 **영원히 안 채워진다** —
  오류도 경고도 없고, `launches.json` 은 TTL 15분이라 저절로 나아 **개발 중에는 보이지도 않는다**
  (2026-09-12 실제로 당했다: 필드 넷을 더했는데 2025년 아카이브에서만 조용히 빠져 있었다).
- **한 세션에 `CACHE_SCHEMA` 를 여러 번 올리면 시간당 한도를 태운다.** 스키마가 바뀌면
  `launches.json` 뿐 아니라 **연도 아카이브도 통째로 다시 받는다**(아카이브는 연도당 최대
  5페이지라 한 번에 요청을 많이 쓴다). 2026-09-13 에 한 세션에서 3→9 로 여섯 번 올렸고
  **429 에 걸려 그 뒤로는 실제 캐시로 검증을 못 했다.** 폴백은 설계대로 동작했지만
  (`[OK(stale)]` + 사람이 읽는 문구) 새 필드가 안 채워진 캐시만 남는다.
  - **여러 기능을 잇달아 넣는 날은 스키마를 마지막에 한 번만 올린다.**
  - 한도에 걸린 뒤의 검증은 **받아 둔 원본 응답을 `api_parsing` 으로 파싱해** `harness.js`
    로 렌더한다 — 네트워크 0이고 실제 LL2 데이터다.
- **터미널 스모크는 `python api_client.py`** — 소스·건수·에러를 `[OK]/[FAIL]` 로 찍는다.
  새 데이터 소스를 붙이면 여기에도 한 줄 추가한다.

---

## pywebview / 프론트엔드 규칙

1. **JS↔파이썬은 `window.pywebview.api.*` 브릿지로만.** 파이썬이 네트워크·캐싱을 담당하고,
   JS는 받은 JSON을 지도에 그리는 역할. 무거운 네트워크를 JS에서 직접 하지 않는다.
2. **사용자·API 문자열은 DOM에 넣기 전 반드시 이스케이프**(`escapeHtml`). 팝업·패널 XSS 방지.
3. **지도·위성 라이브러리와 해안선 데이터는 오프라인 번들**(`web/lib/`)로 exe 안에 포함.
   단 **지도 배경 타일과 발사/위성 데이터는 온라인 필요** — 이 전제를 README에 명시.
   **고해상도 타일을 받아 번들하는 길은 막혀 있다**(P17-2): Esri 가 *"다른 앱을 통해
   오프라인용으로 타일을 조직적으로 요청하는 것"* 을 금지한다. 오프라인 배경은
   퍼블릭 도메인 데이터(Natural Earth)로만 간다.

---

## 설계를 내기 전에 — "이게 최선인가"를 **내가** 먼저 묻는다

**사용자가 "그게 최선인가?"라고 물어야 알게 되면 늦다.** 2026-09-16 하루에 두 번 걸렸고,
**두 번 다 설계가 뒤집혔다.** 공통점은 하나다 — **재야 할 것 중 하나를 안 쟀다.**

| 언제 | 잰 것 | **안 쟀는데 결정적이던 것** | 결과 |
|---|---|---|---|
| P17-2 (오프라인 타일) | exe 증가 **1.35MB** (충분히 싸다) | **라이선스** | 원안 전부 폐기 — Esri 가 금지한다 |
| P19 (주기 갱신) | 요청 **+0.5회/시간** (한도 여유) | **UI 스레드 시간** | 주기화 불가(**115초**) + 기존 결함 발견 |

착수 전에 **네 가지를 스스로 묻고, 답을 설계에 적는다.** 답이 "모른다"면 그게 먼저 할 일이다.

1. **예산이 몇 개인가.** 이 앱은 최소 셋이다 — **API 한도**(시간당 15회) · **UI 스레드**
   (멈추면 앱이 죽은 것처럼 보인다) · **exe 크기**. 하나만 재고 "싸다"고 결론내지 않는다.
   *P19 에서 요청은 쟀는데 UI 스레드를 안 쟀고, 그게 115초였다.*
2. **무엇을 빼도 되나 — 최적화보다 먼저.** *"오늘 밤" 탭은 GEO 를 빼는 것만으로 대상이
   2,925→2,256건, 시간이 115→61초가 됐다.* **가장 비싼 계산이 가장 쓸모없는 결과를
   낼 수 있다.** 빠르게 만들기 전에 "이건 왜 계산하나"를 먼저 묻는다.
3. **보류·기각 근거에 숫자가 있나.** 없으면 그 보류는 **재검토 대상**이다
   (P12-17 은 숫자 없이 두 번 접혔고, 재어 보니 근거 자체가 틀렸다).
   숫자가 있어도 **그 숫자가 결정적인 축인지** 묻는다(P17-2 의 1.35MB 는 맞지만 무의미했다).
4. **테스트가 이걸 잴 수 있나.** 못 재는 구조면 설계를 바꾼다 — 코드가 아니라 **설계**를.
   *P17-1 에서 팝오버 위치를 `getBoundingClientRect()` 로 잡으려다, 스텁 DOM 에 없어
   CSS 가 잡게 바꿨다. 코드도 줄었다.*

**설계를 글로 낼 때 "재지 않은 것"을 한 줄로 밝힌다.** 예: *"UI 스레드 비용은 재지 않았다"*.
그 한 줄이 없으면 읽는 쪽은 전부 쟀다고 읽는다 — 실제로 오늘 그렇게 두 번 지나갈 뻔했다.

## 검증 (이 앱 고유분)

> 난이도별 작업 방식·TaskList·Git·문서 규칙은 전역 `~/.claude/CLAUDE.md`.

- **`build.bat` 은 ASCII 로 유지한다. BOM 도 넣지 않는다.** 파일 상단 주석에 이유가 적혀 있고,
  실제로 그래서 살아남았다 — cmd 는 BOM 을 명령으로 잘못 읽고, 한글 경로(`C:\Users\준\`)는
  코드페이지에 따라 깨진다. `activate.bat` 대신 `.venv\Scripts\python.exe` 를 직접 부르는 것도 같은 이유다.
  인코딩 일반 규칙은 전역 `~/.claude/CLAUDE.md`. (2026-08-15 전수 점검에서 이 프로젝트 파이썬·JS 는
  전부 `encoding="utf-8"` 명시로 위험 지점 0건이었다 — 그 상태를 유지한다.)
- **고쳤으면 실제로 돌려본다.** `python api_client.py` 가 기본 스모크. UI를 고쳤으면 `python main.py`로 실행 확인.
- **`api_parsing.py`·`api_errors.py` 는 `api_client.py` 가 모듈 경유로 부른다**(`api_parsing._parse_launches(...)`).
  이름 import 로 당겨오지 않는다 — 지금은 재대입이 없지만, 이 짝에서 재대입되는 값(`CACHE_DIR`·`_http_get`)이
  **테스트의 격리 수단**이라 같은 규칙을 지킨다(전역 8번).
- **`api_client.py` 의 캐시·TTL·아카이브·설정을 건드렸으면 `python tests/test_cache.py`** — HTTP 를
  주입해 **언제 부르고 실패하면 무엇을 돌려주는지**를 잰다(네트워크 불필요). TTL·폴백은 틀려도
  예외가 안 나고 **요청 수만 조용히 늘어난다** — 429 가 뜨고 나서야 안다.
- **파싱을 건드렸으면 `python tests/test_parsing.py`** — 실제 응답 픽스처로 정규화 결과를 골든과 비교한다
  (네트워크 불필요). 필드를 의도적으로 바꿨을 때만 픽스처 재수집 후 `--update` 로 골든을 갱신한다.
- **`web/js/` 를 건드렸으면 `node tests/test_frontend.js`** — 스텁 DOM 위에서 함수를 직접 호출한다
  (브라우저·네트워크·마우스 불필요). 새 로직을 넣었으면 **여기에 항목을 추가**한다. 대상은
  "깨져도 화면으로는 알아채기 어려운" 것 — 분류·필터·설정 복원 방어·집계. 스텁과 로더는 `tests/harness.js` 에만 둔다.
- **파일을 쪼갰으면 `index.html` 과 `tests/harness.js` 를 둘 다 고친다.** 하네스에만 넣고
  `index.html` 에 빠뜨리면 **테스트는 전부 통과하고 앱만 죽는다**(클래식 스크립트라 "함수가 없다").
  두 목록이 같은 파일을 같은 순서로 읽는지는 이제 `test_frontend.js` 가 잰다(P12-23).
- **`bindUI()` 에 배선을 더하면 `test_frontend.js` 의 `WIRING` 목록에도 넣는다.**
  2026-09-14 정기 점검에서 **배선 18개를 지워도 688건이 전부 통과**했다 — 검색창·패널 닫기·
  탭 전환·타임라인 슬라이더·새로고침·아카이브 불러오기가 통째로 죽어도 테스트는 초록이었다.
  그때까지는 당할 때마다 **개별 테스트를 하나씩** 더했고(2026-09-11 세 번 · 2026-09-12
  `tab-tonight`), 그래서 나머지는 계속 비어 있었다. 이제 전수로 잰다(27개).
  - **목록을 `boot.js` 에서 긁어 오지 않는다.** 긁어 오면 배선을 지울 때 기대 목록에서도
    같이 사라져 **검사가 공허하게 통과한다.** 테스트 파일의 손으로 적은 목록이 정답지다.
  - **붙었는지만 재면 절반이다 — `PRESSES` 로 실제로 눌러 예외가 안 나는지도 잰다.**
    2026-09-12 의 `tab-tonight` 은 **배선이 빠진 게 아니라** 핸들러가 `Set` 에 `.includes()`
    를 불러 죽은 것이었다. 붙었는지만 보는 테스트로는 그 버그가 안 잡힌다(변이로 확인).
  - **누르기 테스트는 "막힌 안내로 빠지지 않는 상태"에서 해야 한다.** 관측지·관심 위성·
    satrec 이 없으면 핸들러가 안내만 내고 끝나 **죽은 경로를 안 탄다** — 2026-09-12 에
    기존 테스트가 딱 그래서 못 잡았다.
- **순수 함수를 떼어냈으면 그 함수를 부르는 쪽도 같이 잰다.** 판정 로직이 전부 맞아도
  `addEventListener` 한 줄이 없으면 앱에서는 아무 일도 안 일어나는데, **순수 함수 테스트는
  전부 통과한다.** 2026-09-11 하루에 세 번 연속 같은 구멍이 났다 — `keydown`(P12-14) ·
  `loadArchive` 의 잘림 기록(P12-6) · 궤적 슬라이더 `input`(P12-16). **세 번 다 변이 실험이
  잡았고, 변이를 안 돌렸으면 셋 다 통과로 남았다.** 하네스가 `doc.has(ev)` 로 document
  핸들러를, 스텁 엘리먼트가 `fire(ev)` 로 실제 호출을 재게 해 뒀으니 그걸 쓴다.
- **테스트는 실행 PC 의 시간대·로캘에 기대지 않는다.** `tests/test_frontend.js` 는 맨 위에서
  `process.env.TZ = "Asia/Seoul"` 로 **고정**한다 — 시간대 테스트(P13-5)가 "현지 ≠ UTC"를 재는데
  **CI 러너는 UTC** 라 그 둘이 같아져 조용히 실패한다. 2026-09-12 에 실제로 당했고
  **로컬 530 통과 / CI 526 통과·4 실패가 다섯 커밋 동안 이어졌다**(아무도 안 봤다).
  같은 갈래로 **로캘 문자열도 비교하지 않는다** — Node 의 ICU 는 `AM 12:30`, 앱(WebView2)은
  `오전 12:30` 을 낸다. 단언은 **차이와 구조**로 한다.
- **화면에 새 오버레이를 붙였거나 위치·크기 CSS 를 건드렸으면 `node tests/test_layout.js`** —
  실제 `index.html`·`style.css`·`utils.js` 를 합쳐 **헤드리스 Edge**(WebView2 와 같은 Chromium)에
  띄우고, 조작해야 하는 요소마다 **중심점에서 `elementFromPoint`** 를 불러 *정말 그것이 잡히는지*
  를 잰다(창 900×600·1024×700·1280×800·1920×1080). 마우스는 안 쓴다 — 좌표 판정만 브라우저가 한다.
  **스텁 DOM 에는 좌표가 없어 이 부류는 1,209건이 전부 초록인 채로 살아 있었다**(2026-09-23):
  상세 패널이 타임라인 위쪽 34px 을 덮어 **과거 연도 선택과 `불러오기` 버튼이 창 크기와 무관하게
  클릭을 못 받았고**, 창을 1,085px 보다 좁히면 사이드바 탭 넷이 툴바 뒤로 들어가 **탭을 누르면
  `↻ 갱신`(강제 API 요청)이 눌렸다**(최소 창이 900px 이라 허용된 크기 안에서 일어난다).
  - **오버레이의 `top` 은 `var(--ui-top, 92px)` 로 쓴다.** 툴바 높이는 창 폭에 따라 변하고
    (자연 폭 1,065px), `utils.js` 의 `syncUiTop()` 이 그 실제 높이를 변수에 넣는다. 92px 을
    직접 적으면 **한 줄 툴바일 때만 맞는 값**이 된다.
  - **패널·사이드바의 `bottom` 은 타임라인(높이 46 + bottom 12)을 피한다.** 사이드바는 66px 로
    피해 있었고 패널만 24px 이었다 — 한쪽만 고친 자리는 이렇게 조용히 남는다.
  - **좌하단에 무엇을 더하면 `--stack-left` 를 쓰고 z-index 는 29 로.** 히트맵 범례·업데이트
    배지·오프라인 배지·첫 실행 카드가 그 스택이다(P35). 사이드바가 열리면 CSS 가 통째로
    오른쪽으로 민다 — **left 를 직접 적으면 사이드바 뒤로 숨는다.** 2026-09-23 에 셋이
    그랬고, `elementFromPoint` 가 셋 다 `#sidebar-list` 를 돌려줬다.
  - **같은 요소를 `clickable`(눌리는가)과 `visible`(겹치지 않는가) 둘로 잰다.** z-index 만
    올리면 **클릭은 전부 받는다** — 사이드바 *위에* 떴을 뿐 목록을 가린 것이다. 클릭만
    재는 동안에는 변이(배지를 원위치로)가 안 잡혔다.
  - ⚠ **이 테스트는 조용히 비어 있을 수 있다.** 재는 목록(`__CLICKABLE`·`__VISIBLE`)을
    페이지에 주입하는 치환이 하나 실패하면 **검사할 것이 0건이 되어 그냥 초록이다.**
    항목을 더했으면 **되돌리는 변이를 한 번 돌려** 빨개지는지 보고 끝낸다.
- **문서를 고쳤거나 파일을 늘렸으면 `node tests/test_docs.js`** — 계획서 제목이 본문과
  어긋나는지(끝난 Phase 가 *착수 전* 이라 말하는지) · 이 표의 파일 개수와 **로드 순서**가
  실제와 같은지 · README·CI 에 실행 명령이 빠졌는지 · 버전이 세 곳에서 같은지를 잰다.
  **사람이 기억해서 고치는 규칙은 또 어긋난다** — 2026-09-22 에 끝난 Phase **12개**의
  제목이 *착수 전* 인 채였고, 이 표는 23개 파일을 **22개**라고 적고 있었다.
- **새 테스트를 만들었으면 `git archive HEAD | tar -x -C <임시폴더>` 로 추적 파일만 뽑아
  거기서 한 번 돌려 본다.** 로컬에는 있고 **저장소에는 없는 파일**(`RL3D.spec` 같은 생성물,
  `%APPDATA%` 캐시)에 기대면 **로컬만 초록**이다 — 2026-09-22 에 문서 테스트가 그렇게
  올라가 CI 를 바로 빨갛게 만들었다.
- **메모리·CPU 를 잴 때는 `msedgewebview2` 자식 프로세스를 함께 센다.**
  `Get-Process -Name RL3D` 는 **파이썬 쪽만** 준다 — 화면(지도·위성·JS)은 WebView2 자식에 있다.
  2026-09-22 실측: 파이썬 **121MB** · WebView2 6프로세스 **466MB** → 실제 앱은 **587MB** 인데
  **79%를 안 세고 있었다.** 재는 법:
  ```powershell
  $rl = @(Get-Process -Name RL3D | % Id)
  $ws = @(Get-CimInstance Win32_Process -Filter "Name='msedgewebview2.exe'" | Select ProcessId,ParentProcessId)
  $d  = @($ws | ? { $rl -contains $_.ParentProcessId } | % ProcessId)   # 자식
  $k  = @($ws | ? { $d  -contains $_.ParentProcessId } | % ProcessId)   # 손자
  Get-Process -Id (@($d)+@($k)) | Measure-Object WorkingSet64 -Sum
  ```
  **PID 는 한 번만 잡고 그 뒤로는 `Get-Process -Id` 로 폴링한다** — `Get-CimInstance` 전체 조회를
  매번 돌리면 한 번에 2분씩 걸려 표본이 안 쌓인다(같은 날 실측).
  기준선(다음에 비교할 값): 위성 20개 **578~588MB** · 전 그룹 2,896개 **691~748MB**(톱니, 바닥 일정).
- **푸시한 뒤 CI 를 본다.** `gh run list --workflow=tests.yml --limit 5` 한 줄이면 된다 —
  로컬이 초록이어도 CI 는 다른 환경이다(위가 그 증거다).
- **시각·실제 데이터에 기대는 단언은 그 자리를 덮는지가 우연이다.** 2026-09-11 하루에 세 개를
  고쳤다(지상궤적선 · ISS 가시 통과 · 정지궤도 가시 구간). *오전엔 통과하고 저녁엔 실패했다.*
  **순수 함수는 고정 시각·합성 입력으로, 배선은 실시간 상대값으로** 잰다. 판정에 쓰이는 값
  (태양표 등)은 **주입해서** 시각과 무관하게 만든다.
- **조건 분기는 "그 분기로 실제로 들어가는" 상태를 만들어 재야 한다.** "오늘 밤" 탭은
  **관측지가 없는 경로만** 테스트가 덮고 있었는데, 그쪽은 막힌 안내를 내고 끝나 예외가 안 난다.
  관측지가 있는 경로에서는 `Set` 에 `.includes()` 를 불러 **도입 이래 줄곧 죽어 있었다**(2026-09-12 발견).
  화면에는 "탭이 안 바뀐다"로만 보인다 — 정기 점검에서 기능을 실제로 켜 보고서야 나왔다.
- **순수 함수를 여럿 붙여 문장을 조립했으면, 실제 캐시 데이터로 렌더해 눈으로 읽어 본다.**
  각 함수를 따로 재는 단언은 전부 통과하는데 **합쳐진 문장이 앞뒤가 안 맞을 수 있다** —
  2026-09-13 에 부스터 줄이 **"신조 · 2번째 비행"** 이라고 말했다(LL2 가 `reused=false`·`flights=1`
  로 주는 기체가 있다). `reused` 와 `flights` 를 따로 재는 테스트로는 영원히 안 잡힌다.
  수법은 마우스 없이 된다: `tests/harness.js` 의 `loadApp()` 으로 렌더 함수를 부르고
  `%APPDATA%\RL3D\cache\launches.json` 을 먹여 결과 HTML 을 텍스트로 덤프한다.
- **테스트가 다 초록이어도 화면을 한 번 띄워 본다.** P12-16 의 "2시간 뒤" 배지는 확대된 지도에서
  **화면 밖**이라 슬라이더를 올려도 아무 일도 안 일어난 것처럼 보였다 — 로직으로는 드러나지 않는
  종류의 결함이다. 확인은 캡처로(마우스 자동화 없이, 아래 규칙대로).
- **테스트는 코드·로직·프로세스 레벨로만.** GUI 검증도 마우스 포인터를 실제로 움직이는 자동화(클릭 좌표 이동 등)를
  쓰지 않는다. 창이 떴는지·오류가 없는지·데이터가 맞는지는 프로세스/로그/API 반환값으로 확인한다.
  **(이 항목은 전역으로 올리지 않았다 — 국방시스템공학은 반대 방침이라 프로젝트마다 다르다. 전역 10번 참조.)**
- **GUI 창은 2번(보조) 모니터에만 띄워 테스트한다.** 개발 실행은 `RL3D_DEV_MONITOR=2` 를 주고
  **`.venv\Scripts\pythonw.exe main.py`** 로 돌린다(`main.py` 의 `dev_window_pos`). 이 환경변수는 배포 exe 엔 영향 없다.
  - **`python.exe` 로 띄우지 않는다.** 앱 창은 2번으로 가지만 `python.exe` 가 함께 띄우는 **콘솔 창은
    주 모니터에 뜬다** — 환경변수가 안 듣는 창이라 규칙이 지켜지지 않은 것처럼 보인다(2026-09-12 지적받음).
    로그는 `%APPDATA%\RL3D\logs\` 에 남으므로(P12-9) 콘솔이 없어도 잃는 정보가 없다.
  **끝나면 앱을 끄고 다음 작업으로 간다** — 띄워 둔 채로 넘어가지 않는다.
  - **exe 스모크는 부모만 죽이면 창이 남는다.** onefile PyInstaller 는 부트로더(부모)가
    **자식 프로세스**를 띄우고 창은 자식이 갖는다. `Start-Process` 가 준 PID 를 `Stop-Process`
    해도 **창은 그대로 떠 있다** — 2026-09-17 에 v1.41.0 exe 창이 그렇게 남아, 다음 작업에서
    v1.42.0 창과 **둘이 동시에** 잡혔다(그제서야 알았다). `Get-Process -Name RL3D` 로 **전부**
    확인하고 끈다. 창 주인을 찾을 때 `$pid` 는 PowerShell 예약 변수라 쓰면 안 된다.
  - **번호는 `order_screens()` 로 정렬한 뒤 센다(주 모니터가 1번).** `Screen.AllScreens` 의 순서는
    사용자가 보는 "디스플레이 1/2"와 **무관하다** — 2026-09-11 실측에서 `AllScreens[0]` 이 보조,
    `[1]` 이 주였고, 그래서 `=2` 가 **주 모니터**를 가리켜 이 규칙이 조용히 안 지켜지고 있었다.
  - 띄운 뒤에는 창 좌표가 어느 모니터 `Bounds` 안에 드는지로 **확인**한다. 환경변수를 줬다는
    사실만으로는 증거가 아니다.

---

## 릴리스 — 버전을 올린 커밋에 태그를 **그 자리에서** 붙인다

**두 번 밀렸다.** P12-22(2026-09-12)에서 태그 14개(v1.3.0~v1.13.0)를 몰아 붙였는데,
**이틀 만에 10개가 다시 밀렸다**(2026-09-14 실측: 태그 `v1.22.1` · 코드 `v1.32.0`).
정리는 일회성이었고 원인은 그대로였다 — 버전을 올릴 때 태그를 안 붙이니 또 쌓인다.

- **`main.py` 의 `__version__` 을 올리는 커밋에는 annotated 태그를 같이 붙여 푸시한다.**
  형식은 `vX.Y.Z — 한 줄 요약 (Pnn-n)`.
- **밀렸는지는 `python api_client.py` 스모크가 말해 준다** — `[OK] update - 최신 릴리스 vX.Y.Z`
  가 코드 버전과 다르면 그만큼 밀린 것이다. 이 줄을 볼 때마다 확인한다.
- **내려받을 수 있는 릴리스는 최신 하나만.** 과거 버전 exe 를 지금 다시 빌드하는 것은
  그 시점의 재현이 아니다(P12-22 에서 정한 것, 그대로 유지).
- 앱의 업데이트 확인(P12-12)이 이 릴리스를 본다. **릴리스가 밀리면 앱이 사용자에게
  "최신입니다"라고 거짓말을 한다** — v1.22.1 릴리스가 v1.32.0 코드를 최신이라고 말하고 있었다.

## 나중에 (필요가 생기면 도입 — 지금은 과함)

혼자 쓰는 초기 단계라 규칙을 가볍게 뒀다. 아래는 그 필요가 실제로 생겼을 때 도입한다.

| 항목 | 도입 시점 |
|------|-----------|
| **버전 체계 + changelog** | 남에게 배포하거나 exe 를 공유할 때. 혼자 쓰는 동안은 git log 로 충분 |
| **로컬 타일 번들(완전 오프라인 지도)** | ~~인터넷 없이 지도 배경까지 필요해질 때~~ **라이선스로 막혔다**(P17-2, 2026-09-16). 퍼블릭 도메인 해안선 번들로 대체했다 — 다시 논의하려면 타일 출처부터 바꿔야 한다 |
| **발사 실시간 텔레메트리** | 무료 데이터 소스가 확인되면 (현재는 무료 API 부재 → 범위 밖). **P13-1(발사 순서표)이 부분 대체**다 |
