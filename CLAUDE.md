# RL3D — 프로젝트 규칙

> 로켓 발사 & 위성 추적 데스크톱 앱. 세부 설계는 `PLAN.md` 참조.
> 이 규칙은 옆 프로젝트(`국방시스템공학`, `피파전적관리`)의 공통 규칙에서 **이 앱에 맞는 것만** 추린 것이다.
> 지금은 혼자 쓰는 초기 단계 → **일부러 가볍게** 둔다. 무거운 절차(버전체계·회귀·감사)는 필요가 실제로 생기면 도입.

## 프로젝트 개요

pywebview + PyInstaller 로 만든 Windows exe. 내부는 웹 UI(HTML/JS + MapLibre GL 2D 지도).
전 세계 로켓 발사(Launch Library 2)를 지도에 표시하고, 위성(Celestrak TLE)을 실시간으로 움직이게 한다.

| 파일 | 역할 |
|------|------|
| `main.py` | pywebview 창 생성 + `Api` 브릿지 클래스(JS→파이썬 호출 창구) |
| `api_client.py` | 외부 API 호출·정규화·디스크 캐싱. **엔드포인트 URL 상수는 전부 이 파일 상단에** |
| `web/index.html` · `style.css` | 레이아웃과 테마. `<script>` 순서가 곧 JS 의존 관계다 |
| `web/js/*.js` | UI 로직 8개 파일: `state` → `utils` → `map` → `launches` → `sats` → `panels` → `settings` → `boot`. **ES 모듈이 아니라 클래식 스크립트** — 최상위 `let`·`function` 이 파일 간에 공유되므로 로드 순서를 지켜야 한다(`file://` 로 열려 모듈을 못 쓴다) |
| `web/lib/` | MapLibre GL JS, satellite.js (오프라인 번들) |
| `build.bat` | exe 빌드 (venv→설치→pyinstaller) |
| `tests/` | 파싱 회귀 테스트(`test_parsing.py`) + 실제 응답 픽스처·골든, 프론트 회귀 테스트(`test_frontend.js` + `harness.js`) |
| `PLAN.md` | 개발 계획·마일스톤 |

```powershell
python main.py     # 개발 실행
build.bat          # exe 빌드 → dist\RL3D.exe
```

---

## API 규칙 (가장 중요 — 외부 데이터 앱의 핵심)

1. **호출량을 아낀다 → 캐싱 우선.** Launch Library 2 는 **시간당 약 15회** 제한(초과 시 429).
   캐시가 유효하면 캐시를 쓰고, 만료됐을 때만 재요청한다. 저장 위치는 `%APPDATA%\RL3D\cache\`
   (exe onefile 은 실행폴더가 임시라 영구 위치 필수). TTL: 발사 15분 / TLE 2시간.
   발사는 프론트가 5분마다 자동 폴링하되 실제 API 호출은 캐시 TTL(15분)이 제어 → 시간당 약 8요청으로 15회 제한 내.

2. **엔드포인트 URL·상수는 `api_client.py` 상단 한 곳에만.** URL 문자열을 함수 안이나 여러 곳에
   흩뿌리지 않는다. API 경로가 바뀌면 **한 곳만 고치면 되게.**

3. **응답 필드는 항상 `.get()`으로 방어적으로 읽는다.** 외부 API가 필드를 빼거나 이름을 바꿔도
   앱 전체가 죽으면 안 된다. 발사 1건 파싱이 실패해도 나머지는 계속돼야 한다.

4. **API 실패는 사람이 읽는 말로 보여준다.** 429/오프라인/타임아웃을 사용자에게 코드 그대로
   띄우지 말 것. 실패 시 **오래된 캐시라도 반환**해 화면이 비지 않게 한다(오프라인 대응).

5. **새 데이터 소스를 붙이면 터미널에서 먼저 확인 가능하게.** `api_client.py`를
   `python api_client.py`로 직접 실행하면 소스·건수·에러를 `[OK]/[FAIL]`로 찍도록 유지한다
   (GUI 띄우기 전 스모크).

---

## pywebview / 프론트엔드 규칙

1. **JS↔파이썬은 `window.pywebview.api.*` 브릿지로만.** 파이썬이 네트워크·캐싱을 담당하고,
   JS는 받은 JSON을 지도에 그리는 역할. 무거운 네트워크를 JS에서 직접 하지 않는다.
2. **사용자·API 문자열은 DOM에 넣기 전 반드시 이스케이프**(`escapeHtml`). 팝업·패널 XSS 방지.
3. **지도·위성 라이브러리는 오프라인 번들**(`web/lib/`)로 exe 안에 포함.
   단 **지도 배경 타일과 발사/위성 데이터는 온라인 필요** — 이 전제를 README에 명시.
4. **리소스 경로는 `_MEIPASS` 경유**(`resource_path`). 상대경로는 exe에서 조용히 깨진다.

---

## 작업 방식

### 난이도별
| 난이도 | 방식 |
|--------|------|
| 낮음 (버그픽스·UI 조정·필드 추가) | 설계 없이 바로 구현 |
| 중간 이상 (새 화면·데이터 소스·구조 변경) | **설계 먼저 → 합의 후 구현** (수정 파일 목록·핵심 변경·의존성 명시) |

### 검증
- **`build.bat` 은 ASCII 로 유지한다. BOM 도 넣지 않는다.** 파일 상단 주석에 이유가 적혀 있고,
  실제로 그래서 살아남았다 — cmd 는 BOM 을 명령으로 잘못 읽고, 한글 경로(`C:\Users\준\`)는
  코드페이지에 따라 깨진다. `activate.bat` 대신 `.venv\Scripts\python.exe` 를 직접 부르는 것도 같은 이유다.
  인코딩 일반 규칙은 전역 `~/.claude/CLAUDE.md`. (2026-08-15 전수 점검에서 이 프로젝트 파이썬·JS 는
  전부 `encoding="utf-8"` 명시로 위험 지점 0건이었다 — 그 상태를 유지한다.)
- **고쳤으면 실제로 돌려본다.** `python api_client.py` 가 기본 스모크. UI를 고쳤으면 `python main.py`로 실행 확인.
- **파싱을 건드렸으면 `python tests/test_parsing.py`** — 실제 응답 픽스처로 정규화 결과를 골든과 비교한다
  (네트워크 불필요). 필드를 의도적으로 바꿨을 때만 픽스처 재수집 후 `--update` 로 골든을 갱신한다.
- **`web/js/` 를 건드렸으면 `node tests/test_frontend.js`** — 스텁 DOM 위에서 함수를 직접 호출한다
  (브라우저·네트워크·마우스 불필요). 새 로직을 넣었으면 **여기에 항목을 추가**한다. 대상은
  "깨져도 화면으로는 알아채기 어려운" 것 — 분류·필터·설정 복원 방어·집계. 스텁과 로더는 `tests/harness.js` 에만 둔다.
- 픽스처에 없는 새 파싱 경로는 **가짜 응답 dict로 먼저 확인**한다(네트워크 없이 즉시 검증).
- **테스트는 코드·로직·프로세스 레벨로만.** GUI 검증도 마우스 포인터를 실제로 움직이는 자동화(클릭 좌표 이동 등)를
  쓰지 않는다. 창이 떴는지·오류가 없는지·데이터가 맞는지는 프로세스/로그/API 반환값으로 확인한다.
- **GUI 창은 2번(보조) 모니터에만 띄워 테스트한다.** 개발 실행은 `RL3D_DEV_MONITOR=2 python main.py` 로 돌려
  창이 2번 모니터에 뜨게 한다(`main.py` 의 `dev_window_pos`). 이 환경변수는 배포 exe 엔 영향 없다.
- **진행 상황·시간을 추측해서 보고하지 않는다.** 실제 출력·시계로 확인한 것만 말한다.
  (두 옆 프로젝트가 공통으로 강조한 원칙 — 빌드/조회가 정체돼 보여도 추측 보고 금지.)

### Git (원격이 붙으면)
- **기능 단위로 완성 후 커밋.** 작업 중간 상태는 커밋하지 않는다.
- `.venv/`, `__pycache__/`, `dist/`, `build/`, 캐시는 커밋하지 않는다(`.gitignore`).

### 문서
- **README.md 를 최신으로 유지.** 실행 방법·데이터 소스·기능이 바뀌면 그 자리에서 갱신.
- 코드 주석은 **"왜 이렇게 했는지"가 비직관적일 때만.** 코드가 이미 말하는 걸 반복하지 않는다.

### 다단계 작업
- 3단계 이상 작업은 착수 시 TaskList로 등록하고 in_progress/completed 갱신.
- 단발·자명한 작업은 TaskList 없이 바로 구현.

---

## 나중에 (필요가 생기면 도입 — 지금은 과함)

혼자 쓰는 초기 단계라 규칙을 가볍게 뒀다. 아래는 그 필요가 실제로 생겼을 때 도입한다.

| 항목 | 도입 시점 |
|------|-----------|
| **버전 체계 + changelog** | 남에게 배포하거나 exe 를 공유할 때. 혼자 쓰는 동안은 git log 로 충분 |
| **로컬 타일 번들(완전 오프라인 지도)** | 인터넷 없이 지도 배경까지 필요해질 때 |
| **발사 실시간 텔레메트리** | 무료 데이터 소스가 확인되면 (현재는 무료 API 부재 → 범위 밖) |
