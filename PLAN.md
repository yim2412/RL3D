# RL3D 개발 계획서 — 로켓 발사 & 위성 추적 데스크톱 앱

> 작성일: 2026-07-24 · 갱신: 2026-09-18 · 상태: **Phase 0~22 완료 → v1.46.0 · Phase 23 후보 선정** (Phase 4는 데이터 소스 부재로 보류)
> 버전·변경 이력은 `CHANGELOG.md`, 버전 상수는 `main.py`의 `__version__`.

참고 사이트: `sbhnews.com` 스타일 — **전체화면 2D 인터랙티브 지도 + 상단 속보 티커 + 사이드 패널** 느낌.
개발 규칙: `CLAUDE.md` (옆 프로젝트 `국방시스템공학`·`피파전적관리`의 공통 규칙에서 이 앱에 맞는 것만 추림).

---

## 1. 프로젝트 개요

전 세계 로켓/위성 발사를 **2D 지도 위에서 추적**하고, 각 발사의 상세 정보(로켓, 기관, 미션, 궤도, 결과)를
볼 수 있으며, 궤도를 도는 **위성의 실시간 위치**까지 지도 위에서 움직이게 보여주는 Windows 데스크톱 앱.

- **형태**: pywebview + PyInstaller 로 패키징한 `.exe` (설치 불필요, 더블클릭 실행)
- **화면**: 웹 기술(HTML/CSS/JS) + MapLibre GL JS 로 구성 (2D 슬리피 맵)
- **데이터**: 인터넷에서 실시간으로 가져와 로컬 캐싱

### 이전 RL3D(3D 지구본)와의 차이
| 항목 | 이전 (삭제됨) | 이번 |
|---|---|---|
| 지도 | 3D 지구본 (globe.gl) | **2D 지도 (MapLibre GL)** |
| 형태 | 데스크톱 exe | 데스크톱 exe (동일) |
| 데이터 | 발사 일정만 | **발사 + 위성 실시간 궤도** |
| 느낌 | 우주에서 본 지구본 | 뉴스 사이트형 실시간 상황판 |

---

## 2. 기술 스택 & 선택 이유

| 영역 | 선택 | 이유 |
|---|---|---|
| 창/패키징 | **pywebview + PyInstaller** | 웹 UI를 네이티브 창으로. exe 단일 파일 배포. 이전 프로젝트 검증됨 |
| 파이썬↔JS 브릿지 | pywebview `js_api` | 파이썬이 API 호출·캐싱, JS에 결과 전달 (요청 제한 방어) |
| 지도 | **MapLibre GL JS** (오픈소스) | 무료·키 불필요. 참고 사이트와 동일 계열. `#map=z/lat/lng` 해시 지원 |
| 지도 배경 타일 | **Esri Dark Gray Canvas** (무료 raster) | 어두운 "상황판" 느낌. API 키 불필요. (온라인 필요) **CARTO dark 에서 2026-09-11 교체** — 아래 11절 참조 |
| 위성 궤도 계산 | **satellite.js** (SGP4) | 브라우저에서 TLE→위경도 계산. 초당 갱신으로 움직임 표현 |
| 발사 데이터 | Launch Library 2 API | 무료. 발사장 좌표·일정·결과 제공 |
| 위성 데이터 | Celestrak TLE | 무료. 궤도 요소(TLE) 제공 |
| HTTP 호출 | 파이썬 표준 `urllib` | 추가 의존성 없음 |

> **오프라인 번들**: MapLibre GL JS 와 satellite.js 는 `web/lib/` 에 파일로 포함해 exe 안에 넣는다.
> 단, **지도 배경 타일과 발사/위성 데이터는 인터넷 연결이 필요**하다 (이 부분은 온라인 전용).

---

## 3. 아키텍처

```
┌─────────────────────────── RL3D.exe (pywebview 창) ───────────────────────────┐
│                                                                                │
│   [ 파이썬 백엔드 ]                         [ 웹 프론트엔드 (WebView2) ]         │
│                                                                                │
│   main.py                                   index.html / style.css             │
│    └ pywebview 창 생성                       js/*.js (10개 파일)                 │
│    └ Api 클래스 (js_api)  ◀── 브릿지 ──▶      ├ MapLibre 지도 렌더링             │
│         ├ get_launches()                     ├ 발사 마커 + 팝업/패널            │
│         └ get_satellites()                   ├ 위성 실시간 궤도 (satellite.js)  │
│                                              ├ 상단 속보 티커                    │
│   api_client.py                              ├ 필터 / 검색 / 타임라인            │
│    ├ Launch Library 2 호출 + 캐싱            └ (배경 타일: 온라인)              │
│    └ Celestrak TLE 호출 + 캐싱                                                  │
│                                                                                │
│   캐시 저장 위치: %APPDATA%\RL3D\cache\                                          │
└────────────────────────────────────────────────────────────────────────────────┘
```

흐름: 앱 시작 → JS가 `pywebview.api.get_launches()` / `get_satellites()` 호출 →
파이썬이 캐시 확인 후 (만료 시) API 호출 → 정규화한 JSON을 JS로 반환 → 지도에 렌더링.

---

## 4. 데이터 소스 상세

### 4-1. 발사 데이터 — Launch Library 2 (thespacedevs)
- 운영 서버: `https://ll.thespacedevs.com/2.2.0`
- 엔드포인트:
  - 예정: `/launch/upcoming/?limit=50&ordering=net`
  - 과거: `/launch/previous/?limit=50&ordering=-net`
- 페이지네이션(`next`) 따라가며 수집, 요청 사이 딜레이로 레이트리밋 보호
- **요청 제한: 시간당 약 15회** → 반드시 캐싱 (아래 6절)
- 뽑아 쓸 필드: 발사장 위경도, 발사명, net(발사시각), 상태/결과, 로켓, 기관, 미션, 궤도, 이미지

### 4-2. 위성 데이터 — Celestrak
- TLE 그룹 예: `https://celestrak.org/NORAD/elements/gp.php?GROUP=<그룹>&FORMAT=tle`
- v1에서 쓸 그룹(성능 고려해 수백 개 규모로 제한):
  - `stations` — ISS, 중국 우주정거장 등 (눈에 잘 띔)
  - `visual` — 지상에서 밝게 보이는 위성 (~150개)
  - (옵션) `starlink` 일부 — 개수 많아 캡 필요
- TLE 는 파이썬이 받아서 그대로 전달 → 브라우저 satellite.js가 현재 시각 기준 위경도/고도 계산

### 4-3. (어려움) 발사 순간 실시간 텔레메트리
- 발사 중 로켓의 실시간 고도/속도/궤적은 **무료 공개 API가 사실상 없음** (SpaceX 정도만 제한적/비공식)
- v1 범위에서 제외. 대신 "다음 발사까지 카운트다운" + 발사 후 위성 궤도 추적으로 대체.
- 향후 실험 과제로만 남김 (아래 12절 Phase 4).

---

## 5. 폴더 / 파일 구조

```
RL3D/
├── main.py             # pywebview 창 + Api 브릿지 + 로그·시작 진단 배선(진입점)
├── api_client.py       # LL2 발사 + Celestrak TLE 호출·정규화·캐싱
├── applog.py           # 로그 파일 설정(P12-9). 아무것도 import 하지 않는다
├── satcat_codes.py     # SATCAT 코드→사람 말 표(P12-5). 아무것도 import 하지 않는다
├── startup.py          # WebView2 부재 안내(P12-11) · 시작 실패 안내(P12-10)
├── requirements.txt    # pywebview, pyinstaller, pythonnet(win)
├── build.bat           # exe 빌드 (venv→설치→pyinstaller)
├── PLAN.md             # 이 문서
├── README.md           # 사용법/빌드법
├── CHANGELOG.md        # 버전별 변경 이력
├── tests/              # 파싱 회귀(test_parsing.py) + 픽스처/골든, 프론트 회귀(test_frontend.js)
├── docs/               # GitHub Pages(index.html) + 개발 노트(notes/)
└── web/
    ├── index.html      # 레이아웃 (지도, 티커, 패널, 필터, 타임라인)
    ├── style.css       # 다크 상황판 테마
    ├── js/             # 지도·마커·티커·위성·필터 로직 (P11-5에서 8개 파일로 분리, +focus.js)
    │   ├── state.js · utils.js · map.js · launches.js · focus.js
    │   └── sats.js · favorites.js · sidebar.js · panels.js · satpanel.js · stats.js
    │       · keys.js · settings.js · update.js · boot.js
    └── lib/
        ├── maplibre-gl.js      # 오프라인 번들
        ├── maplibre-gl.css
        └── satellite.min.js    # SGP4 궤도 계산
```

---

## 6. 캐싱 전략

- 저장 위치: `%APPDATA%\RL3D\cache\` (PyInstaller onefile은 임시폴더에 풀리므로 영구 위치 필수)
- 파일:
  - `launches.json` — TTL 15분 (프론트 5분 자동 폴링 + 캐시로 실제 API는 15분마다)
  - `tle.json` — TTL 2시간 (TLE는 하루 1~2회 갱신이면 충분)
- 로직: 캐시 유효 → 캐시 반환 / 만료 → API 호출 후 갱신 / API 실패 → **오래된 캐시라도 반환**(오프라인 대응)
- "새로고침" 버튼 = 캐시 무시 강제 갱신 (요청 제한 주의 문구 표시)

---

## 7. UI / UX 레이아웃

```
┌──────────────────────────────────────────────────────────────────────┐
│ [🔴속보 티커: "T-02:14:33 · Falcon 9 Starlink … 발사 예정"  흐르는 텍스트]│  ← 상단 바
├──────────────────────────────────────────────────────────────────────┤
│  🔍[검색창]                                            [필터]  [새로고침] │
│                                                                        │
│                        🗺  MapLibre 2D 지도                            │
│                    (발사장 마커 · 위성 점 실시간 이동)                    │
│                                                        ┌─────────────┐ │
│                                                        │ 상세 패널     │ │
│                                                        │ (마커 클릭 시)│ │
│                                                        └─────────────┘ │
│                                                                        │
├──────────────────────────────────────────────────────────────────────┤
│ [◀ 타임라인 슬라이더 ─────────●──────── ▶]   "2026년 7월 24일 까지"      │  ← 하단
└──────────────────────────────────────────────────────────────────────┘
```

- **마커 색상**: 🟢성공 / 🔴실패 / 🟠부분실패 / 🔵예정
- **위성**: 작은 밝은 점 + (선택) 궤적선. 초당 위치 갱신으로 부드럽게 이동
- **상세 패널**: 마커 클릭 시 로켓/기관/미션/궤도/발사시각/이미지 표시
- **필터**: 결과별 on/off, 발사/위성 레이어 on/off
- **타임라인**: 특정 날짜까지의 발사만 표시
- **속보 티커**: 가장 임박한 예정 발사 카운트다운 순환 표시

---

## 8. 데이터 모델 (정규화)

### 발사(launch)
```
{ id, name, net(ISO), status, outcome(success|failure|partial|upcoming),
  rocket, provider, provider_country, pad_name, location_name,
  lat, lng, mission_name, mission_type, orbit, image }
```
### 위성(satellite)
```
{ name, norad_id, tle1, tle2 }   // 위경도는 브라우저에서 satellite.js로 실시간 계산
```

---

## 9. 개발 단계 (마일스톤)

- **Phase 0 — 뼈대** ✅: 폴더/파일 생성, pywebview 창에 빈 MapLibre 지도 띄우기, 브릿지 연결 확인
- **Phase 1 — 발사 지도(핵심)** ✅: LL2 연동·캐싱 → 발사장 마커 + 색상 + 상세 패널 + 검색/필터/타임라인
- **Phase 2 — 실시간 위성** ✅: Celestrak TLE 연동 → satellite.js 로 위성 점 실시간 이동 + 레이어 토글
- **Phase 3 — 상황판 감성** ✅: 속보 티커(카운트다운), 다크 테마 다듬기, 마커 호버/클러스터링
- **Phase 4 — (실험) 라이브 요소**: 발사 궤적 애니메이션 — **보류**(무료 텔레메트리 소스 부재, 4-3절)
- **Phase 5 — 패키징** ✅: build.bat 로 exe 빌드·검증, README 정리

> Phase 6 이후는 13절 로드맵 참조.
> 현재 위치: **Phase 11 종료 → v1.5.1** (P11-1·P11-2·P11-4=v1.3.0, P11-3=v1.4.0, P11-7=v1.5.0,
> P11-8·P11-5는 내부 작업, P11-6은 측정 결과 불필요로 종결하고 캡 상향으로 대체=v1.5.1).
> 이후 v1.5.2(정기 점검 버그 3건)를 거쳐 로드맵이 다시 소진되어 **Phase 12 후보 21건을 13절에 뽑았고,
그중 ①~⑤(P12-9/10/11 · P12-13 · P12-21 · P12-1/2 · P12-5 · P12-5b)를 v1.9.0 까지 마쳤다**.

각 Phase는 독립적으로 "돌아가는 상태"를 유지 → 중간에 언제든 실행해 확인 가능.

---

## 10. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| LL2 시간당 15회 제한 | **15분** 캐싱(TTL_LAUNCHES) + 오프라인 시 stale 캐시 반환 + 강제새로고침 경고 |
| 발사 실시간 텔레메트리 무료 API 부재 | v1 범위 제외, 카운트다운+위성 궤도로 대체 |
| 위성 수천 개 렌더 시 성능 저하 | 그룹 선별 + 개수 캡. **실측(P11-6)으로 렌더는 병목이 아니었다** — 실제 제약은 Celestrak 의 403 |
| 오프라인에서 지도 배경 안 뜸 | "배경 타일은 온라인 필요" 명시. (향후 로컬 타일 번들은 별도 과제) |
| exe 실행 시 흰 화면 | WebView2 런타임 안내 **✅ v1.6.0 에서 구현**(`startup.py`). 그 전까지는 이 표에만 있고 코드엔 없었다 |

---

## 11. 다음에 결정할 것 (미정 사항)

1. ~~위성 표시 범위~~ → v1은 `stations`+`visual` 고정. **P7-6에서 그룹 선택 UI로 확장 예정**
2. ~~지도 배경 스타일~~ → CARTO dark 확정 → **2026-09-11 Esri Dark Gray Canvas 로 교체**(CARTO 가 API 키를 요구하기 시작)
3. ~~속보 티커에 넣을 항목~~ → **최근 결과도 섞기로 결정·구현 완료**(예정 2건마다 결과 1건, 결과는 배지+"N시간 전")
4. 앱 이름/브랜딩: RL3D 유지 vs 새 이름 → **RL3D 유지**(혼자 쓰는 동안 개명 실익 없음)
5. 언어: 한국어 UI 고정 vs 한/영 토글 → **한국어 고정 유지**(도입 안 함)

---

## 12. 요약 (한 줄)
**pywebview exe 안에서 MapLibre 2D 지도로 전 세계 로켓 발사(LL2)를 마커·상세·티커로 보여주고,
Celestrak TLE + satellite.js 로 위성을 실시간으로 움직이게 하는, 뉴스 상황판 감성의 데스크톱 추적 앱.**

---

## 13. 로드맵 — Phase 6 이후 (2026-07-24 추가)

v1(Phase 0~5) 완료 후 정리한 개선·추가 기능 목록. **위에서부터 구현 순서**.
각 항목은 독립적으로 "돌아가는 상태"를 유지한다(9절 원칙 동일).

### Phase 6 — 지도 표현력 (체감 변화 큼 · 각 항목 단발 작업)

| # | 기능 | 난이도 | 핵심 내용 |
|---|------|--------|-----------|
| **P6-1** ✅ | **위성 지상궤적선 + 추적 모드** | 낮음 | 선택 위성의 앞뒤 1주기(≈90분)를 1분 간격 `satellite.propagate`로 계산해 LineString 레이어로. **날짜변경선(±180°) 통과 시 선을 끊어야** 지도를 가로지르는 가짜 선이 안 생긴다. 추적 모드는 지도 중심을 해당 위성에 고정 |
| **P6-3** ✅ | **낮/밤 터미네이터 오버레이** | 낮음 | 태양 직하점(subsolar point) 계산 → 야간 반구를 반투명 검정 폴리곤으로. 외부 데이터 불필요, 분 단위 갱신. 상황판 감성에 직결 |
| **P6-4** ✅ | **발사 목록 사이드바** | 낮음 | 현재는 마커를 찾아 클릭해야만 상세를 본다. 필터·검색 결과를 좌측 리스트로 표시하고 클릭 시 `flyTo` + 패널 오픈 → 검색 기능이 비로소 쓸모 있어짐 |
| **P6-2** ✅ | **관측 위치 기준 통과 예측(pass prediction)** | 중간 | "ISS 21:43 북서→남동, 최대고도 62°". satellite.js `ecfToLookAngles`로 계산(외부 API 불필요). 관측 위치는 지도 클릭으로 지정 후 설정에 저장. **앱을 실제로 쓸 이유를 만드는 기능** |

> 구현 순서는 P6-1 → P6-3 → P6-4 → P6-2.
> P6-2는 중간 이상 난이도 → CLAUDE.md 규칙대로 **설계 먼저 합의 후 구현**.

### Phase 7 — 데이터 커버리지 (지금 있는 구멍)

| # | 기능 | 난이도 | 핵심 내용 |
|---|------|--------|-----------|
| **P7-5** ✅ | **과거 발사 아카이브** | 중간 | 현재 `previous` 50건뿐이라 과거 커버리지가 최근 수 주치 → **타임라인 슬라이더가 사실상 놀고 있다.** 다만 LL2 시간당 15회 제한이 있으므로 매번 페이지네이션은 불가. → **연도별로 한 번 받아 만료 없는 영구 캐시**(`archive_<year>.json`)에 저장하고, 최근 구간만 TTL 갱신. **구현: 타임라인 옆 연도 선택(최근 5년) → 명시적 "불러오기", 지난 연도 영구·올해 6h TTL, 페이지당 2초 딜레이·최대 5페이지** |
| **P7-6** ✅ | **위성 그룹 선택 UI** | 낮음 | 그룹 카탈로그(stations/visual/starlink/gps-ops/galileo/weather/science/geo)+그룹별 캐시. **개수 캡**(starlink 300·geo 200). 툴바 "그룹 ▾" 체크리스트, 변경 시 재로드 |
| **P7-7** ✅ | **마지막 갱신 시각 상시 표시** | 낮음 | `get_launches`가 캐시 age 반환 → 툴바에 "N분 전 갱신" 상시 표시(30초 갱신) |

### Phase 8 — 데스크톱 앱다움

| # | 기능 | 난이도 | 핵심 내용 |
|---|------|--------|-----------|
| **P8-9** ✅ | **창 상태·필터 설정 저장** | 낮음 | `%APPDATA%\RL3D\settings.json`. 필터·낮/밤·위성 토글·그룹·관측 위치(프론트) + 창 위치·크기(파이썬 closing 이벤트). dev 모니터 위치는 저장 안 함 |
| **P8-8** ❌ | **발사 T-10분 Windows 토스트 알림** | 중간 | 구현했다가 **2026-07-25 제거**. 실사용에서 알림이 필요 없다고 판단 → 코드·툴바 🔔 토글·`winotify` 의존성 모두 제거(`Api.notify` 브릿지 포함). 위성 통과 알림도 같은 이유로 도입하지 않기로 결정. 알림이 다시 필요해지면 `git log`에서 P8-8 커밋을 참고 |
| **P8-10** ✅ | **발사 통계 패널** | 낮음 | 총 발사·성공률·예정 타일 + 결과별/연도별/기관/국가 막대. `allLaunches`(라이브+아카이브) 기준. 외부 차트 라이브러리 없이 CSS 막대 |

### Phase 9 — 공개 준비

원격: `github.com/yim2412/RL3D`.

| # | 항목 | 내용 |
|---|------|------|
| **P9-1** ✅ | **README 스크린샷** | `docs/screenshot.png` — 발사 마커(클러스터) + 위성 177개 + 낮/밤 오버레이 + 티커·타임라인이 한 화면에. **캡처 방법**: 화면 좌표 캡처(`CopyFromScreen`)는 다른 창이 위에 있으면 그 창이 찍히므로 쓰지 않는다 → `PrintWindow(PW_RENDERFULLCONTENT)`로 창 자체를 렌더링. 위성/필터를 켠 상태로 찍으려면 `settings.json`을 미리 바꾸는데, **BOM 없는 UTF-8로 써야** 파이썬이 읽는다(`Out-File -Encoding utf8`은 BOM이 붙어 설정이 통째로 무시됨) |
| **P9-2** ✅ | **라이선스 파일** | MIT (`LICENSE`). README 라이선스 절에 서드파티(MapLibre BSD-3, satellite.js MIT)·데이터 출처 조건 명시 |

### Phase 10 — 상세·탐색 확장 (v1.1.0 · v1.2.0에서 구현 완료)

로드맵에 미리 적어두지 않고 v1.0.0 이후 그때그때 정해 넣은 것들. 기록을 위해 사후 정리한다.
공통 성격: **이미 받고 있던 데이터를 화면에 푸는 작업이라 API 요청 수는 늘지 않았다.**

| # | 기능 | 버전 | 핵심 내용 |
|---|------|------|-----------|
| **P10-1** ✅ | **발사 상세 확장** | 1.1.0 | 중계 링크(유튜브·X, `Api.open_url`로 기본 브라우저), 발사 소식 타임라인(최신 6건), 미션 패치, 프로그램 태그, 발사 윈도우, `net_precision` 기반 시각 확정도 경고, 기록 맥락("이 발사대 285번째"), 발사 확률/기상 우려 |
| **P10-2** ✅ | **위성 검색·목록** | 1.2.0 | 사이드바에 `🚀 발사 / 🛰 위성` 탭. 이름·NORAD 검색 → 클릭 시 선택·추적·`flyTo`. 수백 개 중 ISS를 찾는 게 목적 |
| **P10-3** ✅ | **발사장 관점 화면** | 1.2.0 | 상세 패널의 발사장 이름 클릭 → 그 발사장만의 통계(총 발사·성공률·결과별/로켓/기관/패드 막대 + 예정·최근 목록). 패드 이름 축약(`Space Launch Complex 4E`→`SLC 4E`) |
| **P10-4** ✅ | **배경 지도 전환** | 1.2.0 | 다크 ↔ 위성사진(v1.7.1부터 둘 다 Esri). 두 레이어를 함께 두고 표시만 토글 → 마커·궤적·터미네이터가 재생성되지 않는다. 설정에 저장 |
| **P10-5** ✅ | **위성 고도별 색상** | 1.2.0 | 저궤도(하늘색)→중궤도(보라)→GPS 대역(분홍)→정지궤도(주황) |

### Phase 11 — 후보 (2026-07-26 정리 · **아직 착수 전, 순서 미정**)

Phase 6~10으로 원래 로드맵이 소진되어 새로 뽑은 목록. 위쪽일수록 "지금 있는 구멍"에 가깝다.
**착수 전에 이 중 무엇을 할지 먼저 합의한다** — 전부 하기로 정해진 게 아니다.

| # | 후보 | 난이도 | 왜 / 무엇 |
|---|------|--------|-----------|
| **P11-1** ✅ | **기관·로켓 관점 화면** | 낮음 | P10-3(발사장 화면)을 **발사장/기관/로켓 공용으로 일반화**(`showEntityStats(kind, value)` + `ENTITY_VIEWS`). 뼈대(타일·결과별·예정/최근 목록)는 공유하고 중간 막대만 관점별로 바꾼다. 패드가 1개뿐인 발사장은 "패드별" 막대 생략 |
| **P11-2** ✅ | **관심 발사·위성 즐겨찾기** | 낮음 | `settings.json`의 `favorites{launches,sats}` + 사이드바 ⭐ 탭 + 지도 금색 강조(발사는 굵은 테두리, 위성은 큰 점). **id는 항상 문자열로** 보관 — LL2 id는 문자열, NORAD는 숫자로 와 섞인다 |
| **P11-3** ✅ | **위성 필터(궤도 대역)** | 낮음 | `그룹 ▾` 팝오버 안에 LEO/MEO/GEO 체크박스. **대역은 실시간 고도(P10-5의 색)가 아니라 평균운동 → 반장축에서 얻은 평균 고도로 나눈다** — 전파 없이 즉시 구하고 타원 궤도도 한 대역에 머문다(경계 2,000km·35,000km). `visibleSats()` 한 곳에서 걸러 지도·목록이 같은 기준을 쓰고, **관심 위성은 필터 예외**. 세 대역이 모두 꺼진 저장 설정은 무시. 검색어는 사이드바 전용으로 남겼다 — 지도까지 걸면 검색창을 닫은 뒤 왜 위성이 없는지 알 수 없다 |
| **P11-4** ✅ | **지도 위치 복원** | 낮음 | `moveend` → 1초 디바운스로 `camera{lng,lat,zoom}` 저장, 다음 실행에 그 위치로 연다. 추적 모드 중 위치는 저장하지 않는다(매 초 지도를 옮긴다). 딥링크(`#map=`)는 데스크톱 앱이라 실익이 없어 도입하지 않음 |
| **P11-5** ✅ | **`web/app.js` 모듈 분리** | 중간 | 1,737줄 단일 파일 → `web/js/` 8개 파일(state·utils·map·launches·sats·panels·settings·boot). **ES 모듈은 쓰지 않았다** — `main.py`가 `url=`에 파일 경로를 주고 `http_server` 없이 `webview.start()` 해서 페이지가 `file://`로 열리기 때문(로드맵에 적어둔 "확인 필요"의 답). **클래식 스크립트는 최상위 `let`·`function`을 파일 간에 공유**하므로 로직 변경 없이 잘라 붙이기만 했고, `index.html`의 `<script>` 순서가 곧 의존 관계다. **방법**: 라인 범위 매핑 스크립트로 잘라 "모든 줄이 정확히 한 번씩" 배치됐음을 검증 → `node --check` 8개 + 이어붙인 결과 → 선언 개수 대조(함수 107·최상위 바인딩 50, 원본과 동일) |
| **P11-6** ❌ | **위성 렌더 성능** | 중간 | **측정해보니 최적화가 불필요**해서 도입하지 않았다(2026-07-26). 전제부터 틀렸다 — 갱신은 매 프레임이 아니라 초당 1회고, 실제 앱(WebView2)에서 **8,000개도 동기 28ms · 화면 반영 33ms**(177개 0.7ms / 2,000개 7.4ms). satrec 을 깊은 복사해 위성마다 별개 객체로 둔 뒤에도 같은 수치라 SGP4 캐시 효과도 아니었다. Node 단독 벤치는 5,000개 86ms 로 더 느리게 나오는데, 판단 근거는 실제 앱 수치를 썼다. **대신 캡을 올렸다**(GEO 무제한·Starlink 2,000 → v1.5.1). 캡의 실제 제약은 렌더가 아니라 Celestrak 403(Starlink 전체 10,776개·1.7MB 반복 요청). 다시 성능이 문제가 되면 화면 밖 스킵·갱신 주기 분리가 후보다 |
| **P11-7** ✅ | **오프라인 화면 다듬기** | 낮음 | 지도 좌하단 안내 배지. **타일 연속 3회 실패**에서만 띄운다(한두 개 실패는 정상적으로 흔하다) — MapLibre `error` 이벤트의 `sourceId`가 carto/esri 인 것만 집계하고, `data` 이벤트에서 `tile.state === "loaded"` 를 보면 카운터를 되돌린다. `online`/`offline` 이벤트에도 반응. 클릭하면 그 세션 동안 닫힘. **검증**: 타일 URL을 도달 불가 호스트로 잠시 바꿔 실제 렌더를 캡처로 확인(네트워크 어댑터를 건드리지 않는다) |
| **P11-8** ✅ | **프론트엔드 회귀 테스트** | 중간 | `tests/harness.js`(스텁 DOM·map·pywebview + `vm` 로더) + `tests/test_frontend.js`(**102항목**). `node tests/test_frontend.js`, 네트워크·브라우저·마우스 불필요. **`app.js`의 모듈 스코프 `let`은 컨텍스트 객체에 안 보인다** → 같은 스코프에 접근자를 이어붙여 상태를 주입/조회한다(`STATE_KEYS`). **로더는 harness.js 한 곳에만** 둬서 P11-5로 ES 모듈이 되면 그 파일만 교체하면 되게 했다. 도입 시 뮤테이션으로 실효성 확인(대역 경계 2000→2500, 타일 임계 3→2 각각 정확히 1건 실패). **2차 확장(2026-07-26)**: 계산 로직까지 — `computePasses`(통과 예측)·`computeGroundTrack`(날짜변경선)·`applyFilters`·`recomputeTimeline`·`renderSidebar`·`announceChanges`. 실제 `satellite.min.js`를 쓰며, **반드시 앱과 같은 vm 컨텍스트에 올려야 한다** — 별도 컨텍스트면 라이브러리 내부 `date instanceof Date` 가 realm 불일치로 false 가 되고 좌표가 전부 NaN 이 된다(브라우저에선 없는 문제, 처음엔 `every()` 가 공허하게 통과해 속기 쉽다). 통과 예측은 외부 정답이 없으니 **물리 불변식**으로 검증: 경사 51.6° 위성은 남극(-82°)에서 통과 0건, 통과 간격은 궤도 주기의 정수배 근처, 하루 2~12회, `minEl` 상향 시 단조 감소. 뮤테이션 5종(날짜변경선 끊기 제거·고도↔방위 혼동·통과 병합 실패·타임라인 부등호 반전·정렬 반전) 모두 검출 |

> 착수 순서: **P11-1 ✅ → P11-2 ✅ → P11-4 ✅** (v1.3.0) → **P11-3 ✅** (v1.4.0) → **P11-7 ✅** (v1.5.0)
> → **P11-8 ✅ → P11-5 ✅** (둘 다 앱 동작이 바뀌지 않아 버전은 그대로) → **P11-6 ❌ 불필요로 종결**(대신 캡 상향 = v1.5.1).
> **Phase 11 종료.** P11-5 때 하네스는 로더 한 곳(`APP_FILES` 이어붙이기)만 고쳤고 테스트 62항목은 그대로 통과했다.
>
> **여기서 배운 것**: 로드맵에 "성능/구조가 문제일 것"이라고 적어둔 항목은 **착수 전에 측정부터** 한다.
> P11-6은 측정 30분으로 구현 자체가 불필요해졌고, 그 측정이 대신 캡 상향이라는 실제 개선을 찾아냈다.

> **P11 검증 방법 메모** — 마우스 자동화 없이 확인한 방법(다음에도 그대로 쓸 것):
> ① `settings.json`을 미리 써두고 재실행 → `PrintWindow` 캡처로 **복원 결과**를 눈으로 확인(카메라 위치·관심 마커 강조).
> ② 클릭이 필요한 로직은 `web/js/*.js`를 Node `vm`에 스텁 DOM으로 올려 함수를 직접 호출(P11-8에서 `tests/`에 상설화).
> 초기 로드만으로는 `moveend`가 발생하지 않아 카메라가 저장되지 않는다 — 정상 동작이며, 저장 경로는 ②로 검증했다.

### Phase 11.5 — 로드맵 밖 작업 (v1.5.2 이후 · 2026-09-11 사후 정리)

Phase 11 종료와 Phase 12 착수 사이에, **로드맵에 미리 적지 않고 그때그때 정해 넣은 것들**.
Phase 10 과 같은 이유로 사후 정리한다 — 적어두지 않으면 계획서가 실제와 어긋난 채로 남고,
다음 세션이 "이건 왜 있지"를 다시 조사하게 된다.

공통 성격: **앱 코드는 거의 건드리지 않았다.** 버그 3건을 뺀 나머지는 저장소 바깥에서
이 프로젝트가 어떻게 보이는지에 대한 작업이다.

| # | 작업 | 버전 | 핵심 내용 |
|---|------|------|-----------|
| **P11.5-1** ✅ | **정기 점검 버그 3건** | 1.5.2 | 타임아웃을 "인터넷 연결 확인"으로 잘못 안내하던 것(`URLError(reason=timeout)` 래핑), 403 등 상태코드별 문구 표(`HTTP_ERROR_MESSAGES`), 창을 화면 밖으로 복원하던 것, 옛 `tle.json` 캐시 정리 |
| **P11.5-2** ✅ | **개발 노트 2편** | — | `docs/notes/` 에 후보 34건 중 둘. ① *고장이 아닌데 고장으로 보인다* — 같은 갈래가 이 저장소에서 **일곱 번** 반복됐고 고친 방식이 매번 달랐다. ② *조용히 통과하던 검사* — 초록인데 아무것도 검증하지 않던 셋. 고르고 남은 후보는 `CANDIDATES.md` |
| **P11.5-3** ✅ | **README 에 노트 절** | — | 노트를 써 놓고 저장소 첫 화면에서는 있는 줄 알 수 없었다. 링크 대상은 파일 존재로 확인(경로가 틀리면 README 에서만 조용히 깨진다) |
| **P11.5-4** ✅ | **프로젝트 페이지(GitHub Pages)** | — | `docs/index.html`. README 를 읽지 않고도 이 앱이 뭔지 알 수 있는 자리. 두 규칙을 지켰다 — **노트 본문을 복제하지 않고**(어느 쪽이 최신인지 알 수 없게 된다), **낡을 숫자를 넣지 않는다**(손으로 적은 통계 다섯 중 셋이 틀렸던 적이 있다). 손으로 만들지 않고 `개발노트/make_pages.py` 한 템플릿에서 뽑는다 |
| **P11.5-5** ✅ | **언어 표기 — 진단이 틀렸다** | — | GitHub 이 이 저장소를 JavaScript 로 잡는 것이 `web/lib/` 번들 탓인 줄 알고 `linguist-vendored` 를 넣었으나 **효과가 없었다.** 재보니 linguist 가 그 번들을 이미 자동 제외하고 있었고, JS 112,095B = `web/js` 82,823 + `tests` 29,272 로 정확히 일치한다. **바이트로 세면 맞는 판정이라 억지로 바꾸지 않았다** — 근거를 `.gitattributes` 주석에 남겼다 |
| **P11.5-6** ✅ | **페이지 가독성 2회** | — | "눈이 아프다"는 피드백에서 출발. 대비 16:1 → 약 9:1(종이 톤), 한글 자간 0(라틴 기준 음수 자간은 한글에서 답답하다), 본문 16 → 18px, 모서리·경계선을 눅였다. **WCAG 는 하한만 정하고 상한을 정하지 않는다** — 기준을 통과해도 오래 보면 피곤할 수 있다는 것이 이 작업의 근거 |

> **여기서 배운 것**: P11.5-5 는 **앞 커밋의 진단이 틀렸다는 것을 다음 커밋이 실측으로 뒤집은** 사례다.
> 고치지 않고 근거만 남긴 것이 결론이었다 — `.js` 에 `linguist-language=Python` 을 거는 것은 거짓말이고,
> 테스트를 빼도 82,823 vs 46,467 로 여전히 JavaScript 다. **틀린 판단을 지우지 않고 남겨야** 같은 자리를
> 다시 건드릴 때 되돌아가지 않는다.

### Phase 12 — 진행 중 (2026-09-12 갱신 · **①~⑮ 완료, 나머지 착수 전**)

Phase 11 로 원래 로드맵이 다시 소진되어 새로 뽑은 목록. **착수 전에 이 중 무엇을 할지 먼저 합의한다**
— 전부 하기로 정해진 게 아니다. 이미 기각한 것(발사 텔레메트리 4-3절 · 토스트 알림 P8-8 ·
한/영 토글 11절 · 딥링크 P11-4)은 **다시 올리지 않았다.**

> **뽑을 때 실측한 것 (2026-09-11)**: `logging` 이 코드에 **한 줄도 없었고**, **WebView2 부재
> 안내도 코드에 없었다** — 10절 리스크 표에는 "안내"라고 적혀 있어 문서와 코드가 어긋난 상태였다.
> **둘 다 v1.6.0(P12-9/10/11)에서 해소됐다.**

#### A. 앱을 여는 이유를 늘린다 (사용자 가치)

| # | 후보 | 난이도 | 왜 / 무엇 |
|---|------|--------|-----------|
| ✅ **P12-1** | **가시 통과 예측(실제로 눈에 보이는 것만)** | 중간 | 지금 통과 예측(P6-2)은 **기하학적 통과를 전부** 낸다. 실제로 보이려면 ① 위성이 햇빛을 받고 ② 관측자는 어두워야 한다 — `visual` 그룹의 존재 이유가 이건데 그 판정이 없다. **subsolar 계산은 터미네이터(P6-3)에 이미 있어 재사용**, 외부 API 불필요 |
| ✅ **P12-2** | **"오늘 밤 볼 만한 통과" 요약** | 낮음 | 현재는 위성 **1개를 골라야** 통과를 본다 — 무엇을 고를지 모르면 못 쓴다. 관심 위성 + `visual` 전체를 훑어 24시간 통과를 최대고도 순으로 한 목록에. P12-1 과 짝 |
| ✅ **P12-3** | **발사 카운트다운 집중 화면** (v1.10.0) | 낮음 | T-1시간 안쪽 발사가 있으면 큰 카운트다운 + 중계 링크 + 발사장 확대. 티커는 흘러가서 "지금 곧 발사"라는 사실이 약하다 |
| ✅ **P12-4** | **발사 궤적 근사선** (v1.16.0) | 중간 | 보류된 Phase 4 의 **실현 가능한 대체**. `sin A = cos i / cos φ` 로 방위각을 풀어 대권 2,200km 를 점선으로. **실측 아님을 "근사입니다"가 아니라 가정을 숫자로** 적는다 — 실측 441건 중 57%(LEO)는 이름만으로 경사를 알 수 없어 그 사실을 함께 밝힌다. 자세한 것은 `docs/DECISIONS.md` |

#### B. 데이터 커버리지 구멍

| # | 후보 | 난이도 | 왜 / 무엇 |
|---|------|--------|-----------|
| ✅ **P12-5** | **위성 메타데이터(SATCAT)** · ✅ **P12-5b** 종류/소유국 필터(v1.9.0) | 중간 | 위성 상세가 지금 **TLE 에서 나오는 것뿐**(이름·NORAD·궤도요소). Celestrak SATCAT 에는 발사일·소유국·타입(payload/rocket body/debris)·크기가 있다 → 국가별 필터와 "이건 위성이 아니라 잔해" 구분이 생긴다. **새 데이터 소스이므로 `python api_client.py` 스모크에 한 줄 추가**(로드맵 원칙) |
| ✅ **P12-6** | **아카이브 연도 상한·페이지 캡 재검토** (v1.12.0) | 낮음 | `ARCHIVE_MAX_PAGES = 5`(연도당 500건) · 최근 5년. 지금은 여유가 있지만 **캡에 걸리면 조용히 잘린다** — 잘렸다는 표시가 화면에 없다 |
| ✅ **P12-7** | **통계의 모수를 명시** (v1.11.0) | 낮음 | 통계 패널(P8-10)이 "불러온 데이터 기준"인데 화면엔 그 말이 없다. 아카이브를 안 불러오면 **성공률이 최근 100건 기준**인 걸 모른 채 본다 — 조용한 거짓말의 전형 |
| ✅ **P12-8** | **재진입·수명 종료 표시** → **S16-2 에서 열림**(v1.34.0) | 중간 | TLE 가 사라진 위성(재진입)을 구분하지 못한다. 그룹에서 빠지면 그냥 없어진다. **보류 근거(새 저장 구조가 필요하다)는 그대로 맞지만 유일한 길이 아니었다** — SGP4 전파가 실패하는 시점을 이진 탐색해 저장 없이 예보를 낸다. Phase 16 절 참조 |

#### C. 배포 앱으로서의 기본기 — **지금 가장 얇은 곳**

| # | 후보 | 난이도 | 왜 / 무엇 |
|---|------|--------|-----------|
| ✅ **P12-9** | **로그 파일** | 낮음 | **`logging` 이 코드에 한 줄도 없다.** exe(`pythonw`)로 돌면 stderr 도 사라져 **사용자가 겪은 실패의 신호가 0** 이다. `%APPDATA%\RL3D\logs\` 에 회전 로그(`encoding="utf-8"` 명시 — 전역 규칙) |
| ✅ **P12-10** | **크래시 안내 화면** | 낮음 | 시작 중 예외가 나면 창이 그냥 안 뜬다. 로그 경로를 띄우는 최소 안내. **"고장이 아닌데 고장처럼 보인다"(A 계열)의 마지막 구멍** |
| ✅ **P12-11** | **WebView2 부재 안내** | 낮음 | 10절 리스크 표에 있는데 **코드에 없다.** 흰 화면만 뜬다 |
| ✅ **P12-12** | **업데이트 확인** (v1.14.0) | 낮음 | GitHub Releases API 로 새 버전만 알린다. 비교는 파이썬이 끝내고 JS 는 배지 표시만. **`update_available` 을 캐시에 굳히지 않는다** — 굳히면 사용자가 업데이트한 뒤에도 TTL 동안 계속 새 버전이 있다고 말한다. 자세한 것은 `docs/DECISIONS.md` |
| ✅ **P12-22** | **밀린 릴리스 정리** (2026-09-12) | 낮음 | 실측대로 릴리스가 `v1.2.0`, 코드가 `v1.13.0` 이었다. **태그 14개(v1.3.0~v1.13.0)를 버전을 올린 커밋에 annotated 로 붙여 푸시**하고, 내려받을 수 있는 릴리스는 **`v1.13.0` 하나**만 냈다 — 과거 버전 exe 를 지금 다시 빌드하는 것은 그 시점의 재현이 아니다. 자세한 것은 `docs/DECISIONS.md` |
| ✅ **P12-13** | **CI (GitHub Actions)** | 낮음 | `test_parsing.py`(34) + `test_frontend.js`(102) 둘 다 **네트워크 불필요** → 그대로 올라간다 |

#### D. 지도·화면 표현력

| # | 후보 | 난이도 | 왜 / 무엇 |
|---|------|--------|-----------|
| ✅ **P12-14** | **키보드 조작** (v1.11.0) | 낮음 | 단축키가 하나도 없다. `/` 검색 · `Esc` 패널 닫기 · `S` 사이드바 · 숫자키 레이어 |
| ✅ **P12-15** | **발사 밀도 히트맵** (v1.15.0) | 중간 | 마커·클러스터는 "어디서 몇 번"을 **구조적으로** 못 보여준다 — 같은 발사장에 겹친 점은 1건이나 300건이나 똑같고, 클러스터는 줌에 따라 기준이 바뀐다. 소스를 따로 파고(기존 소스는 `cluster:true` 라 클러스터 하나가 점 하나로 세어진다) 색 눈금을 데이터에 맞춰 다시 잡는다. **테스트가 만점인 채로 화면이 두 번 틀렸다** — 자세한 것은 `docs/DECISIONS.md` |
| ✅ **P12-16** | **위성 궤적 다중 주기 / 미래 위치 스크럽** (v1.13.0) | 낮음 | 지금은 ±1주기(P6-1). "3시간 뒤 어디" |
| **P12-17** | **로컬 타일 번들(완전 오프라인)** | 높음 | "나중에" 표에 이미 있던 항목. 저줌(0~4)만 번들해도 **"검은 지도"가 사라진다** — exe 크기와의 교환 |

#### E. 구조·검증 (겉으로 안 보이지만 다음 작업을 싸게 만든다)

| # | 후보 | 난이도 | 왜 / 무엇 |
|---|------|--------|-----------|
| ❌→✅ **P12-18** | ~~**`panels.js` 분리**~~ → **P14-4 에서 실행**(2026-09-13) | 중간 | **당시 기각(2026-09-12 재측정).** 660줄이고 가장 자주 건드리지만(30커밋 6회) **매번 작다**(중앙값 22줄) — 분리해서 아낄 것이 별로 없다 |
| ✅ **P12-23** | **`sats.js` 분리** (2026-09-12) | 중간 | **762줄로 이 리포에서 가장 크고, 변경 규모도 가장 크다**(30커밋 4회·443줄). TLE 로드·렌더·지상궤적·통과예측·가시판정·대역필터·추적이 한 파일이다. 후보를 뽑던 시점(2026-09-11)에는 아직 작아서 목록에 없었다. **762 → 169+196+226+175줄 네 파일.** 앱 동작이 안 바뀌어 버전은 그대로. `index.html`↔하네스 로드 순서 일치 테스트를 같이 넣었다 |
| ✅ **P12-19** | **`api_client.py` 분리** (2026-09-12) | 중간 | 722 → **480 + `api_parsing.py` 225 + `api_errors.py` 50**. **순수 부분만 떼어냈다** — `CACHE_DIR`·`_http_get` 은 테스트가 재대입해서 격리하는 값이라 옮기면 전역 8번에 걸린다. 앱 동작이 안 바뀌어 버전은 그대로 |
| ✅ **P12-20** | **파이썬 테스트를 파싱 밖으로** (v1.12.0) | 중간 | 현재 회귀는 **파싱만** 덮는다. TTL 만료 판정·stale 캐시 폴백·아카이브 페이지 캡·설정 병합은 **틀려도 조용하다**(C 계열과 같은 모양) |
| ✅ **P12-21** | **PLAN.md 사후 정리** | 낮음 | v1.5.2 버그 3건 · 개발 노트(`docs/notes/`) · `docs/index.html`(GitHub Pages)은 **로드맵에 없던 작업**이다. Phase 10 처럼 사후 정리해 넣지 않으면 계획서가 실제와 어긋난 채로 남는다 |

> **Phase 12 는 끝났다**(①~⑮ · v1.6.0 ~ v1.16.0 + 릴리스 정리). P12-18 은 재측정으로 기각(아래 ⚠),
> P12-8·P12-17 은 보류다. **살아 있는 계획은 아래 Phase 13 절 한 자리에만 둔다**(전역 규칙 5) —
> 2026-09-12 정기 점검에서 이 자리에 낡은 "다음 착수" 표가 남아 Phase 13 절과 **서로 다른 말**을
> 하고 있었다. 규칙이 경고한 바로 그 상태였다.
>
> ⚠ **구조 정리 후보를 다시 쟀다 — 후보 자체가 바뀌었다(2026-09-12, v1.16.0 직후).**
> 최근 30커밋을 **파일명으로 정확히 걸러** 횟수와 변경 규모를 함께 냈다:
>
> | 파일 | 현재 | 횟수 | 총 변경 | 커밋별 |
> |---|---|---|---|---|
> | `web/js/sats.js` | **762줄** | 4회 | **443줄** | 94·165·21·163 |
> | `api_client.py` | 722줄 | 5회 | 281줄 | 98·40·97·9·37 |
> | `web/js/panels.js` | 660줄 | 6회 | 171줄 | 5·10·66·14·31·45 |
> | `main.py` | 228줄 | **12회** | 85줄 | 2·2·6·2·2·2·2·2·5·22·2·36 |
>
> ① **`main.py` 12회는 또 거짓말이다** — 중앙값 2줄, 전부 버전 상수 한 줄. 전날 관찰이 그대로 재현됐다.
> ② **P12-18(`panels.js`)은 기각한다.** 가장 자주 건드리지만 **매번 작다**(중앙값 22줄).
>    자주 건드리는데 매번 조금이면 분리해서 아낄 것이 별로 없다 — "자주 바뀐다"만으로
>    분리 근거를 삼으면 안 된다는 것을 이걸로 배웠다.
> ③ **가장 비싼 곳이 후보 목록에 아예 없었다** → `sats.js` 를 **P12-23** 으로 새로 넣는다.
>    위성 기능(P12-1·P12-5·P12-16)이 연달아 들어가며 커진 파일이라, 후보를 뽑던 2026-09-11
>    시점에는 아직 작았다. **후보 목록도 측정과 함께 낡는다.**
>
> ⚠ **(이력) P12-19 의 착수 근거가 낡았다 — 재측정으로 정정한다(2026-09-12).**
> 2026-09-11 에 "최근 20커밋 중 14회 수정, 이 리포에서 가장 자주 바뀌는 파일"이라고 적었는데,
> **오늘 다시 재니 `api_client.py` 5회 · `panels.js` 5회로 동률**이다. 측정 창(최근 20커밋)이
> 그 사이 이동했을 뿐이고, 두 파일의 순서를 가르던 근거는 지금 없다.
> **그리고 이번 재측정에는 함정이 하나 더 있었다**: 1위가 `main.py`(10회)로 나왔지만,
> 그중 여덟 번이 `1 insertion, 1 deletion` = **버전 상수 한 줄**이었다. 릴리스마다 올리는
> 줄을 세면 구조적 부담과 무관한 파일이 1위가 된다. **churn 은 횟수만 세면 거짓말을 한다 —
> 변경 규모를 같이 봐야 한다.**
> → P12-19 · P12-18 의 순서는 **착수 시점에 다시 재고** 정한다. 파일 크기(722 vs 655줄)만으로는
> 갈리지 않는다.

> ⚠ **P12-8(재진입 표시)에 대한 앞선 판단은 틀렸다 — 실측으로 정정한다(2026-09-11).**
> "`decay_date` 가 이미 들어왔으니 거의 공짜"라고 적었으나, **우리가 쓰는 그룹의 SATCAT 에는
> 재진입 건수가 0 이다**(stations·visual·geo·starlink 전부). 그룹 목록은 *지금 추적 중인* 물체만
> 담기 때문이다. 전체 `satcat.csv` 기준으로는 70,649건 중 35,559건이 재진입이지만, 그건 6.7MB
> 전체를 받아야 보인다. **P12-8 의 실제 내용은 "지난 실행에 있던 NORAD 가 이번에 사라졌는가"를
> 기억하는 것**이고, 그건 새 저장 구조가 필요하다 — 싸지 않다.

> **착수 전에 측정한다 (P11-6 에서 배운 것)**: P12-17·P12-18·P12-19 는 **먼저 재고** 판단한다.
>    타일 번들은 "exe 가 몇 MB 늘어나는가", 분리는 "실제로 그 파일을 얼마나 자주 건드리는가".
>    **이 규칙이 이번에 값을 했다** — P12-18 이 먼저인 줄 알았는데 측정해 보니 `api_client.py`(P12-19)가
>    2배 넘게 자주 바뀌는 파일이었다.

> 📒 **①~⑫ 번의 설계·실측·오판 정정은 `docs/DECISIONS.md` 로 옮겼다**(2026-09-11 이후 계속).
>    계획서에는 **남은 일만** 둔다 — 세션 기록이 쌓여 어느 것이 최신인지 날짜로만 구분되던 상태였다.

### Phase 13 — **완료** (2026-09-12 · 실측으로 뽑아 하루에 소진)

Phase 12 로 A(사용자 가치)·C(배포 기본기)·E(구조) 계열이 전부 소진돼 목록이 비었다.
**브레인스토밍이 아니라 측정으로** 다시 뽑았다 — 지금 앱이 **이미 받아오고도 안 쓰는 데이터**와
**화면에 있지만 실제로는 안 보이는 것**을 찾았다.

> **실측 방법**: LL2 예정 발사 **라이브 50건**에서 필드별로 값이 있는 비율을 셌다(요청 1회).
> 픽스처 6건과 캐시로 교차 확인. `grep TODO/FIXME` 0건, 앱 로그의 WARNING/ERROR **0건** —
> 청소할 잔해는 없다는 것도 같이 확인했다.

#### A. 이미 받아오는데 안 쓰는 데이터 (새 API 요청이 0이다)

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P13-1** | **발사 순서표(`timeline`)** (2026-09-12) | 중간 | 상세 패널의 순서표 + 집중 화면의 "지금 어느 단계". **착수 때 적은 10% 는 예정 발사만 본 수치였다 — 캐시 100건(예정 50·지난 50) 실측은 35건(35%)이고 거의 전부 지난 발사다.** 자세한 것은 `docs/DECISIONS.md` |
| ✅ **P13-2** | **"올해 전 세계 N번째 발사"** (v1.18.0) | 낮음 | 통계 패널이 처음으로 **표본이 전체의 얼마인지** 말한다(`215건 중 49건, 23%`). **이미 일어난 발사와 예정 발사를 나눠 세야 한다** — 실측 215 대 356, 섞으면 141건을 부풀린다. 자세한 것은 `docs/DECISIONS.md` |
| ✅ **P13-3** | **발사대 재사용 간격·발사장 통산** (v1.21.0) | 낮음 | 상세 패널 기록 줄 + 발사장 관점의 **통산 모수**(`912회 중 14건`). 실측 범위 **2.79일 ~ 1541일**이라 단위를 크기에 맞춰 바꾼다. 기간 파서는 P13-1 것을 재사용 |

#### B. 화면에 있는데 실제로는 안 보이는 것

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ❌ **P13-4** | ~~**`probability`·`weather_concerns` 행이 죽어 있다**~~ | 낮음 | **기각 — 진단이 틀렸다(2026-09-12 정정).** 아래 ⚠ |
| ✅ **P13-5** | **시간대 표기** (v1.19.0) | 낮음 | 툴바 버튼이 **지금 쓰는 시간대를 상시 표시**(`🕓GMT+9`/`🕓UTC`)하고 `T` 로 전환. **다섯 포맷터가 한 스위치를 본다** — 갈라지면 표기가 없는 것보다 나쁘다. 자세한 것은 `docs/DECISIONS.md` |

#### C. 쓰기 전에 마찰이 있는 것

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P13-6** | **관측 위치 지정** (v1.20.0) | 낮음 | 도시 검색·좌표 입력·지도 클릭 팝오버. **외부 지오코딩 없이** 번들 목록 + 이미 가진 발사장으로 요청 0. 곁들여 **오늘 밤 탭의 안내가 시작할 수 없는 안내였던 것**도 고쳤다(버튼이 위성을 골라야 나타났다). 자세한 것은 `docs/DECISIONS.md` |

> ⚠ **P13-4 는 틀린 진단이었다 — 실측으로 정정한다(2026-09-12).**
> "56건 전부 비어 있다"고 적었는데, 그 표본이 **예정 발사 50건 + 픽스처 6건**뿐이었다.
> 캐시 100건(예정 50 · **지난 50**)으로 다시 세니 `probability` **14건** · `weather_concerns` **9건**이고
> **전부 지난 발사**다(예정은 0건). 죽은 UI 가 아니라 **발사 뒤에 채워지는 필드**다 —
> P13-1 캡처에서 "발사 확률 70%"가 화면에 실제로 떠 있는 것을 보고 알았다.
> **표본을 한쪽(예정)만 보고 "전부 비어 있다"고 말한 것이 잘못이다.** 지울 것이 없으므로 기각한다.
>
> **Phase 13 은 P13-4(기각)를 빼고 전부 끝났다**(v1.17.0 ~ v1.21.0).
>
> ⚠ **그 뒤 곧바로 결함이 하나 드러났다 — 캐시 스키마(v1.22.0 에서 고침).**
> Phase 13 에서 파싱에 필드를 넷 더했는데, **지난 연도 아카이브 캐시는 TTL 이 없어 영구**라
> 이미 불러온 해에서는 새 기능이 **영원히 안 보였다**(오류도 경고도 없다).
> `launches.json` 은 TTL 15분이라 저절로 나아 **개발 중에는 보이지도 않았다** —
> 세션 끝에 캐시 파일을 직접 열어 보고서야 찾았다.
> **기능을 여럿 이어 붙인 날에는 그 데이터가 어디에 굳어 있는지 한 번 확인한다.**
> 보류는 그대로: **P12-8**(재진입 — 새 저장 구조가 필요) · **P12-17**(오프라인 타일 — 라이선스).
>
> **다음 후보는 2026-09-13 에 실측으로 뽑아 아래 Phase 14 절에 두었다.**

### Phase 14 — 착수 전 (2026-09-13 · 실측으로 뽑음)

Phase 13 이 소진되어 다시 **측정으로** 뽑았다. 이번에는 한 층 더 내려갔다 —
정규화 결과(35개 필드)는 **전부 JS 에서 참조되고 있어** 그 층엔 구멍이 없었고,
**원본 응답 vs `api_parsing.py`** 를 대조하니 값이 20% 이상 채워지는데 **파싱이 이름조차
안 보는 필드가 64개**였다. 전부 이미 받아오는 것이라 **새 API 요청이 0**이다.

> **실측 방법 (2026-09-13 · 네트워크 0)**: 캐시 `launches.json` 100건으로 정규화 필드
> 채움률을, `git ls-files web/js | grep` 으로 JS 참조 수를 셌다. 원본 필드는 픽스처
> `ll2_upcoming/previous.json` **6건**을 2단계까지 평탄화해 `api_parsing.py` 소스와 대조.
> SATCAT 은 `celestrak_satcat.csv` 7행의 컬럼별 채움률.
>
> ⚠ **표본 한계를 먼저 적는다** (P13-4 에서 표본을 한쪽만 보고 틀렸다).
> LL2 **원본**은 픽스처 6건뿐이다. 아래 필드 대부분은 로켓·기관의 **정적 메타데이터**라
> 발사 상태와 무관하게 채워지지만, **`launcher_stage`(4/6)만은** 예정/지난 편향 가능성이
> 있어 **착수 전 라이브 1회로 재확인**한다.
>
> **청소할 잔해는 없다**: 소스 `TODO/FIXME` **0건** · 호출부 없는 최상위 JS 함수 **0건** ·
> 앱 로그 160줄 중 WARNING **1건**(`429 → 오래된 캐시 사용` — 설계대로 동작한 기록) ·
> 테스트 **661건 전부 통과**(파싱 69 · 캐시 56 · 프론트 536).

#### A. 이미 받아오는데 파싱이 안 보는 것 (새 요청 0)

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P14-1** | **로켓 제원 + 부스터 재사용 이력** (v1.23.0) | 중간 | 상세 패널의 로켓이 지금 **이름 한 줄뿐**이다. `rocket.configuration.*` 의 `length·diameter·launch_mass·leo_capacity·to_thrust·maiden_flight·reusable·min/max_stage` 와 통산 성적(`total/successful/failed/consecutive_successful_launches`)이 **6/6(100%)**. `rocket.launcher_stage`(**4/6, 66%**)에는 부스터 시리얼·비행 횟수·착륙 결과가 있어 **"이 부스터 19번째 비행"**이 가능해진다 — 재사용 시대의 핵심 정보인데 앱 어디에도 없다 |
| ✅ **P14-2** | **발사장 현지 시간대** (v1.24.0) | 낮음 | `pad.location.timezone_name` **6/6(100%)**. **P13-5(시간대 표기)와 정확히 붙는다** — 지금 고를 수 있는 건 내 시간대/UTC 둘뿐이라 **"발사장 현지로는 새벽 3시"**를 모른다. 포맷터 다섯이 이미 한 스위치를 보고 있어 붙이는 비용이 낮다(갈라지면 표기가 없느니만 못하다는 P13-5 의 교훈은 그대로 적용) |
| ⚠️✅ **P14-3** | **~~SATCAT 궤도 제원~~ → 근지점·원지점 (TLE 계산)** (v1.25.0) | 낮음 | **착수하며 재 보니 후보가 틀렸다.** ① `PERIOD`·`INCLINATION` 은 **이미 화면에 있다**(TLE 실시간 계산) — SATCAT 과 차이가 중앙값 0.06분·0.00° 라 중복이다. ② `APOGEE`·`PERIGEE` 는 **TLE 로 계산하는 편이 낫다**(차이 중앙값 2.69km, TLE 커버리지 157/157, SATCAT 은 그룹에 따라 아예 안 받는다). ③ `ORBIT_TYPE`·`ORBIT_CENTER` 는 실제 `visual` 159행에서 **전부 빈값 / 전부 `EA`** 라 정보량 0 → **기각**. 후보를 뽑을 때 본 픽스처 7행이 역사적 물체라 채워져 보였던 것이다 |

#### B. 구조 — 기각 근거가 낡았다

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P14-4** | **`panels.js` 분리** (앱 동작 불변 → 버전 유지) | 중간 | **P12-18 의 기각을 재고한다.** 2026-09-12 에 *"30커밋 6회·중앙값 22줄이라 아낄 게 별로 없다"* 고 기각했는데, 2026-09-13 에 다시 세니 **30커밋 12회·811줄**이다 — 횟수도 규모도 두 배 넘게 늘었고 **773줄로 리포 최대 JS** 다. **P14-1~3 을 넣고 다시 재니 902줄·15회 940줄**이다(예상대로 — 앞 셋이 전부 이 파일을 키웠다). **두 번째로 큰 파일(`launches.js` 475줄)의 거의 두 배**이고, 내용은 다섯 덩어리로 뚜렷하게 갈린다: 관심 목록 83 · 사이드바 136 · 발사 상세 261 · 위성 상세 127 · 통계와 관점 화면 293줄. **후보 목록만 낡는 게 아니라 기각 근거도 낡는다**(P12-23 에서 배운 것의 뒷면) |

#### P14-4 결과 — 902줄 → 여섯 파일 (2026-09-13, 앱 동작 불변이라 버전 유지)

| 파일 | 줄 | 들어간 것 |
|---|---|---|
| `favorites.js` | 84 | `isFavLaunch`·`isFavSat`·`saveFavorites`·`toggleFav*`·`updateFavBtn`·`favBtnHtml`·`bindFavBtn`·`renderFavList` |
| `sidebar.js` | 137 | `setSidebarTab`·`tonightBlocker`·`renderTonightList`·`renderSatList`·`pickSatellite`·`renderSidebar`·`toggleSidebar` + 최상위 `basemap`·`sidebarTab` |
| `panels.js` | 249 | 발사 상세 — `rocketSpecBlock`·`boosters*`·`windowText`·`contextText`·`updatesBlock`·`openPanel`·`closePanel` + `NET_PRECISION_KO` |
| `satpanel.js` | 128 | `satDetails`·`apsides`·`apsidesText`·`satRowsHtml`·`satMetaHtml`·`satBadgeHtml`·`openSatPanel`·`refreshSatPanel` + `MU_EARTH`·`R_EARTH` |
| `stats.js` | 295 | `computeStats`·`statsScope`·`scopeNoteHtml`·`statBars`·`entityBars`·`siteTotals*`·`showEntityStats`·`orbitalYear*` + `PAD_ABBREV`·`ENTITY_VIEWS`·`topEntries` |
| `utils.js` (+18) | — | `row`·`entityRow` 를 올렸다 |

로드 순서는 `panels` 자리에 **`favorites` → `sidebar` → `panels` → `satpanel` → `stats`**.
`index.html` 과 `tests/harness.js` 를 둘 다 고쳤고, 두 목록의 일치는 `test_frontend.js` 가 잰다(P12-23).

- **합의해 둔 근거 하나가 실측에 빗나갔다.** 계획은 *"`row()`·`entityRow()` 는 다섯 중 셋이 쓴다"* 고
  적었지만, 세어 보니 **`row` 는 둘**(발사 상세·위성 상세), **`entityRow` 는 하나**(발사 상세)뿐이다.
  올린 결론은 그대로 맞다 — 위성 상세가 발사 상세에 **역방향으로 의존**하던 것을 끊는 게 이유고,
  그건 사용처가 둘이어도 성립한다. 다만 **"셋"이라는 수치는 세어 보지 않고 적은 것**이었다.
- **검증**: 프론트 회귀 **584건 전부 통과** · 분리 전후 최상위 심볼 **누락 0·중복 0**
  (`git show HEAD:web/js/panels.js` 와 여섯 파일의 `^function|let|const` 를 `comm` 으로 대조).
  앱을 2번 모니터에 띄워 창 좌표 `(2880, 375)` 와 티커·툴바·지도·타임라인 렌더를 눈으로 확인,
  `rl3d.log` 에 에러 0건.

> **순서: ~~P14-1~~ → ~~P14-2~~ → ~~P14-3~~ → ~~P14-4~~ (전부 완료).**
> 분리(P14-4)를 **마지막에** 두는 이유: 앞 셋이 **전부 상세 패널을 키우는** 작업이라,
> 지금 나누면 새 기능이 어느 조각에 들어갈지 모른 채 경계를 긋게 된다.
> P12-23(`sats.js`)도 커진 뒤에 쟀고 그래서 경계가 맞았다 — **재측정은 기능을 넣은
> 뒤의 수치로 해야 의미가 있다.**
>
> ⚠ **셋 다 정규화 결과의 모양을 바꾼다 → `CACHE_SCHEMA` 를 올린다.**
> v1.22.0 에서 당한 바로 그 자리다(지난 연도 아카이브는 TTL 이 없어 **영구**라,
> 안 올리면 새 필드가 **영원히 안 채워지고 오류도 경고도 없다**).
>
>
> ⚠ **P14-3 에서 후보 자체가 실측에 뒤집혔다(2026-09-13).** 세 필드 중 둘은 이미 화면에 있거나
> TLE 로 더 잘 구해지고, 나머지 둘은 실제 그룹에서 **정보량이 0**이었다. 원인은 **후보를 뽑을 때
> 쓴 표본이 픽스처 7행**이었다는 것 — 1957년 물체가 섞인 역사적 데이터라 채움률이 높아 보였다.
> **P13-4 에서 예정 발사만 보고 "전부 비어 있다"고 틀렸던 것과 같은 함정**을, 이번엔 반대 방향으로
> 밟았다. 후보 단계의 채움률은 **그 기능이 실제로 쓸 그룹**에서 다시 재야 한다.
> 보류는 그대로: **P12-8**(재진입 — 새 저장 구조) · **P12-17**(오프라인 타일 — 라이선스).

### Phase 15 — 착수 전 (2026-09-13 · 실측으로 뽑음)

Phase 14 가 소진되어 다시 쟀다. **이번엔 표본을 라이브로 바꿨다** — P14-3 에서 픽스처
7행을 보고 후보를 뽑았다가 실제 그룹에서 정보량 0이라 기각했기 때문이다.

> **실측 방법 (2026-09-13 · LL2 요청 2회)**
> ```powershell
> # 원본: 라이브 예정 50 + 지난 50 을 받아 2단계까지 평탄화 → api_parsing.py 소스와 대조
> .venv\Scripts\python.exe -c "import api_client as ac; print(ac._http_get(ac.LL2_UPCOMING)[:80])"
> # 정규화 채움률·JS 참조: 캐시 launches.json 100건(예정 50·지난 50) + git ls-files web/js
> # 잔해: grep TODO/FIXME · CSS 클래스 대 HTML/JS · 최상위 JS 함수 대 호출부
> ```
> **표본 한계**: 라이브 100건은 `net` 기준 앞뒤 50건씩이라 **최근 몇 달에 쏠려 있다.**
> 아래 채움률은 "요즘 발사"의 값이고, 지난 연도 아카이브는 다를 수 있다.
> 그래서 **예정/지난을 갈라 적었다** — 한쪽만 채워지는 필드를 여기서 걸러야 한다.

**측정 결과, 파싱이 이름조차 안 보는 필드가 86개**다(둘 중 한 그룹에서 20% 이상 채워지는 것만).
전부 이미 받아오는 것이라 **새 API 요청이 0**이다. 값을 실제로 찍어 쓸 것만 추렸다.

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P15-1** | **맥락 줄의 나머지 반쪽** (v1.26.0) | 낮음 | **지금 네 축이 저마다 다른 기준으로 말한다** — 발사대·발사장은 *통산*, 기관·세계는 *올해*. 한 줄에 `이 발사장 통산 53번째 · SpaceX 올해 47번째` 가 나란히 서서, 읽는 쪽은 두 숫자를 같은 기준으로 읽기 쉽다. 빠진 반쪽이 전부 들어와 있다: `pad_launch_attempt_count_year` **100/100** · `location_launch_attempt_count_year` **100/100** · `agency_launch_attempt_count` **100/100** · `orbital_launch_attempt_count` **98/100** |
| ✅ **P15-2** | **발사 비용** (v1.27.0) | 낮음 | `rocket.configuration.launch_cost` **65/100**(예정 32·지난 33 — 편향 없음), 서로 다른 값 12종(600만~9천만 USD). 앱에 **비용 축이 통째로 없다.** 제원 블록 한 줄로 시작해, 통계·관점 화면의 합계까지 가면 중간 난이도 |
| ✅ **P15-3** | **착륙 통산 성적** (v1.30.0) | 중간 | P14-1 이 **부스터 한 대의** 착륙 결과를 넣었는데, 그 **위 집계가 없다**. 기관 `attempted/successful/consecutive_successful_landings` **66/100** · 로켓 **53/100** · 발사장 `total_landing_count` **59/100**. 0 인 쪽(재사용 안 하는 기관·로켓)이 34~47건이라 **0 이면 안 그리는** 처리가 필요하다 |
| ✅ **P15-4** | **로켓 계열(family)** (v1.32.0) | 중간 | `rocket.configuration.family` **86/100**(착수 시 재측정 · 21종). 로켓 관점 화면이 **변형별로 쪼개져** `Falcon 9 Block 5` 와 `Falcon Heavy` 가 남남이었고 `Long March` 는 10종으로 갈렸다. 상세 패널 `계열 Falcon · 변형 2종 ›` + 계열 관점 화면(**변형별 막대**) + 로켓 화면에서 올라가는 줄. `variant`(**72/100**)는 **기각** — 아래 ⚠ |
| ✅ **P15-5** | **미션 참여 기관** (v1.29.0) | 낮음 | `mission.agencies[]` **59/100** · 23곳(SES · ESA · USSF · ROSCOSMOS …). 앱은 **쏘는 쪽(발사 서비스 제공자)만** 말하고 **누구를 위한 발사인지**를 말한 적이 없다 |
| ✅ **P15-6** | **GTO 탑재량** (v1.28.0) | 낮음 | `gto_capacity` **56/100**. P14-1 제원 블록에 **LEO 만** 넣었다 — 정지천이궤도 탑재량은 통신위성 발사를 읽을 때 쓰는 수치다. 블록이 이미 있어 한 줄이다 |
| ✅ **P15-7** | **정보 갱신 시각** (v1.31.0) | 낮음 | `last_updated` **100/100** · 100건 전부 다른 값. 앱의 `1분 전 갱신` 은 **내가 언제 받아왔나**이고, **LL2 가 이 발사 정보를 언제 고쳤나**는 다른 값이다. 예정 발사가 며칠째 안 바뀌었는지가 보인다 |

**기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| `rocket.configuration.apogee` | 채움 **42/100** 인데 **서로 다른 값이 4개뿐**(200 · 500 · 20 · 40000). P14-3 의 `ORBIT_TYPE`(전부 빈값) 과 같은 갈래 — 채움률만 보면 통과하고 **정보량에서 떨어진다** |
| `vidURLs[].start_time` · `publisher` · `end_time` | **예정 5/50 · 지난 39~40/50.** 중계 예고로 쓰려던 것인데 **예정 발사에는 거의 없다** — 쓰려는 그룹에서 비어 있으면 기능이 성립하지 않는다 |
| `infoURLs[]` · `flightclub_url` | 같은 편향(예정 3~4/50) |
| `*_url` · `logo_url` · `wiki_url` · `administrator` · `nation_url` · `featured` | 채움률은 높지만 화면에 쓸 자리가 없다 |

> **P15-2 에서 값의 성격이 후보 설명과 달랐다(2026-09-13).** "발사 비용"으로 적어 뒀지만
> 재 보니 **같은 로켓은 항상 같은 값**(라이브 43종에 예외 0)이라 이 발사의 계약가가 아니라
> **로켓의 공시가**였다. 그래서 제원 블록이 자리가 되고, **합계·순위는 내지 않기로** 했다 —
> 채움률이 미국 49/55 대 **중국 2/21** 이라 어떤 집계도 "중국 발사는 공짜"로 읽힌다.
> **채움률이 높아도 그 분포가 한쪽에 쏠려 있으면 집계는 못 쓴다** — 채움률·정보량에 이어
> 세 번째로 볼 것이다(P14-3 에서 둘까지는 배웠다).
>
> 곁들여 **P14-1 이래 조용히 있던 결함**이 나왔다: `fmtQty(0, "kg")` 가 `"0 kg"` 이라는
> **truthy 문자열**을 내서 호출부의 `.filter(x => x[1])` 를 통과했다. 화면에
> `LEO 탑재량 0 kg` 이 떴다(GSLV Mk. II). **거르려고 쓴 falsy 필터가, 거를 값을
> 문자열로 감싸는 포맷터를 만나면 아무것도 안 거른다.**

> **P15-5 에서 채움률이 착수하며 반토막 났다(2026-09-13).** 후보에 `59/100` 으로 적었는데,
> 그중 **28건이 발사 서비스 제공자 자신**이었다(Starlink 자체 발사 등). 빼고 나니 **31/100**.
> **"값이 있다"와 "말할 게 있다"는 다르다** — 채움률·정보량·분포 쏠림에 이어 네 번째다.
> 다행히 남은 31건은 전부 새 정보였다(`SpaceX → NRO` · `Arianespace → EUMETSAT`).
>
> ⚠ **픽스처 6건은 이 기능을 전혀 안 덮는다** — `mission_agencies` 가 6건 모두 빈 리스트라
> 골든을 갱신해도 `[]` 만 굳는다. 그래서 `TestMissionAgencies` 5건을 명시로 넣었고,
> **제공자 제외를 지우는 변이는 그 5건이 잡는다**(골든은 못 잡는다).

> **P15-3 의 0 은 P15-2 의 0 과 성격이 달랐다(2026-09-13).** 착륙 시도가 0 인 기관이
> 34/100 인데, 그 0 은 **데이터가 없어서가 아니라 실제로 안 해서**다(Arianespace · ULA ·
> ROSCOSMOS · JAXA 는 소모형만 쓴다). P15-2 의 비용 0(중국 데이터 부재)과 달라
> **집계해도 거짓말이 안 된다.** 채움률이 같아도 **0 이 사실인지 부재인지**를 갈라야 한다.
>
> **발사장 `total_landing_count`(59/100)는 일부러 뺐다** — "이 발사장에 몇 번 착륙했나"는
> 착륙장 겸용 여부라 의미가 다르고, 세 자리를 한 번에 건드리면 검증이 흐려진다. 다음 후보로 남긴다.
>
> ⚠ **LL2 값끼리 안 맞는 자리를 찾았다.** SpaceX 기관 착륙이 `699 시도 · 671 성공 · 29 실패`
> 인데 671+29 = **700** 이다. 그래서 실패를 `시도-성공` 으로 **유도하지 않고** 받은 값만 쓴다 —
> 유도하면 우리가 틀린 숫자를 지어내게 된다. 로켓 쪽은 합이 맞는다(620 = 615+5).

> **P15-7 에서 경고를 만들려다 접었다(2026-09-13).** 30일 넘게 안 바뀐 예정 발사가
> 23/50 이라 경고감으로 보였는데, 겹침을 재 보니 **22건은 이미 `net_precision` 경고가
> 떠 있었다.** 새로 알려 주는 것은 **1건**뿐이다. **새 경고를 놓기 전에 기존 경고와
> 겹치는지 센다** — 안 세면 같은 말을 두 번 하는 화면이 된다. 사실 한 줄로 낮췄다.
>
> ⚠ **한 세션에 `CACHE_SCHEMA` 를 여섯 번(3→9) 올려 LL2 시간당 한도를 태웠다.**
> 스키마가 바뀌면 아카이브까지 통째로 다시 받는다. 429 에 걸린 뒤로는 **실제 캐시로
> 검증할 수 없어**, 받아 둔 원본 응답을 `api_parsing` 으로 파싱해 `harness.js` 로 렌더했다.
> 규칙은 `CLAUDE.md` 에 옮겼다 — **여러 기능을 잇달아 넣는 날은 스키마를 마지막에 한 번만.**

**청소할 잔해는 이번에도 없다** — `TODO/FIXME` **0건** · 앱 로그 WARNING **1건**(429 → 오래된 캐시, 설계대로) ·
테스트 **전부 통과**(프론트 584 · 파싱 · 캐시).

> ⚠ **"죽은 코드" 탐지가 처음에 전부 오탐이었다.** 호출부 없는 JS 함수 **14개**와 안 쓰는
> CSS 클래스 **7개**가 나왔는데, 확인하니 **22개 모두 살아 있었다.** 함수 쪽은
> `addEventListener("click", fn)` · `.filter(fn)` · `.map(fn)` 처럼 **이름만 넘기는 자리**를
> 못 본 것이고, CSS 쪽은 `m-${d.outcome}` · `tk-${...}` · `reason-${kind}` 로 **문자열을 이어 만드는**
> 클래스였다. **`grep name(` 로 세면 이 둘이 전부 죽은 것으로 나온다** — 잔해를 잴 때는
> 후보를 내기 전에 한 개씩 되짚어야 한다.

> ⚠ **P15-1~7 중 정규화 결과의 모양을 바꾸는 것은 전부 `CACHE_SCHEMA` 를 올린다.**
> 지난 연도 아카이브는 TTL 이 없어 **영구**라, 안 올리면 새 필드가 **영원히 안 채워지고
> 오류도 경고도 없다**(v1.22.0 에서 당한 자리).

> **P15-4 에서 배운 것 두 가지 (2026-09-14).** Phase 15 는 이것으로 끝났다.
>
> ① **변이 실험이 "단언이 두 상태를 구분하지 못하는" 결함을 잡았다.**
> 계열 버튼의 클릭 배선을 재려고 `includes("Falcon 계열")` 로 단언했는데, **로켓 화면에도
> `Falcon 계열 전체 ›` 줄이 있어 클릭 전에 이미 참**이었다. `addEventListener` 를 통째로
> 뜯은 변이가 **통과했다**(7건 중 이 하나만 놓쳤다). 고친 방법은 **클릭 전에 거짓임을 먼저
> 단언**하고 두 화면을 가르는 문자열(`<h2>🚀 Falcon 계열</h2>`)로 바꾼 것.
> CLAUDE.md 의 *"보호 장치를 재는 테스트는 막지 않았으면 무엇이 일어났을 것인가를 먼저
> 단언한다"* 와 같은 갈래인데, 이번엔 **보호가 아니라 배선**에서 나왔다.
>
> ② **화면이 스스로와 어긋난 것은 렌더해 보고서야 나왔다.** 상세 패널은 `변형 10종` 이라고
> 말하는데 계열 화면의 변형별 막대는 **상위 6종에서 잘렸다**(다른 막대와 상한을 공유했다).
> 테스트는 전부 초록이었고, 실제 라이브 100건으로 렌더해 텍스트를 읽고서 보였다 —
> P12-6 의 *"캡에 걸리면 조용히 잘린다"* 와 같은 자리다. **변형별만 상한을 안 쓴다.**
>
> ⚠ **`variant`(72/100)는 채움률로 통과하고 조합에서 떨어졌다.** `name=Ariane 64` +
> `variant=64 Block 2`, `name=Nuri` + `variant=2` 라 이어 붙이면 말이 안 되고,
> `full_name` 이 이미 변형을 담고 있어 붙일 자리가 없다. 채움률·정보량·분포 쏠림·
> "말할 게 있는가"에 이어 **다섯 번째로 볼 것: 다른 필드와 조합이 되는가.**
>
> ⚠ **P12-22(밀린 릴리스 정리)가 이틀 만에 재발했다 — "끝났다고 보는 기준"이 틀렸다(2026-09-14).**
> 2026-09-12 에 태그 14개를 몰아 붙였는데, **태그 `v1.22.1` · 코드 `v1.32.0` 으로 10개가
> 다시 밀려 있었다.** 그동안 앱의 업데이트 확인(P12-12)은 **v1.22.1 을 "최신"이라고**
> 말하고 있었다. 정리를 일회성으로 본 것이 잘못이고, 원인(버전을 올릴 때 태그를 안 붙인다)은
> 그대로였다. 이번에 v1.23.0~v1.32.0 태그 10개와 v1.32.0 릴리스를 냈고,
> **재발을 막는 규칙은 `CLAUDE.md` 에 옮겼다** — 같은 정리를 세 번째로 하지 않기 위해서다.
>
> 보류는 그대로: **P12-8**(재진입 — 새 저장 구조) · **P12-17**(오프라인 타일 — 라이선스).
> 다음 후보로 남긴 것: **발사장 `total_landing_count`**(P15-3 에서 뺐다 — 착륙장 겸용
> 여부라 의미가 다르다).
>
> **Phase 16 후보는 아직 뽑지 않았다.** 뽑을 때는 P14-3·P15-2·P15-5·P15-4 의 순서를
> 그대로 따른다 — 채움률 → 정보량 → 분포 → 말할 게 있는가 → 조합이 되는가.

### 정기 점검 — 2026-09-14 (스냅샷 · 갱신하지 않는다)

직전 점검(2026-09-12) 이후 **21커밋 · 10개 버전**이 들어갔고 그게 릴리스로 나갔다.
그 열 개는 대부분 같은 자리(상세 패널·관점 화면)를 키웠는데 **실제로 켜 본 적이 없었다.**
전부 **네트워크 0 · 마우스 0** 으로 했다.

| 무엇을 | 어떻게 | 결과 |
|---|---|---|
| 화면 문장 | 실제 캐시 440건 + 위성 195건 + 관점 157종 = **792 렌더** 를 텍스트로 덤프해 의심 패턴·모순 검사 | **0건** |
| 잔해 | `TODO/FIXME` · 호출부 없는 JS 함수 · 안 쓰는 CSS | **0건** |
| 로그 | `rl3d.log` 280줄 | ERROR **0** · WARNING 3(전부 429 → 오래된 캐시, 설계대로) |
| 테스트 | 파싱 94 · 캐시 56 · 프론트 688 | 전부 통과 |
| 앱 | 히트맵·터미네이터·관측지·관심·위성 4그룹(792개)을 켠 `settings.json` 으로 기동 | 2번 모니터 (2880, 375) · 에러 0 |

**⚠ 여기서 하나가 나왔다 — 배선 18개가 통째로 안 덮여 있었다.**
`bindUI()` 의 배선을 하나씩 **실제로 지워** 보니, 18개를 지워도 **688건이 전부 통과**했다.
검색창 · 패널 닫기 · 탭 전환 · 타임라인 슬라이더 · 새로고침 · 아카이브 불러오기 ·
낮/밤 · 위성 토글 · 배경 전환 · 통계 열기가 **통째로 죽어도 테스트가 초록**이라는 뜻이다.
2026-09-12 에 실제로 죽어 있던 `tab-tonight` 도 그대로 목록에 있었다.

**당할 때마다 개별 테스트를 하나씩 더해 온 것이 문제였다**(2026-09-11 세 번 · 09-12 한 번).
그 방식은 *다음에 죽을 배선*을 못 막는다. 이제 **전수로 잰다**(27개 · `WIRING` 목록).
목록은 `boot.js` 에서 긁어 오지 않고 테스트에 손으로 적는다 — 긁어 오면 배선을 지울 때
기대 목록에서도 사라져 **공허하게 통과한다.** 변이 18/18 로 확인했다. 규칙은 `CLAUDE.md`.

> **점검 도구 자체도 변이로 쟀다.** 렌더 점검의 모순 검사가 처음엔 `신조 · (\d+)번째` 였는데,
> 태그를 걷어낸 실제 문장은 `신조 30번째 비행` 으로 **구분점이 없어** 아무것도 안 보고 있었다.
> 변이 3종(P15-3 의 버그 되살리기 · `undefined` 출력 · 빈 조각 구분점)으로 3/3 을 확인한 뒤에야
> "0건"이 뜻을 가졌다. **점검이 0건을 냈으면, 그 점검이 무엇이든 잡기는 하는지 먼저 본다.**
>
> **오탐을 되짚는 데 값을 했다.** 첫 판에서 `-0` 250건 · CSS 7건이 나왔는데 **전부 오탐**이었다
> (`T-0:45` 순서표 · `InnoSat-0` 위성 이름 · `m-${d.outcome}` 조합 클래스). P15 에서 적어 둔
> 그대로였고, 되짚지 않았으면 없는 결함 22건을 보고할 뻔했다.

**⚠ 붙었는지만 재는 것은 절반이었다 — 같은 날 2단으로 메웠다.**
2026-09-12 의 `tab-tonight` 은 **배선이 빠진 게 아니었다.** 핸들러는 붙어 있었고, 누르면
`Set` 에 `.includes()` 를 불러 `TypeError` 로 죽었다. 즉 위의 전수 테스트(27개 붙음)로도
**그 버그는 안 잡힌다.** 그래서 `PRESSES` 로 27개를 **실제로 눌러** 예외가 안 나는지까지 잰다
(단축키 8종 포함). 변이 4/4 로 확인했고, 그중 하나가 **09-12 의 그 버그를 되살린 것**이다.

- **누르기는 "막힌 안내로 빠지지 않는 상태"에서 해야 한다** — 관측지·관심 위성·satrec 을
  넣어 두지 않으면 핸들러가 안내만 내고 끝나 **죽은 경로를 안 탄다.** 09-12 에 기존 테스트가
  딱 그 상태였다.
- **예외가 안 나는 것과 일이 일어나는 것은 다르다** — 탭 전환은 `sidebarTab` 이 실제로
  바뀌는지까지 본다. 나머지 배선의 "무엇을 하는가"는 각 기능 테스트가 따로 잰다.

**Phase 16 후보로 남는 것**: 발사장 `total_landing_count`.

### Phase 16 — 착수 전 (2026-09-14 · 실측으로 뽑음 · **위성 축**)

발사(LL2) 쪽은 P15 에서 86개 필드를 훑어 소진됐다. 그래서 **축을 위성으로 바꿨다** —
P12-5(SATCAT)·P14-3(근지점) 두 번밖에 안 건드렸는데 그동안 발사 쪽만 열 개 버전이 들어갔다.

> **실측 방법 (2026-09-14 · 네트워크 0)**: `%APPDATA%\RL3D\cache\` 의 TLE 8그룹
> **2,929건**을 `satellite.js` 로 파싱해, satrec 이 주는 값 중 화면에 안 쓰는 것을 셌다.
> SATCAT 은 컬럼 목록을 `api_parsing._parse_satcat` 과 대조했다.
> **지금 위성 상세가 보여주는 것**: 고도·속도·현재 위치·궤도 주기·경사각·근지점~원지점·
> 이심률(TLE 계산) + 타입·소유·상태·발사일·발사장·국제식별번호·크기·재진입(SATCAT).

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **S16-1** | **위성 위치의 신선도** (v1.33.0) | 낮음 | **결함에 가깝다.** 발사 쪽은 `N분 전 갱신`(P7-7)이 툴바에 상시 뜨는데, `get_satellites` 는 **캐시 나이를 아예 반환하지 않는다**. 게다가 `sats.js` 가 `res.error && !res.stale` 이라 **오래된 캐시를 쓸 때 경고를 끈다** — 정확히 알려야 할 때 침묵한다. 실측: 캐시에 **49.8일 된 파일이 셋**(starlink 2,000건 포함). TLE 나이는 `rec.jdsatepoch` 로 **요청 0**에 구해지고 채움 **2,929/2,929** |
| ✅ **S16-2** | **재진입 예보** (v1.34.0 · P12-8 을 연다) | 중간 | P12-8 은 *"지난 실행에 있던 NORAD 가 사라졌는가"* 를 기억해야 해서 보류됐는데, **SGP4 로 30일 전파하면 저장 구조 없이** 신호가 나온다. 실측: 30일 뒤 전파가 실패하는 물체 **30건**(근지점 17~206km) · 장반경 30일 감소 중앙 **1.59km** · 50km 넘게 줄어드는 것 **47건**. ⚠ 낮은 근지점이 거의 전부 starlink(49.8일 된 파일)이라 **S16-1 뒤에 최신 TLE 로 다시 재야 한다** |
| **S16-3** | **발사장 `total_landing_count`** | 낮음 | **착수하며 다시 재니 `pad.location.total_landing_count` 는 100/100 채워지는데 0 이 아닌 값이 5종**(25개 발사장 중 Vandenberg 35 · Cape Canaveral 79 · Rocket Lab LC-1 17 · Jiuquan 3, 나머지 0). 한 줄 때문에 `CACHE_SCHEMA` 를 올려 **아카이브를 통째로 다시 받을** 값은 아니다 → **다음 파싱 변경과 함께** 넣는다 |

**기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| SATCAT 나머지 컬럼 | 17개 중 10개를 파싱한다. 안 보는 7개는 **P14-3 에서 이미 훑어 기각**했고(`PERIOD`·`INCLINATION` 중복 · `APOGEE`·`PERIGEE` 는 TLE 가 낫다 · `ORBIT_TYPE`·`ORBIT_CENTER` 정보량 0), 남은 `DATA_STATUS_CODE` 하나뿐이다. **이 축은 소진됐다** |
| RAAN · 근지점 인수 | 채움 **2,929/2,929** · 서로 다른 값 2,892 로 채움률·정보량은 만점인데 **"말할 게 있는가"에서 떨어진다** — 궤도면 각도는 이 화면에서 문장이 안 된다. P15 의 `*_url` 과 같은 갈래 |
| 통산 궤도 수(`revnum`) | satellite.js 가 노출하지 않는다(**0/2,929**). TLE 문자열에서 직접 뽑을 수는 있으나 쓸모가 RAAN 과 같은 급이다 |

> **실측에서 얼마나 어긋나는지까지 쟀다 (요청 0).** 같은 NORAD 가 낡은 파일과 새 파일에
> 둘 다 있는 **4건**을 찾아, 두 TLE 로 *같은 시각*의 위치를 계산해 거리를 냈다:
> **중앙값 1,250km · 최대 2,880km**(HXMT 2,880 · HST 1,250 · TERRA 198 · SDO 73).
> **저궤도일수록 크다** — 물리와 맞는 방향이다. 표본이 4건뿐인 것은 겹치는 위성이 그뿐이라서다.
>
> **S16-2 로 P12-8 이 열렸다 — 보류 근거가 틀린 게 아니라 길이 하나 더 있었다(2026-09-14).**
> P12-8 은 *"지난 실행에 있던 NORAD 가 사라졌는가를 기억해야 한다 → 새 저장 구조가 필요
> 하다"* 고 접었다. 그 판단 자체는 지금도 맞다 — **다만 그게 유일한 길이 아니었다.**
> SGP4 는 대기권에 들어가면 값을 못 내므로, 그 첫 시점을 이진 탐색하면 저장 없이 예보가 난다.
> 실측 **190/2,926(6.5%)** · 30일 안 20건 · 전체 계산 476ms.
> **"비싸다"고 접은 항목은 방법을 바꿔 다시 재 볼 값이 있다** — 보류 근거는 대개 *그 방법이*
> 비싸다는 것이지 *그 기능이* 불가능하다는 게 아니다.
>
> ⚠ **테스트가 `rec.error` 의 성질을 잡아냈다.** 가드로 `rec.error` 를 썼는데, 그건 TLE 의
> 성질이 아니라 **직전 전파가 남긴 찌꺼기**다 — 재진입 너머로 한 번 전파하면 `6` 이 박혀
> **두 번째 호출부터 null** 이 됐다. `fc(body)` 를 **두 번 부르는 단언**이 아니었으면 영영
> 안 보였을 것이다. 실시간 위치는 멀쩡해서(매 호출이 `error` 를 다시 쓴다) 화면으로도
> 안 드러난다. **라이브러리가 넘겨주는 객체의 필드가 "상태"인지 "성질"인지 확인한다.**
>
> ⚠ **테스트 예제를 잘못 골라 한 번 틀렸다.** 로켓 몸체(CZ-2C R/B)를 "30일 밖" 예제로 잡았는데
> 실측 **13.4일**이라 안쪽이었다. 코드가 아니라 **기대가 틀린 것**이었고, 86일짜리(SWIFT)로
> 바꿔 고쳤다. 경계를 재는 예제는 **그 경계 밖에 있는지 값을 찍어 보고** 고른다.

> **S16-1 에서 임계가 한 번 틀렸다 — 렌더해 보고 알았다(2026-09-14).**
> 처음엔 낡음 임계를 **3일 하나**로 뒀는데, 실제 캐시로 렌더하니 `gps-ops` **32개가 전부**
> 걸렸다(중앙 3.84일). MEO 는 대기가 없어 궤도가 안정적이라 나흘 된 TLE 도 정확하다 —
> **거짓 경보**였다. 위에서 이미 잰 값이 그 답을 갖고 있었다: 50일 어긋남이 저궤도
> **2,880km** 대 고궤도 **73km** 로 20~40배다. 그래서 **저궤도 3일 · 그 위 30일**로 갈랐고,
> 대역 판정은 P11-3 의 `orbitBand()` 를 그대로 쓴다. 갈랐더니 거짓 경보 41건이 사라졌다
> (gps-ops 32 · geo 8 · weather 1). **임계를 하나로 두기 전에 "이 값이 대상마다 다르게
> 변하는가"를 묻는다** — 실측 표에 이미 답이 있었는데 한 숫자로 뭉갤 뻔했다.
>
> **`CACHE_SCHEMA` 를 안 올렸다.** 에포크는 TLE 문자열에 들어 있어 프론트가 바로 구한다 —
> 파싱이 안 바뀌니 아카이브를 다시 받을 이유가 없다. S16-3 을 굳이 끼워 넣지 않은 이유이기도 하다.
>
> ⚠ **감쇠 측정을 두 번 틀렸다 — 기록해 둔다.**
> ① `ndot` 단위를 잘못 잡아 **92,000,000 km/일** 같은 불가능한 값이 나왔다. 물리적으로
>    말이 안 되는 수치는 **후보가 아니라 측정 오류의 신호**다.
> ② 고쳐서 SGP4 로 전파했더니 이번엔 **순간 고도**로 재서, 타원·경사 궤도에서 감쇠가 아니라
>    **궤도상 어디에 있는지**를 쟀다(GEO 위성이 "가장 빨리 내려가는" 목록 1위로 올라왔다).
>    셋째 판에서 **장반경**(vis-viva)으로 바꾸고서야 물리와 맞는 값이 나왔다.
>    **"그럴듯한 숫자"와 "맞는 숫자"는 다르다 — 순위표 상단이 말이 되는지 먼저 본다.**
> ③ 2판은 **옛 캐시 형식 파일 셋을 조용히 빠뜨려** 2,929건 중 816건만 봤다(봉투 없는 구조).
>    건수가 예상과 다르면 그 자리에서 멈춘다.

### Phase 17 — 착수 전 (2026-09-14 · 실측으로 뽑음 · **앱 자체**)

발사(LL2)는 P15 에서, 위성(TLE·SATCAT)은 S16 에서 소진됐다. **데이터가 아니라 앱 자체를 쟀다** —
구조·설정·단축키·툴바 폭·규모별 동작·시작 비용·캐시 크기. 전부 **네트워크 0**(타일 측정만 예외).

> **먼저 확인된 것 (후보가 아니다)**
> - **잔해 0건** · `TODO/FIXME` 0 · 설정에 *저장만 하고 복원 안 하는 키* 0건
> - **구조는 문제없다**: 가장 큰 파일이 `api_client.py` **540줄**이고 리포 최대 JS 는 483줄이다.
>   P12-23·P14-4 로 쪼갠 뒤 다시 커진 파일이 없다 — **분리 근거가 지금은 없다**
> - **성능도 문제없다**: 실측 위성 2,926건 `satDetails` **83ms** · 사이드바 렌더 4ms ·
>   통계 패널 3ms · 재진입 예보 전량 476ms. P11-6 때와 같은 결론이다
> - ⚠ **"위성 목록이 조용히 잘린다"는 오탐이었다.** 2,926건 중 400건만 그리길래 P12-6 과
>   같은 결함인 줄 알았는데, 확인하니 `…외 2,526개. 검색으로 좁혀보세요.` 가 **있다.**
>   위성 탭을 렌더한 뒤 발사 탭으로 덮어 놓고 발사 쪽 HTML 을 검사한 내 실수였다

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P17-1** | **툴바 오버플로 정리** (v1.35.0) | 낮음 | **"나중에" 표에 적어 둔 도입 조건이 이미 충족됐다.** 조건은 *"기본 창(1280px)에서 두 줄로 밀릴 때"* 인데, **오늘 찍은 캡처 두 장 모두 두 줄**이다(`갱신 시각` 이 아래 줄로 내려가 있다). P13-5 에서 검색창을 240→200px 로 줄여 한 줄로 되돌렸다고 `style.css` 주석에 적혀 있는데 그 뒤 다시 밀렸다. 툴바 항목은 지금 **버튼 6 · 체크박스 7 · 검색창 1 · 라벨 1** |
| ✅ **P17-2** | **오프라인 배경** (v1.36.0 · 원안은 기각) | 중간 | **보류 근거(exe 크기와의 교환)가 실측에 뒤집혔다.** 줌 0~4(341장)는 **1.35MB** 다 — 현재 exe 14.0MB 의 **10%**. 표본 편향도 확인했다(육지 6.3KB · 바다 3.2KB · 육지 29% → 가중 4.1KB, 무작위 표본 4.0KB 와 일치). 전부 육지라 쳐도 2.1MB. **줌 0~5 는 4.2MB(+30%) · 0~6 은 13.7MB 로 exe 가 두 배**가 되니 거기서 끊긴다. → **남은 장애물은 크기가 아니라 라이선스다**(Esri 타일 재배포 조건). 착수 전에 그것만 확인하면 된다 |
| ✅ **P17-3** | **단축키 4개** (v1.35.0) | 낮음 | 지금 `Esc · / · ? · s · r · t · 1~7` 13개가 있다. 안 덮인 툴바 기능은 **넷**: 통계(`stats-btn`) · 배경 전환(`basemap-btn`) · 위성 그룹(`sat-groups-btn`) · 아카이브 불러오기(`arch-load`). P17-1 과 붙는다 — 버튼을 접으면 단축키가 대신 길이 된다 |
| ✅ **P17-4** | **첫 실행 화면이 얇다** (v1.37.0) | 중간 | 설정 파일이 없는 첫 실행은 **위성 OFF · 히트맵 OFF · 아카이브 미로드**다. 그래서 처음 여는 사람이 보는 것은 **2026년 99건**(예정 49 · 지난 50)뿐이고, 통계·히트맵·타임라인이 전부 "데이터가 모자란" 상태로 열린다. 요청을 아끼려는 의도적 설계지만 **그 사실을 화면이 말하지 않는다** — 무엇을 눌러야 채워지는지는 통계 패널 안내에만 있다 |

> **P17-1·P17-3 을 함께 넣었다 (2026-09-16 · v1.35.0).** 접은 것은 **둘**이다 — 설계에서는
> 셋(시간대·배경·통계)이었는데, `utils.js` 에 *"툴바 버튼이 현재 시간대를 상시 보여주는 것이
> 이 기능의 핵심"* 이라고 적혀 있었다. **접는 것과 상시 표시는 양립하지 않는다** — 코드에 남은
> 의도를 읽고 설계를 고쳤다. 둘만 접어도 한 줄이 됐다(캡처 실측: 오른쪽 끝 x≈1210 / 1280).
>
> ⚠ **테스트 770건이 전부 초록인데 화면은 깨져 있었다.** 팝오버의 버튼 글자가 **세로로 한
> 글자씩** 쌓였다(absolute 박스의 기준이 36px 짜리 래퍼라 거기 맞춰 줄어든다 → `width: max-content`).
> 배선·판정·Esc 우선순위는 전부 맞았고 **캡처가 아니었으면 안 보였다** — "테스트가 다 초록이어도
> 화면을 한 번 띄워 본다"(CLAUDE.md)가 또 맞았다. P12-16 의 화면 밖 배지와 같은 갈래다.
>
> **팝오버 위치를 JS 로 계산하려다 되돌렸다.** `getBoundingClientRect()` 로 버튼 좌표를 읽는
> 판을 먼저 썼는데, 스텁 DOM 에는 그 함수가 없어 **테스트가 못 재는 코드**가 된다. 버튼과 같은
> 래퍼에 넣어 CSS 가 잡게 하니 코드가 줄고 검사 가능한 자리로 돌아왔다 —
> **"테스트가 이걸 잴 수 있나"는 설계를 고르는 기준이 된다.**
>
> **남은 P17 은 P17-2(라이선스 확인 먼저) · P17-4 다.** P17-1 이 P17-4 를 조금 건드린다 —
> `📊 통계`가 한 단계 뒤로 들어갔으므로, 첫 실행 안내(P17-4)는 **무엇을 눌러야 채워지는지**를
> 말할 때 `⋯` 안쪽까지 가리켜야 한다.

> **P17-2 는 원안이 기각되고 다른 길로 들어갔다 (2026-09-16 · v1.36.0).**
> *"남은 장애물은 크기가 아니라 라이선스"* 라고 좁혀 뒀던 그 라이선스가 **닫혀 있었다**:
> Esri 문서가 *"Systematically requesting ArcGIS tiles for offline use through other apps
> or services is prohibited"* 라고 명시하고, 마스터 계약도 승인된 경로 밖에서는
> *"may not otherwise scrape, download, or store Data"* 다. **크기를 재기 전에 라이선스를
> 먼저 쟀어야 했다** — 1.35MB 라는 숫자는 맞았지만 쓸 수 없는 숫자였다.
>
> 대신 **퍼블릭 도메인 벡터**(Natural Earth 육지 폴리곤)를 번들했다. 실측 비교:
> **110m 100KB · 50m 1.18MB**(좌표 3자리·속성 제거 후). 원안(1.35MB)과 같은 급인데
> **줌 상한이 없고** 라이선스가 깨끗하다. 오프라인 캡처 A/B 로 **50m** 를 골랐다 —
> 110m 는 확대 시 필리핀 군도가 덩어리 하나로 뭉개졌다.
>
> ⚠ **첫 판은 "동작하는데 안 보였다".** 육지 채움만 깔았더니 바다와 구분되지 않아
> 오프라인 화면이 여전히 빈 것처럼 보였다. 같은 소스로 해안선 선을 얹어(추가 용량 0)
> 해결했다. **레이어가 있다는 것과 읽힌다는 것은 다르다 — 캡처로만 갈린다.**
>
> ⚠ **변이 실험이 테스트의 결함을 드러냈다.** 레이어 순서를 뒤집는 변이를 넣었더니
> 단언이 실패한 게 아니라 **테스트가 예외로 죽어 뒤 항목이 통째로 안 돌았다**
> (`st.layers[3].layout` 을 인덱스로 집었다). id 로 찾게 고쳤다 —
> **단언은 틀렸을 때 '실패'해야지 '중단'하면 안 된다.**

> **P17-4 로 Phase 17 이 끝났다 (2026-09-16 · v1.37.0).** 고친 것은 기본값이 아니라
> **기본값을 말하지 않는 것**이었다 — 요청을 아끼려 꺼 두는 설계는 그대로 두고, 그 상태를
> 숫자로 말하고 켜는 길을 같이 줬다. P17-1 에서 `📊 통계`를 `⋯` 안으로 접었기 때문에
> **통계 패널 안쪽의 모수 안내에만 기대는 구조는 더 나빠질 참**이었다(그때 적어 둔 연결이다).
>
> ⚠ **실제 첫 실행을 재현하다 한 번 헛돌았다.** 설정 파일을 치우고 띄웠는데 캡처가
> **로드 전**에 찍혀 "데이터를 불러오는 중…"만 나왔다. 안내는 설계대로 데이터가 들어온
> 뒤에 뜨므로, **캡처 시점이 곧 테스트 조건**이었다.
>
> ⚠ **한 번은 카드가 사라지고 2025년 아카이브가 로드된 상태로 찍혔다.** 코드에 자동
> 클릭 경로는 없고(`fr-arch` 는 click 리스너뿐), 설정에는 `firstRunSeen: true` 만 남아
> 있었다 — 창이 사용자 화면(2번 모니터)에 떠 있었으므로 **사람 조작으로 보인다.**
> 확정하지 않고 적어 둔다: 마우스 자동화를 쓰지 않는 이 프로젝트에서 **떠 있는 창은
> 공유 자원**이라, 무인 캡처는 그 사이 조작을 배제하지 못한다.

> **P12-17 을 두 번 접는 동안 아무도 크기를 안 쟀다.** 계획서에 *"착수 전에 측정한다 —
> 타일 번들은 'exe 가 몇 MB 늘어나는가'"* 라고 **적어 놓고도** 그 측정 없이 두 번 보류했다.
> 오늘 20여 장을 받아 재니 저줌은 사실상 공짜다. **보류 근거에 숫자가 없으면 그 보류는
> 재검토 대상이다** — S16-2 가 P12-8 을 연 것과 같은 갈래다(그쪽은 방법이, 이쪽은 비용이 틀렸다).

### Phase 18 — 착수 전 (2026-09-16 · 실측으로 뽑음 · **실패·빈 상태 축**)

발사(LL2)는 P15 에서, 위성(TLE·SATCAT)은 S16 에서, 앱 구조·성능은 P17 에서 소진됐다.
남은 축은 **한 번도 체계적으로 재지 않은 것** — *잘 되는 화면*이 아니라 **안 될 때의 화면**이다.

> **실측 방법 (2026-09-16 · 네트워크 0)**: `tests/harness.js` 로 부트 경로를 세우고
> `get_launches`·`get_satellites`·`get_archive` 에 **실패를 주입해** 각 화면이 무엇을
> 말하는지 덤프했다(스크래치패드의 일회용 프로브). 캐시·로그는 `%APPDATA%\RL3D\` 를 직접 읽었다.

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P18-1** | **발사 탭 빈 상태가 통째로 빈칸** (v1.38.0) | 낮음 | 사이드바 네 탭 중 **발사 탭만** 빈 상태 문구가 없다. 위성 탭은 *"위성 레이어가 꺼져 있습니다…"* · 관심 탭은 *"관심 항목이 없습니다…"* · 오늘 밤 탭은 *"눈에 보이는 통과가 없습니다…"* 를 내는데, `renderSidebar` 는 빈 배열이면 `"".join` 으로 **빈 칸**을 남긴다(`sidebar.js:108-128`). 게다가 원인이 둘이라 **말해야 할 내용이 다르다**: ① 데이터가 0건(오프라인·첫 조회 실패) ② 필터·검색·타임라인으로 다 걸러짐 → 후자는 *"필터를 확인하세요"* 여야 한다. 실측: 검색어 `존재하지않는로켓이름` → 목록 빈칸 · `0건` |
| ✅ **P18-2** | **갱신 시각이 분 고정** (v1.38.0) | 낮음 | 실측 `🕒 1800분 전 갱신`(30시간 된 캐시 + 429 주입). `settings.js:8-9` 가 분만 쓴다. **같은 리포에 단계를 가진 함수가 이미 둘 있다** — 티커의 `agoText`(분→시간→일→년, `launches.js:412-416`)와 TLE 신선도의 `tleAgeText`(시간→일, `sats.js:149-151`). 평소엔 TTL 15분이라 안 드러나고 **오프라인·429 일 때만** 나타나서 지금껏 안 보였다 |
| ✅ **P18-3** | **위성 조회가 실패해도 목록은 "불러오는 중…"** (v1.38.0) | 낮음 | 403 을 주입하니 상태줄은 *"⚠ 위성: 서버가 접근을 거부했습니다(403)."* 로 맞게 말하는데, **사이드바 위성 탭은 `위성을 불러오는 중…` 에 영구히 머문다**(`sidebar.js:69-77` 은 *레이어가 켜졌는가*만 보고 **실패했는가**는 모른다). 툴바의 `sat-count` 도 빈칸으로 남아 `위성 (20)` 자리가 사라진다. 화면 둘이 **서로 다른 말**을 하고 있다 |
| ✅ **P18-4** | **0건 통계가 숫자 0 으로 그려진다** (v1.38.1) | 낮음 | 발사 0건에서 통계를 열면 *"현재 불러온 0건 기준 / 0 총 발사 — 성공률 확정 0건 중 0 / 성공 0 실패 0 부분 0 예정 0"* 이 나온다. 예외는 안 나지만 **"데이터가 없다"고 말하지 않는다**. 다만 0건 자체가 드문 상태(오프라인 첫 실행)라 **P18-1~3 뒤** |

**확인만 하고 기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| 로그 위생 | `rl3d.log` **340줄 중 WARNING 3 · ERROR 0 · 크래시 0**. 경고 셋은 전부 2026-09-13 의 429(`발사 조회 실패(HTTP Error 429…) — 오래된 캐시 사용`)로, **폴백이 설계대로 돌았다는 기록**이다. 손댈 것이 없다 |
| 캐시 비대 | 파일 **15개 · 2.5MB**. 가장 큰 것이 `archive_2025.json` 1.4MB 로, 영구 캐시 설계 그대로다 |
| 에러 문구 표 | `api_errors.HTTP_ERROR_MESSAGES` 가 403·404·429·500·502·503·504 + 타임아웃 + `URLError` + 기타를 덮는다. **세 소스(LL2·Celestrak·GitHub)에서 실제로 나오는 코드는 전부 들어 있다** |
| 아카이브 실패 경로 | 실패 시 안내가 뜨고 `arch-load` 버튼도 `disabled=false` 로 되돌아온다. 정상 |
| 옛 형식 캐시 청소 | `tle_gps-ops.json` 만 **봉투 없는 옛 형식**으로 5.3일째 남아 있다(그 그룹을 안 켜 갱신 기회가 없었다). 항목 키가 현재와 **똑같아**(`name·norad_id·tle1·tle2`) 폴백으로 읽혀도 안전하고 크기도 6KB — `LEGACY_CACHE_FILES` 를 늘릴 값이 아니다 |

> **왜 이 축이 남아 있었나.** 지금까지의 실측은 전부 *"데이터에 무엇이 더 있나"*(P15·S16)
> 와 *"앱이 어떻게 생겼나"*(P17)였다. 둘 다 **잘 되는 상태**를 잰 것이다. 이번엔 실패를
> 주입해 봤더니 **네 곳에서 화면이 침묵하거나 서로 다른 말을 했다.** 셋이 "말하지 않는다"
> 계열이고 하나(P18-3)는 "두 화면이 어긋난다"다.
>
> ⚠ **프로브를 한 번 잘못 짰다.** 하네스의 `.flt:checked` 셀렉터를 안 채워 필터가 전부 꺼진
> 상태가 됐고, "정상 상태인데 발사 탭이 비었다"는 **오해할 결과**가 먼저 나왔다. 또 `freshness`
> 를 재려고 `state.lastLaunchLoad` 를 넣었는데 그 키가 `STATE_KEYS` 에 없어 **전부 빈칸**이
> 나왔다(`1800분` 은 실제 주입 경로에서 나온 값이라 그쪽이 근거다).
> **측정 도구가 틀리면 후보가 통째로 가짜가 된다 — 숫자가 이상하면 도구부터 의심한다.**

> **P18-1~3 을 한 번에 넣었다 (2026-09-16 · v1.38.0).** 셋을 묶은 이유는 난이도가 아니라
> **성격이 하나**여서다 — *실패·빈 상태를 화면이 말하게 한다*. 따로 내면 CHANGELOG 세 줄이
> 같은 얘기를 세 번 하게 된다. P18-4(0건 통계)는 성격이 달라 남겼다.
>
> **표기를 새로 만들지 않은 것이 이번의 핵심 판단이다.** P18-2 에서 단계 표기가 필요했는데
> 리포에 이미 `agoText` 와 `tleAgeText` 둘이 있었다 — 세 번째를 만들면 *"1시간 전"* 의 기준이
> 세 군데로 갈린다. 그대로 불러 썼고, 테스트도 *"티커와 같은 계단을 쓴다"* 로 그 사실을 잰다.
>
> ⚠ **고친 뒤 프로브를 다시 돌려 한 번 더 걸렀다.** 위성 실패 문구가 한 상자 안에서
> **두 번** 나왔다(*"받지 못했습니다 / 불러오지 못했습니다"*). 단언은 전부 통과하는 종류의
> 결함이라 — 각각은 맞는 문장이다 — 실패 화면을 **문장으로 읽어 보고서야** 보였다.
> 2026-09-13 의 부스터 줄(*"신조 · 2번째 비행"*)과 같은 갈래다.

> **P18-4 로 Phase 18 이 끝났다 (2026-09-16 · v1.38.1).** 0건은 곧 *"아직 못 받았다"* 여서
> — 통계는 필터가 아니라 `allLaunches` 를 센다 — P18-1 과 달리 **원인을 나눌 필요가 없었다.**
> 같은 "빈 상태"라도 **무엇이 그 상태를 만들 수 있는가**에 따라 할 말의 수가 달라진다.
>
> **Phase 18 결산**: 네 후보 전부 소진. 고친 것은 기능이 아니라 **말**이다 — 침묵하던 세 곳과
> 두 화면이 어긋나던 한 곳. 다음 축을 찾을 때 참고할 것: *잘 되는 화면*은 세 Phase 에 걸쳐
> 훑었고(P15·S16·P17), *안 될 때의 화면*은 이번에 훑었다.

### Phase 19 — 착수 전 (2026-09-16 · 실측으로 뽑음 · **오래 켜 두는 동안의 화면**)

P15·S16(데이터) · P17(앱 구조) · P18(안 될 때의 화면)이 소진됐다. 남은 축은 **시간**이다 —
지금까지 잰 것은 전부 *막 띄운 앱*이었는데, 이 앱은 발사를 기다리며 **켜 두는** 앱이다.

> **실측 방법 (2026-09-16 · 네트워크 0)**: `setInterval` 등록/해제를 전수로 짝맞추고,
> 하네스에서 **자동 갱신을 직접 돌려** 무엇을 다시 받고 무엇을 안 받는지 호출을 셌다.
> TLE 에포크 나이는 `%APPDATA%\RL3D\cache\` 의 2,925건을 직접 파싱했다.

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P19-1** | **자동 갱신이 위성을 다시 받지 않는다** (v1.40.0) | 중간 | 자동 갱신 1회를 돌려 호출을 세니 **`launches` 하나뿐**이다. 위성은 부트·그룹 변경·**강제 갱신(↻)** 때만 온다(강제 갱신은 `launches(force)·satellites·satcat` 셋을 부른다 — 경로는 이미 있다). **TLE 캐시 TTL 2시간이 사실상 死문자**다: 앱이 요청을 안 하면 TTL 이 할 일이 없다. 켜 둔 시간만큼 에포크가 늙고, 지도의 점은 **조용히** 어긋난다(S16-1 실측: 50일 차 TLE 은 저궤도에서 2,880km). S16-1 의 신선도 배지는 **경고만 하고 스스로 고치지 않는다.** 현 캐시 2,925건 기준 에포크 나이 중앙 **2.5일** · 3일 초과 **15.6%** — 여기서 켜 둔 시간이 그대로 더해진다. 요청 예산도 여유다: 발사 폴링 5분 주기의 실사용은 **4회/시간**(캐시 TTL 15분이 제어), TLE 을 TTL 대로 받으면 **+0.5회/시간** |
| ✅ **P19-2** | **열어 둔 통과 목록이 다시 계산되지 않는다** (v1.40.0 · 접근 수정) | 낮음 | `renderTonightList` 는 **탭 전환·관측지 변경·가시 전용 토글·시간대 전환** 때만 불린다. 자동 갱신을 돌린 뒤 HTML 을 비교하니 **글자 하나 안 바뀌었다**. 몇 시간 켜 두면 *이미 지나간 통과*가 목록에 남고, 화면으로는 "곧 지나갈 통과"와 구분되지 않는다. 통과 패널(`showPasses`)도 발사 갱신과 무관하게 정지해 있다 |
| ✅ **P19-3** | **업데이트 확인이 부트 때 한 번** (v1.40.0) | 낮음 | `initUpdateCheck()` 는 `pywebviewready` 에서 한 번 불리고 끝이다. 며칠 켜 두면 그동안 나온 릴리스를 모른다 — 오늘만 해도 다섯 번 올렸다. **캐시 TTL 이 하루라 주기 확인을 붙여도 요청은 늘지 않는다**(캐시가 먹는다) |

**확인만 하고 기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| 타이머 누수 | `setInterval` 7곳 중 정리 코드가 없는 둘(`updateFreshness` 30초 · `terminatorTimer` 60초)은 **부트에서 각각 한 번만** 등록된다. 나머지 다섯은 전부 `clearInterval` 짝이 있다 |
| 아카이브 목록 누적 | `archiveLaunches.push` 앞에 기존 id `Set` 이 있어 다시 불러도 중복되지 않는다 |
| 아카이브 연도 목록 | `populateArchiveYears` 도 부트 때 한 번이라 **연말을 넘겨 켜 두면 새해가 목록에 없다**. 다만 연 1회짜리 경계고, 그 시점엔 새해 발사도 없다 → 값이 안 된다 |

> ⚠ **측정 도구가 또 한 번 거짓말했다.** 업데이트 확인이 주기적인지 보려고 `boot.js` 에
> `/setInterval\([^)]*[Uu]pdate/` 를 걸었는데 **`setInterval(updateFreshness, …)` 에 걸려**
> *"주기적"* 이라고 나왔다. 실제로는 부트 때 한 번이다(P19-3 은 그래서 후보다).
> P18 때도 하네스 셀렉터를 안 채워 가짜 후보가 나왔다 — **두 세션 연속 같은 자리에서 당했다.
> 정규식·스텁으로 잰 값은 반드시 원본 코드에서 눈으로 확인한다.**

> **설계를 검토하다 더 큰 것이 나왔다 — P19-0 (2026-09-16 · v1.39.0).**
> P19-1~3 설계를 내고 *"그게 최선인가"* 를 다시 물었더니, **요청 수(+0.5회/시간)만 쟀고
> CPU 를 안 쟀다**는 것이 드러났다. 재 보니:
>
> | 재려던 것 | 실측 | 판정 |
> |---|---|---|
> | 위성 전량 재로드(P19-1) | **194ms** | 5분 주기에 합류시켜도 무해 |
> | 위치 계산 1틱(지금도 매초) | 74ms | 기준선 |
> | **오늘 밤 통과 계산(P19-2로 주기화하려던 것)** | **115,442ms** | **주기화 불가** |
>
> 그 자리에서 **P19-2 의 접근이 뒤집혔다**: 주기 재계산은 5분마다 앱을 2분씩 얼리므로
> 불가하고, 지나간 통과는 **계산 없이 시각 비교만으로** 걸러낼 수 있다(공짜).
>
> 그리고 **더 큰 것**: 그 115초는 *지금도* 탭을 누르면 도는 계산이다. 기본 그룹
> (`stations`+`visual` **176건**)으로도 **9초** — 큰 그룹을 켜야만 나는 문제가 아니었다.
> 도입(v1.7.0) 이래 줄곧 그랬고, **예외가 안 나고 멈출 뿐이라** 테스트도 P17 의 성능 측정도
> 못 잡았다(P17 은 `satDetails`·사이드바·통계를 쟀지 이 탭을 안 쟀다).
>
> **교훈 둘.**
> ① **"요청을 몇 번 더 쓰나"만 재고 "얼마나 걸리나"를 안 쟀다.** 이 앱의 예산은 둘이다 —
>    API 한도와 **UI 스레드**. 후자는 이번에 처음 예산으로 취급했다.
> ② **가장 비싼 계산이 가장 쓸모없는 결과를 낼 수 있다.** GEO 는 비용 2배·가치 0 이었다.
>    "무엇을 빼도 되나"를 먼저 물었으면 최적화 전에 절반이 사라졌다.
>
> ⚠ **P18 에서 고친 실수를 그대로 반복했다.** 새 테스트에서 큐를 `queue.shift()()` 로
> 방어 없이 집어, 변이 둘이 **단언 실패가 아니라 TypeError 로 테스트를 중단**시켰다.
> 같은 세션에서 같은 교훈을 두 번 적는 셈이다 — *단언은 틀렸을 때 실패해야지 중단하면 안 된다.*
> 조각 크기(25→10)를 바꾸자 테스트가 깨진 것도 같은 갈래였다(**상수에 기댄 단언**).

> **Phase 19 끝 (2026-09-16 · v1.39.0 + v1.40.0).** P19-0(오늘 밤 탭)이 설계 검토에서
> 튀어나와 먼저 나갔고, 나머지 셋을 v1.40.0 으로 묶었다.
>
> **새 규칙이 이번에 실제로 작동했다.** `CLAUDE.md` 에 넣은 *"설계를 내기 전에 «이게
> 최선인가»를 내가 먼저 묻는다"* 를 P19-1~3 에 적용하니:
> - ① **예산이 몇 개인가** → 안 잰 축(**브릿지 전송량**)이 나와 쟀다(0.58MB·5ms).
> - ② **무엇을 빼도 되나** → 넣으려던 최적화(`age` 비교로 재파싱 스킵)가 **불필요**로
>   판명됐다. 총 200ms/5분이라 이미 싸다 — **재지 않았으면 안 해도 될 일을 설계에 넣을 뻔했다.**
> - ④ **테스트가 잴 수 있나** → `tickTonightFreshness()` 가 `Date.now()` 를 직접 부르는
>   구조라 "지난 통과"를 만들 수 없었다. **함수가 시각을 주입받도록 설계를 고쳤다.**
>
> ⚠ **변이 실험이 배선 구멍을 또 잡았다.** 순수 함수(`dropPastPasses`)와 정리 함수
> (`tickTonightFreshness`)를 각각 재는 단언은 전부 통과하는데, **주기에 연결하는 한 줄**을
> 지워도 초록이었다. 주기 콜백을 직접 눌러 보는 단언을 더해서야 잡혔다 —
> 2026-09-11 에 하루 세 번 당한 것과 **정확히 같은 구멍**이다.

### Phase 20 — 착수 전 (2026-09-17 · 실측으로 뽑음 · **조작하는 동안의 화면**)

P15·S16(데이터) · P17(앱 구조) · P18(안 될 때의 화면) · P19(켜 두는 동안)이 소진됐다.
남은 축은 **조작**이다 — 지금까지 잰 것은 전부 *가만히 있는 앱*이었는데,
이 앱에서 사람이 실제로 하는 일은 **검색창에 글자를 치고 슬라이더를 끄는 것**이다.

P19 가 처음으로 **UI 스레드를 예산으로** 취급했다. 그 예산을 조작 경로에 대 보니
P19-0(오늘 밤 탭)과 **같은 갈래가 하나 더** 있었다 — 도입 이래 줄곧, 예외 없이.

> **실측 방법 (2026-09-17 · 네트워크 0)**: 조작 지점을 `index.html` 에서 전수로 뽑아
> JS 참조·핸들러와 대조하고, `tests/harness.js` 의 `loadApp()` 에 **실제 캐시**
> (`launches.json` 98건 · `archive_2025.json` 341건)를 먹여 각 경로를 직접 호출해 시간을 쟀다.
> 아카이브 연도 수는 `populateArchiveYears` 의 실제 선택지(**최근 5년**)까지 늘렸다.

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P20-1** | **키 한 타마다 발사 전량을 다시 그렸다** (v1.41.0) | 낮음 | `search` 의 `input` 이 `applyFilters` 에 **직결**돼 있고(디바운스는 앱 전체에 **0곳**), 그 안의 `renderSidebar` 가 필터 결과 전부를 매번 조립한다. 아카이브를 불러올수록 선형으로 늘어난다 — **98건 2.5ms → 5년(1,803건) 89.3ms**. `"falcon"` 여섯 타면 **0.54초**, 타임라인 슬라이더는 `input` 이 드래그 내내 터져 30틱에 **2.68초**가 UI 스레드에 박힌다. **원인의 77%가 한 줄이다**: `fmtDate` 가 행마다 `d.toLocaleString("ko-KR", {…})` 을 불러 **포매터를 매번 새로 만든다**. 2,144행 기준 쪼갠 값 — 날짜 포맷 **82.5ms** · `escapeHtml` 17.2ms · 정렬 3.4ms · 필터링 0.1ms · `launchesToFC` 3.0ms. **포매터를 한 번 만들어 재사용하면 87.3ms → 2.0ms(44배)**. P19-0 과 같은 갈래다 — 예외가 안 나고 멈추기만 해서 테스트도 P17 의 성능 측정도 못 잡았다 |
| ✅ **P20-2** | **목록 캡이 두 탭에서 정반대였다** (v1.42.0) | 낮음 | 위성 목록은 `slice(0, 400)` 에 *"…외 N개"* 안내까지 있는데 **발사 목록에는 캡이 없었다.** 같은 사이드바의 두 탭이 반대 방침이었고 어느 쪽이 맞는지 적힌 곳이 없었다. 위성 쪽 방침으로 맞추고 캡 상수를 `SIDEBAR_CAP` 한 곳으로 모았다. **정렬한 뒤에 자른다** — 앞에서 자르면 예정 발사가 통째로 날아간다 |
| ✅ **P20-3** | **끄는 동안 읽지도 않는 목록을 다시 그렸다** (v1.43.0 · 접근이 두 번 바뀜) | 낮음 | 기각했다가(근거가 JS 만 잰 18.8ms 라 틀렸다) 되살렸고, **되살린 뒤 접근이 또 바뀌었다.** 착수 전에 *"초당 몇 번 나는가"* 를 재려다 먼저 걸린 것은 **화면 주사율**이다 — 이 PC 는 **240Hz**, 프레임 예산이 16.7ms 가 아니라 **4.17ms** 다. 그래서 `requestAnimationFrame` 합류를 설계하려다 *"무엇을 빼도 되나"* 를 먼저 물었더니 답이 달랐다: 한 틱 22ms 중 사람이 끌면서 보는 **지도는 0.69ms**, 나머지 **약 20ms 는 사이드바 목록**(JS 7.74 + DOM 12.3)이었다. **비용의 97%를 끄는 동안 읽지 않는 것이 쓰고 있었다.** 합류가 아니라 **빼는 것**이 답이다 — `input` 은 지도만, `change`(놓을 때)가 목록을 한 번 맞춘다. 건수는 `setLaunchCount` 로 살려 둔다. **끄는 중 22ms → 1.1ms**, 240Hz 예산 안에 든다 |

**확인만 하고 기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| 배선이 빠진 조작 지점 | `index.html` 의 `button`·`input`·`select` **29개 전수**를 JS 와 대조 — 참조조차 없는 것 **0건**. 핸들러가 없는 `arch-year` 하나는 **의도한 것**이다(값은 `arch-load` 클릭과 단축키 `a` 가 읽는다) |
| 단축키가 입력을 뺏는다 | `keyAction` 이 `tagName` 으로 `input`/`textarea`/`select` 를 먼저 걸러내고, 조합키(Ctrl/Alt/Meta)도 비켜난다. 주석에 이유까지 적혀 있다 — **이미 고쳐진 자리** |
| 그 밖의 조작 비용 | 상세 패널 열기 **1.1ms** · 통계 패널 **7.9ms** · 관심 탭 전환 **0.5ms** · 위성 검색 한 타 **1.9ms**(캡 덕이 아니라 원래 싸다). 전부 예산 안 |
| `escapeHtml` 의 정규식 5회 | 2,144행에 **17.2ms** 로 보였는데, P20-1 을 고치고 다시 재니 **3.2ms** 다 — 그 값은 포매터 비용에 **묻어 있던 것**이었다. 고칠 것이 없다 |

> **재지 않은 것을 밝혀 둔다** (전역 규칙 — 이게 없으면 읽는 쪽은 전부 쟀다고 읽는다).
> ① ~~**WebView2 의 실제 `Intl` 비용**을 재지 않았다~~ → **쟀다(2026-09-17)**.
>    Chromium 153 헤드리스(설치된 WebView2 런타임 `153.0.4234.32` 와 같은 메이저)에서
>    2,144행 **108.1 → 2.3ms(47.8배)**, 출력은 200건 전부 동일. **Node 로 쟀을 때(44배)보다 크다** —
>    즉 이 수정은 앱에서 더 크게 듣는다.
> ② ~~**`innerHTML` 을 실제로 파싱·레이아웃하는 비용**을 재지 않았다~~ → **쟀다(2026-09-17)**.
>    Chromium 153 에서 실제로 넣고 레이아웃을 강제해 재니 **400행 12.3ms · 1,803행 56.9ms**(행당 약 30µs)다.
>    **이 값이 판단을 두 번 뒤집었다** — P20-2 를 *"성능 항목이 아니다"* 라고 적은 것이 틀렸고(문자열 조립 0.3ms 만 보고 판단했다),
>    P20-3 기각 근거도 틀렸다. **하네스는 DOM 을 재지 않는다** — 하네스 수치만으로 성능을 단정하지 않는다.

> ⚠ **이번에도 예상이 먼저 빗나갔다.** "목록에 캡이 없어서 느리다"고 보고 캡을 후보로
> 적으려다, 재 보니 **2,925행 조립이 0.3ms** 였다. 비싼 것은 행 수가 아니라 **행마다 만드는
> 포매터**였다. 쪼개서 재지 않았으면 **엉뚱한 것을 고치고 89ms 를 그대로 뒀을 것이다** —
> P19 의 *"가장 비싼 계산이 가장 쓸모없는 결과를 낼 수 있다"* 와 같은 자리이고,
> 세 세션 연속으로 **측정이 직관을 뒤집었다.**
>
> ⚠ **그리고 P20-3 에서 세 번째로 교정됐다.** 합류(rAF)를 설계하려던 것이 *"무엇을 빼도
> 되나"* 한 번에 뒤집혔다 — **빼고 나니 합류할 것이 남지 않았다**(22ms → 1.1ms).
> 세 번 다 같은 자리다: **무엇을 재고 있는지**(조립이냐 렌더냐) · **누구의 것을 재고 있는지**
> (앱이냐 SearchHost 것까지냐) · **무엇을 줄이려는지**(횟수냐 한 번의 값이냐).
>
> ⚠ **그리고 그 교정이 다시 교정됐다(같은 날).** 위에서 *"캡은 비싸지 않다(0.3ms)"* 라고
> 결론냈는데 **그것도 틀렸다** — 0.3ms 는 문자열 조립이고, DOM 에 넣는 값이 **56.9ms** 였다.
> 즉 **처음 직관(캡이 필요하다)이 맞았고, 첫 측정이 틀린 것을 쟀다.** 교훈은 "직관을 믿어라"가
> 아니라 **"무엇을 재고 있는지 확인하라"** 다 — 스텁 DOM 위에서 잰 값은 *조립*이지 *렌더*가 아니다.

> **Phase 20 끝 (2026-09-17 · v1.41.0 + v1.42.0 + v1.43.0).** 후보 셋 전부 소진.
> 고친 것은 전부 **같은 한 줄기**였다 — *사람이 손을 대는 동안 앱이 멈춘다*.
>
> | 언제 | 처음 | 지금 |
> |---|---|---|
> | 검색 키 한 타 | 약 146ms | **약 20ms** |
> | 슬라이더 끄는 중(틱당) | 약 146ms | **1.1ms** |
>
> **이 Phase 의 진짜 소득은 숫자가 아니라 측정 습관이다.** 판단이 **세 번** 뒤집혔고,
> 세 번 다 "무엇을 재고 있는가"에서 갈렸다:
> 1. **조립이냐 렌더냐** — 2,925행 HTML 조립 0.3ms 를 보고 *"캡은 성능 항목이 아니다"* 라고
>    적었다. DOM 에 넣는 값은 1,803행에 **56.9ms** 였다. **하네스는 스텁 DOM 이라 렌더를 안 잰다.**
> 2. **누구 것을 재고 있나** — 켜 둔 앱의 자원을 잴 때 프로세스를 **이름으로** 묶어
>    Windows SearchHost 의 WebView2 까지 셌다(934MB → 실제 **567MB**). 자손으로 묶어야 한다.
> 3. **횟수냐 한 번의 값이냐** — 슬라이더를 `rAF` 로 **합류**시키려다, *"무엇을 빼도 되나"* 를
>    물으니 한 틱의 **97%가 끄는 동안 읽지도 않는 목록**이었다. 빼고 나니 **합류할 것이 남지 않았다.**
>
> **`CLAUDE.md` 의 자기점검 네 질문이 이번에도 값을 했다** — 특히 ②(*무엇을 빼도 되나*)가
> **두 번**(P20-3 · P19 때처럼) 최적화 자체를 불필요하게 만들었다. 다만 ①(*예산이 몇 개인가*)에
> **화면 주사율**이 빠져 있었다: 이 PC 는 240Hz 라 프레임 예산이 **4.17ms** 다. 16.7ms 를
> 기준으로 판단하면 4배 느슨하게 본다.

### Phase 21 — 착수 전 (2026-09-17 · 실측으로 뽑음 · **화면이 말하는 값이 맞는가**)

다섯 Phase 가 각각 물었다 — 데이터가 **오는가**(P15·S16) · 앱이 어떻게 **생겼나**(P17) ·
안 될 때 **말하는가**(P18) · 켜 두면 **낡는가**(P19) · 조작이 **빠른가**(P20).
**"그 숫자가 맞는가"는 한 번도 안 물었다.** `tests/` 가 대조하는 것은 전부 내부 일관성이다
(분류·정렬·배선·골든 = 우리 출력 vs 우리 예전 출력). **외부 기준과 맞춰 본 적이 없다.**

이 앱이 존재하는 이유가 그 숫자다 — 지도 위의 점 하나, 통과 목록 한 줄이 전부 계산 결과다.
그리고 **이 축의 실패가 가장 조용하다**: 멈추지도(P19-0·P20) 비지도(P18) 낡지도(P19) 않는다.
**틀린 위치는 맞는 위치와 화면상 똑같이 생겼다.**

> **실측 방법 (2026-09-17 · 네트워크 0 · 새 의존성 0)**: 참조 라이브러리(`sgp4`·`skyfield`)가
> 없어 **물리로 정해지는 기준**에 댔다 — 분점·지점의 태양 적위, 남중고도 = 90−|위도|+적위,
> 정지궤도의 경도 유지와 고도 35,786km, 한 주기 뒤 위도 복귀, 위도 ≤ 궤도경사, 천정에서 고도 90°,
> 같은 경도 남/북에서 방위 180°/0°. GMST 는 `satellite.js` 의 **독립 구현**과 대조했다.

**결론부터: 계산은 맞다.** 20여 개 기준에 전부 들었다.

| 검사 | 결과 |
|---|---|
| 율리우스일 (J2000·Unix epoch 정의) | 오차 0 |
| 태양 적위 (2026 춘분·하지·추분·동지) | **0.005° 이내** |
| GMST (우리 식 vs `satellite.js`, 6개 시각) | **0.01초 이내** |
| 남중고도 (적도·서울 3계절·북극) | **0.19° 이내** |
| 궤도 주기 (ISS 92.96분 · 정지 1436.10분) | TLE 값과 0.05분 |
| 고도 (ISS 424km · 정지 35,848km) | 알려진 범위 안 |
| 정지위성 24시간 경도 이동 (ABS-6) | **0.025°** |
| ISS 한 주기 뒤 위도 복귀 / 경도 −23.18°(기대 −23.2°) | 0.03° |
| 최대 위도 ≤ 궤도경사 51.63° | 0.15° |
| 천정 고도 90° · 거리 = 고도 | 0.03° / 0.001km |
| 방위 정남 180° · 정북 0° | **0.000°** |
| 그림자 판정 3케이스(반태양·태양·원통 밖) | 전부 기대대로 |
| 일출·일몰 (서울 춘분) vs 기상청 | 3~4분 — **코드가 일부러 무시한 대기굴절로 설명된다** |
| 집계 (건수·분류 합·기관 종류) vs 캐시 원본 직접 계수 | 일치 |

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P21-1** | **계산이 틀어져도 아무도 몰랐다** (v1.44.0) | 낮음 | 위 표의 기준들이 테스트에 하나도 없었다. 변이로 쟀다 — 큰 오류는 잡혔지만(적위 부호 반전 2건 · GMST 0.1% 3건 · 태양 고도식 부호 3건 · 그림자 원통 2배 1건) **작은 오류는 전부 놓쳤다**: 황도경사 0.44° · 중심차 2차항 · **태양 황경 0.5° 편향(시각 약 12분)** · **율리우스일 1분 오프셋** — 넷 다 **914개 전부 통과**. 고칠 코드는 없었다(계산은 맞다). **남긴 것은 회귀 37개**다 — 고정 시각·픽스처 TLE 로 물리 기준에 댄다. 넣고 다시 변이하니 **황도경사 2건 · 황경 편향 4건 · 율리우스일 5건 · 주기 2% 5건 · 방위 45° 회전 3건**이 잡힌다. **중심차 2차항(0.02°)만 일부러 안 잡는다** — 코드가 스스로 밝힌 "저정밀 근사" 안쪽이라, 더 좁히면 모델을 손댈 때마다 헛되이 빨개진다 |
| ✅ **P21-2** | **`tonightJob` 의 태양표 인덱스가 밀렸다** (v1.45.0) | 낮음 | 표는 job 생성 시각 기준인데 `computePasses` 가 **호출될 때마다 `Date.now()` 를 새로** 잡았다. 조각 처리라 호출이 뒤로 갈수록 `table[i]` 가 과거의 태양을 가리켰다 — 실측 61초 경과 → **2칸(60초)**, 1,441개 중 '어둠' 판정이 달라지는 지점 **4개**, ISS 로는 뒤집힌 통과 **0건**. **고친 이유는 크기가 아니라 구조다**: 안에서 시각을 만들면 테스트가 이 함수를 못 잰다(전역 자기점검 ④). 이제 **표가 시간축을 정하고**(`sun[0].t`), 표가 없으면 `startMs` 로 주입할 수 있다. 덤으로 **위성마다 창이 조금씩 다르던 것**도 사라졌다 — 한 job 은 한 창을 본다 |

**확인만 하고 기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| 천문 계산이 틀렸다 | 위 표 그대로 — 20여 개 기준에 전부 들었다. **고칠 것이 없다** |
| 집계가 원본과 다르다 | 건수·분류 합·기관 종류 수가 캐시 원본 직접 계수와 일치 |
| 발사장 통산 횟수가 이상하다(앱 2,077 vs 목록 437) | **내 비교가 틀렸다.** 그 값은 LL2 가 주는 **패드 통산** 발사 횟수지 우리 목록의 계수가 아니다 |
| 참조 라이브러리를 들인다(`sgp4`·`skyfield`) | 물리 기준만으로 위 표가 나왔다. **테스트 전용 의존성을 더할 이유가 지금은 없다** — 필요가 생기면(예: SGP4 자체를 의심하게 되면) 그때. 대신 픽스처 하나를 더했다(`celestrak_geo.txt`, 경사 0.0244° 인 ABS-6) |

> ⚠ **이번에도 틀린 것은 앱이 아니라 내 측정이었다 — 세 번.**
> ① **realm `Date`** — Node 쪽 `Date` 를 vm 안 `satellite.js` 에 넘겨 GMST 가 11.5시간 어긋난 것처럼
>    나왔다. `harness.js` 주석이 **바로 그것을 경고해 두었는데도** 밟았다(realm Date 로 다시 재니 0.01초).
> ② **경사 정지궤도** — `tle_geo` 의 첫 항목(TDRS 3)은 위도가 ±12° 흔들리는 위성이라 경도가 움직이는 게
>    정상이다. 경사 <0.1° 인 것(567개 중 330개)을 골라야 "정지"의 기준이 된다.
> ③ **값의 의미** — 위 발사장 건.
>
> **세 번 다 "기준이 틀린" 것이지 "앱이 틀린" 것이 아니었다.** 기준을 의심하는 데 쓴 시간이
> 앱을 고치는 데 쓴 시간보다 길었고, 그게 이 축의 성질이다 — **맞는 값과 틀린 값이 똑같이 생겼으니
> 판정자(기준)부터 검증해야 한다.**

> **Phase 21 끝 (2026-09-17 · v1.44.0 + v1.45.0).** 후보 둘 소진.
>
> **이 Phase 는 앱을 거의 고치지 않았다.** 20여 개 물리 기준에 댄 결과 **계산은 맞았고**,
> 고친 것은 *틀어져도 아무도 모르던 상태*(P21-1)와 *테스트가 못 재던 구조*(P21-2)다.
> 회귀는 914 → **962개**가 됐다.
>
> **가장 값어치 있던 것은 "변이가 안 잡히는 것"을 본 순간이다.** 두 번 있었다:
> ① P21-1 착수 전 — 황도경사 0.44° · 태양 황경 0.5° 편향 · 율리우스일 1분 오프셋이
>    **914개를 전부 통과**했다. 회귀를 깔고 나니 각각 2·4·5건이 잡힌다.
> ② P21-2 도중 — 표를 통째로 1분 밀어도 **959개가 전부 통과**했다. `dark` 가 길게 이어지는
>    값이라 몇 칸 어긋나도 같은 true 를 읽는다. **실제 영향이 작았던 이유가 그대로 테스트의
>    눈을 가렸다.** 통계가 아니라 **짝을 직접 재는 단언**(표 항목의 `dark` 를 getter 로 바꿔
>    읽힌 시각을 기록)을 넣고서야 잡혔다.
>
> **교훈:** *"영향이 작다"* 는 *"테스트가 잡는다"* 를 뜻하지 않는다. 오히려 **영향이 작은 결함일수록
> 통계적 단언을 빠져나간다.** 그런 자리는 값이 아니라 **구조**를 재야 한다.
>
> ⚠ **이 축에서는 틀린 것이 앱이 아니라 내 기준이었다 — 세 번**(realm `Date` · 경사 정지궤도를
> 정지궤도로 착각 · 발사장 "통산 횟수"의 의미). **맞는 값과 틀린 값이 똑같이 생기니, 앱보다
> 판정자를 먼저 검증해야 한다.** 세 번 다 코드 주석과 CHANGELOG 에 못 박았다.

### Phase 22 — 착수 전 (2026-09-17 · 실측으로 뽑음 · **파일을 함께 쓰는 자리**)

여섯 Phase 가 각각 물었다 — 데이터가 **오는가**(P15·S16) · 앱이 어떻게 **생겼나**(P17) ·
안 될 때 **말하는가**(P18) · 켜 두면 **낡는가**(P19) · 조작이 **빠른가**(P20) ·
그 숫자가 **맞는가**(P21). 전부 **앱 하나가 혼자 도는 것**을 전제했다.

**단서는 오늘 실측 중에 걸렸다**: P20-2 작업 중 `v1.41.0` 창과 `v1.42.0` 창이 **동시에 떠 있었다.**
이 앱은 두 번 뜰 수 있고, 캐시·설정·로그는 `%APPDATA%\RL3D\` **한 곳**을 쓴다.
그 자리를 한 번도 본 적이 없다.

> **실측 방법 (2026-09-17 · 네트워크 0)**: `api_client` 의 `CACHE_DIR`·`SETTINGS_PATH` 를
> 임시 디렉터리로 재대입해(진짜 캐시·설정은 건드리지 않고) 읽고-고치고-쓰기를 실제로 엇갈리게
> 했다. 파일을 잘라 **반쯤 쓰인 상태**를 만들고 앱이 무엇을 읽는지 봤다.
> 실제 exe 도 **두 번 띄워** 확인했다(진짜 `settings.json` 은 먼저 백업하고, 끝나고 대조했다).

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| ✅ **P22-1** | **설정이 반쯤 쓰이면 전부 날아가고, 다음 저장이 그것을 굳혔다** (v1.46.0) | 낮음 | `open(path, "w")` 는 **여는 순간 자른다** — 실측으로 연 직후 파일이 **0바이트**였다. 48KB 설정을 300번 쓰는 동안 동시에 읽던 쪽이 **빈 설정을 45번** 봤고, 그 `{}` 위에 한 번 저장하면 **키 5개 → 1개**가 되며 파일은 정상 JSON 이 된다. 이제 임시 파일에 쓰고 `os.replace` 로 바꿔 끼운다 — **빈 설정을 본 횟수 45 → 0회**. 캐시도 같은 헬퍼를 쓴다 |
| ❌ **P22-2** | **중복 실행 가드** — **기각**(2026-09-17) | 낮음 | *단일 인스턴스 + 기존 창 포커스* 로 가기로 하고, 착수 전에 두 가지를 쟀더니 **근거가 둘 다 빠졌다.** ① **설정 유실이 거의 사라졌다** — v1.46.0 의 *"못 읽었으면 저장 안 함"* 이 프로세스 간에도 방어한다. 두 프로세스가 각 400회씩 저장해도 유실 **0회**였고, 읽기-쓰기 사이를 **10ms 로 인위적으로 벌려야** 재현됐다(실제 간격은 마이크로초~2ms). ② **기존 창을 앞으로 가져올 수 없다** — `SetForegroundWindow` 가 **두 번 다 `False`**(백그라운드 호출 · 갓 실행된 프로세스 호출). Windows 의 포그라운드 잠금이고, 특히 전체화면 게임이 떠 있으면 확실히 거부된다 — **이 앱의 실제 사용 환경이 그렇다**(켜 두고 딴 일 하는 앱). 착수 전에 세운 원칙 *"앞으로 가져오는 데 성공했을 때만 막는다"* 를 지키면 **가드는 언제나 막지 않게 되어 넣어도 아무 일도 하지 않는다.** 남은 선택(조용히 종료 · 경고만)은 부작용이 이득보다 크다 — 특히 **좀비 자식 프로세스**(오늘 실측: 부모를 죽여도 자식이 창을 들고 남는다)가 뮤텍스를 쥐면 **앱이 아예 안 뜬다** |
| ✅ **P22-3** | **설정 저장이 읽고-고치고-쓰기였다(lost update)** (v1.46.0) | 낮음 | 착수 전 미측정 항목이던 *"한 인스턴스 안에서도 나는가"* 를 먼저 쟀다 — **난다.** `webview/util.py` 가 브릿지 호출마다 `Thread` 를 만들어(근거를 소스에서 확인) 두 저장이 겹친다. 실측 **200회 중 1회(0.5%)** 한쪽 값이 사라졌다. `threading.Lock` 으로 읽기·병합·쓰기를 묶어 **0회**가 됐다. ⚠ 이 락은 **한 프로세스 안**에서만 듣는다 — 인스턴스가 둘이면 여전하다(P22-2) |

**확인만 하고 기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| **인스턴스가 둘이면 요청도 2배** | **틀렸다 — 내 가설이었고 측정이 뒤집었다.** TTL 판정이 **디스크 캐시의 나이**로 이뤄지므로 두 인스턴스가 같은 TTL 을 나눠 쓴다. 새 인스턴스를 흉내 내 다시 불러도 **추가 요청 0회**였다. LL2 시간당 15회 한도는 안전하다(동시 호출 레이스는 남지만, 그건 캐시 파일이 늦게 써지는 순간뿐) |
| 반쯤 쓰인 **캐시** 파일 | `_cache_read` 가 `None` 을 돌려주고 다음 호출이 다시 받는다 — **설계대로다.** 다만 *오프라인 + 깨진 캐시*면 오래된 캐시 폴백(P18)도 못 쓴다. 캐시는 다시 받으면 되는 것이라 값이 작다 |
| 로그 파일 동시 회전 | `RotatingFileHandler`(512KB·백업 3)라 두 프로세스가 **회전 순간**에 겹치면 Windows 파일 잠금으로 실패한다. 다만 실측 증가 속도가 **5일에 27KB(약 5.5KB/일)** → 회전까지 **약 93일**이다. ⚠ **겹쳤을 때 무슨 일이 나는지는 재지 않았다** — 빈도가 이 정도면 지금 잴 값이 아니다 |

> ⚠ **고치려다 새 실패 모드를 두 번 만들었고, 두 번 다 측정·테스트가 잡았다(2026-09-17).**
>
> ① **원자적 쓰기가 저장 실패를 만들었다.** Windows 는 **대상 파일이 열려 있으면 교체를 거부한다**
>    (`WinError 5`). 읽기가 도는 동안 저장이 238회 실패했고, 그 실패로 `load_settings` 가 `{}` 를
>    돌려주면서 *빈 설정을 본 횟수가 **45 → 126회로 오히려 늘었다.*** 진짜 결함은 원자성이 아니라
>    그 아래에 있었다 — **`load_settings` 가 "설정이 없다"와 "못 읽었다"를 합쳐 버리는 것.**
>    교체에 재시도를 넣고, **못 읽었으면 저장을 건너뛰게** 했다(덮어쓰면 나머지를 잃는다). → **0회**.
> ② **그랬더니 영구히 망가진 파일에서 설정이 다시는 저장되지 않게 됐다.** 기존 테스트
>    (`test_broken_settings_file_does_not_kill_the_app`)가 바로 잡았다. **잠깐 못 읽는 것**과
>    **내용이 못 쓰게 된 것**은 대응이 반대다 — 재시도 사이에 **바이트가 변하는가**로 가르고,
>    후자는 `settings.bad.json` 으로 **치워 두고**(지우지 않고) 새로 시작한다.
>
> ⚠ **테스트도 한 번 공허했다.** "쓰는 동안 읽어 빈 설정이 나오나"는 **타이밍에 기대는 단언**이라
> 옛 비원자적 방식으로 되돌려도 통과했다. **대상 파일을 쓰기로 여는지**를 직접 보는 구조 테스트로
> 바꾸고서야 잡혔다 — P21-2 와 **정확히 같은 교훈**이다(통계가 아니라 구조를 잰다).

> ⚠ **두 인스턴스를 실제로 띄웠을 때는 설정이 안 깨졌다.** 조작을 안 했으니 저장이 거의 없었다 —
> **깨지는 경로는 격리 실험으로 증명했지, 야생에서 관측한 것이 아니다.** 확률은 낮고 결과는 전부다.
> 그래서 P22-1 의 근거는 *"자주 난다"* 가 아니라 *"나면 되돌릴 수 없다"* 이고, 고치는 값이 3줄이다.

> **Phase 22 끝 (2026-09-17 · v1.46.0).** 후보 셋 중 둘을 고치고 하나를 기각했다.
>
> **고친 것은 확률이 낮고 결과가 전부인 자리였다.** 설정이 반쯤 쓰이면 관측지·관심 목록·
> 위성 그룹·필터·배경이 **한 번에, 조용히, 영구히** 사라졌다 — 그리고 파일은 정상 JSON 이 되어
> 되돌릴 수 없었다. 근거는 *"자주 난다"* 가 아니라 *"나면 끝이다"* 였고, 고치는 값이 작았다.
>
> **이번 Phase 의 특징은 "고치다가 더 망가뜨린 것을 두 번 잡은 것"이다.**
> ① 원자적 쓰기가 Windows 파일 잠금과 부딪혀 **빈 설정을 본 횟수가 45 → 126회로 늘었다.**
>    진짜 결함은 원자성이 아니라 **`load_settings` 가 "없다"와 "못 읽었다"를 합친 것**이었다.
> ② 그것을 고치니 **망가진 파일에서 영영 저장이 안 되게 됐다** — 기존 테스트가 잡았다.
>    *잠깐 못 읽는 것*과 *내용이 못 쓰게 된 것*은 대응이 반대다(바이트가 변하는가로 가른다).
>
> **그리고 기각이 이 Phase 에서 가장 값어치 있었다.** P22-2 는 *설계를 내기 전에 재라* 는
> 규칙이 실제로 막아 준 사례다 — 착수 전 측정 둘이 **근거를 둘 다 무너뜨렸고**, 만들었다면
> **아무 일도 하지 않으면서 앱이 안 뜰 위험만 더하는 코드**가 될 뻔했다.
> 미리 세운 안전 원칙(*앞으로 가져오는 데 성공했을 때만 막는다*)이 그대로 기각의 근거가 됐다.

### Phase 23 — 착수 전 (2026-09-18 · 실측으로 뽑음 · **넓은 범위**)

일곱 Phase 가 각각 하나씩 물었다 — 데이터가 **오는가**(P15·S16) · 앱이 어떻게 **생겼나**(P17) ·
안 될 때 **말하는가**(P18) · 켜 두면 **낡는가**(P19) · 조작이 **빠른가**(P20) · 숫자가 **맞는가**(P21) ·
파일을 **함께 쓰는가**(P22). 이번에는 축을 하나로 좁히지 않고 **여덟 축을 한 번에 훑었다** —
좁혀 뽑을 축이 남아 있는지부터가 물음이었기 때문이다.

> **실측 방법 (2026-09-18 · 네트워크 0 · exe 시작 시간만 실제 실행)**:
> 로그 5일치(418줄)의 메시지 종류를 전수 분류 · `web/js` 22파일 + `index.html` 전수 grep ·
> **실제 캐시**(`launches.json` 96건 + `archive_2025.json`, 중복 제거 437건)를 직접 파싱 ·
> exe 를 한 번 띄워 **프로세스 시작 → 파이썬 첫 로그 → 창 생성**을 쟀다(2번 모니터, 끝나고
> `Get-Process -Name RL3D` 로 전부 종료 확인) · 설정 키의 저장/복원을 짝맞춤.

| # | 후보 | 난이도 | 실측 근거 / 무엇 |
|---|------|--------|------------------|
| **P23-1** | **프론트에서 예외가 나면 아무 데도 안 남고, 부트가 통째로 멈춘다** | 중간 | `applog.install_excepthook()` 의 docstring 이 이유를 이미 적어 뒀다 — *"GUI 에선 traceback 이 아무 데도 안 남는다"*. **그 논리를 JS 에는 적용하지 않았다**: `window.onerror`·`unhandledrejection` **0건**(`web/js` 22파일 + `index.html` 전수) · `console.error` 4곳뿐인데 `pythonw` 로 띄우면 그 콘솔이 없다 · Api 브릿지 10개 메서드에 **로그 통로가 없다**. 로그 5일치 실측은 INFO 418 · WARNING 4 · **ERROR/CRITICAL 0건** — 파이썬은 남기고 있고 정작 **코드의 72%(JS 4,557줄 대 파이썬 1,739줄)** 가 침묵한다. 그리고 `pywebviewready` 핸들러는 **try 없이 10단계**이고 첫 단계가 `bindUI()`(`getElementById` 27개를 줄줄이 부른다) — 하나가 던지면 **`initMap()` 까지 안 돌아 지도가 아예 안 뜨는데** 화면에도 로그에도 아무 말이 없다 |
| **P23-2** | **클러스터를 풀면 마커 85개가 정확히 한 점에 포개진다** | 중간 | 실제 캐시 437건 중 **서로 다른 발사장 좌표는 55개**다. `(28.5619, -80.5774)` 한 점에 **85건** · 다음이 84건이고, **425건(97%)** 이 겹치는 점 위에 있다. `clusterMaxZoom: 6` 이라 **줌 7 이상에서 클러스터가 풀리는데** 좌표가 동일하므로 갈라지지 않고 전부 같은 픽셀에 그려진다 — 클릭하면 `e.features[0]` 하나만 열리고 나머지 84건은 **지도에서 열 길이 없다**. 클러스터 클릭의 `getClusterExpansionZoom` 도 동일 좌표에선 갈라 줄 것이 없다. ⚠ **코드 주석이 이미 절반을 알고 있다**(`launches.js`: *"개별 점은 완전히 겹쳐 1건과 300건이 똑같이 보인다"*) — 그래서 히트맵(P12-15)을 만들었지만 히트맵은 **"몇 번"만 답하고 "그 85건이 뭔지"는 답하지 않는다** |
| **P23-3** | **의존성이 고정돼 있지 않은데 배포가 전제다** | 낮음 | `requirements.txt` 가 전부 `>=` 다(`pywebview>=6.0` · `pyinstaller>=6.0` · `pythonnet>=3.0`). `build.bat` 은 빌드마다 `pip install -r requirements.txt` 를 돌리므로 **새 메이저가 나오면 조용히 올라간다.** 지금 빌드된 조합은 pywebview **6.2.1** · pyinstaller **6.21.0** · pythonnet **3.1.0** 인데 **어디에도 안 적혀 있다** — 지금 exe 가 무엇으로 만들어졌는지 리포만 보고는 알 수 없다. 전역 9번의 *"배포가 전제인 프로젝트는 그 조건이 이미 충족된 것"* 에 해당한다. ⚠ **재지 않은 것: 실제로 깨지는가.** 상위 버전을 설치해 빌드해 보지 않았다 |

**확인만 하고 기각한 것 (값을 찍어 보고)**

| 후보 | 왜 |
|---|---|
| **onefile 시작 비용** | 전역 규칙 8번이 `--onedir` 를 권하는데 이 앱은 `--onefile` 이라 재 봤다. 실측: 프로세스 시작 → 파이썬 첫 로그 **583ms**(압축 해제) → 창 생성 **+247ms** = **약 830ms**, exe 15.0MB. **싸다** — 바꿀 근거가 없다. (P22 가 찾은 좀비 자식 프로세스는 onefile 의 산물이지만 그건 내 테스트 방식의 문제지 사용자의 문제가 아니다) |
| **브릿지 실패 처리** | `pywebview.api.*` 호출 **11곳 전부** `try`/`.catch()` 안에 있다. P18 이 이 축을 이미 닫았다 |
| **설정 키 저장/복원 짝** | 저장 12키 · 복원 12키. `applySettings` 가 9개를 맡고 나머지 셋(`favorites`·`dismissedUpdate`·`firstRunSeen`)은 각자 모듈에서 복원한다. **구멍 0건**(P17 측정과 같은 결과) |
| **`net_precision` 을 화면이 지어내나** | 실제 캐시의 **39%(37/96)** 가 Month·Quarter·Year 급이라 후보로 보였는데, `focus.js`·`panels.js`·`sequence.js` 가 **전부 이미 쓰고 있고** `test_frontend.js` 에 *"모르면 안 띄운다"* 단언까지 있다. **소진된 축** |
| **캐시 무한 증가** | 현재 캐시 디렉터리 **2.6MB**(2025 아카이브 1.47MB가 대부분). 연도당 약 1.5MB 라 10년을 불러와야 15MB다. 지금 잴 값이 아니다 |
| **접근성(aria·tabindex)** | `aria-`·`role=`·`tabindex` **전부 0건** · `:focus` 2건. 다만 혼자 쓰는 앱이고 P17-3 이 단축키 13개를 이미 깔았다 — 전역 9번(YAGNI)대로 **도입 조건이 없다**. 남에게 배포하게 되면 다시 본다 |
| **README 회귀** | README 가 v1.40.0 이후 안 바뀌어 6버전 밀린 줄 알았는데, v1.41~v1.46 은 전부 **고침**(성능·정확도·설정 안정성)이라 기능 목록이 바뀌지 않았다. 단축키 목록도 `keys.js` 와 **일치**. 밀리지 않았다 |

> ⚠ **측정 도구가 또 가짜 후보를 낼 뻔했다 — 세 세션 연속 같은 자리다.**
> 로그에 *"RL3D 시작" 85회 / "정상 종료" 6회* 가 찍혀 있어 **93%가 비정상 종료**로 보였다.
> 코드를 보니 내 exe 스모크가 `RL3D_DEV_MONITOR=2` 로 돌아 `dev_mode` 였고(그래서 `_save_geometry`
> 자체가 안 붙는다) 끝을 `Stop-Process` 로 냈다 — **내 테스트 방식이 만든 숫자**였다.
> P18 은 하네스 셀렉터를 안 채워, P19 는 정규식이 엉뚱한 `setInterval` 에 걸려 같은 일을 당했다.
> **측정값이 극단적일수록(93%·115초·0건) 도구부터 의심한다.**

> **축을 더 넓히지 않기로 했다 (2026-09-18).** 남은 두 축(위성 화면 · 오래 켜 둔 exe)은
> **이미 한 번씩 훑린 자리**다 — 위성은 S16 이 데이터를, P19 가 켜 둔 동안을, P20 이 조작
> 속도를 쟀다. 반면 뽑은 셋은 축이 서로 안 겹친다(**증거 · 지도 · 빌드**). 더 캐면 뽑는 데만
> 세션이 간다.
>
> **순서는 P23-1 → P23-3 → P23-2 다.**
> ① **P23-1 이 먼저인 이유는 나머지가 여기에 기대기 때문이다.** 지금은 앱이 조용히 죽으면
>    재현시켜 보는 것 말고 알 방법이 없다. P23-2 를 고쳐도 "그게 실제로 도는지" 확인할 통로가 없다.
> ② **P23-3 은 P23-1 커밋에 얹는다** — `requirements.txt` 고정은 3줄이라 따로 세션을 쓸 일이 아니다.
> ③ **P23-2 는 설계부터.** 겹친 85건을 어떻게 열게 할지가 갈린다(같은 좌표 클릭 시 목록 팝오버 /
>    미세 오프셋으로 펼치기 / 발사장 관점 화면으로 보내기) — 전역 2번대로 설계 먼저, 합의 후 구현.
>
> ⚠ **P23-1 에서 재지 않은 것 둘.**
> ㉠ **실제로 예외가 얼마나 나는지는 못 쟀다** — 로그가 없어서 잴 수가 없고, 그게 곧 이 후보의
>    근거다(순환이라 숫자로 내놓을 수 없다). 그래서 근거는 *"자주 난다"* 가 아니라
>    *"나면 알 방법이 없다"* 이다 — P22-1 과 같은 모양의 근거다.
> ㉡ **로그 폭주 비용을 안 쟀다.** JS 가 로그를 쓸 수 있게 되면 **매초 도는 위성 틱**에서 예외가
>    날 때 초당 한 줄씩 쌓인다. 회전이 512KB·백업 3이라 **증거가 되레 밀려 날아갈 수 있다.**
>    착수하며 이걸 먼저 재고 같은 메시지 억제를 넣을지 정한다.

### 로드맵 원칙
- 각 항목 완료 시 이 표에 ✅ 표시하고 README 기능 목록을 함께 갱신한다(CLAUDE.md 문서 규칙).
- 새 데이터 소스가 붙는 항목(P7-5, P7-6)은 `python api_client.py` 스모크 출력에 반드시 포함시킨다.
- LL2 시간당 15회 제한은 모든 항목의 상수다 — 기능 추가로 요청 수가 늘면 캐시 전략을 먼저 재검토한다.
