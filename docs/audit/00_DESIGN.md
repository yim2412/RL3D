# 전면 감사 설계 — 2026-09-24 (합의본)

> 절차: `~/.claude/commands/audit.md`. 이 프로젝트 `CLAUDE.md` 에 자체 감사 절차는 없다.
> 멈추는 곳은 **설계(합의 완료)** 와 **분석 끝** 두 곳뿐이다.

## 합의된 선택

- 독립 리뷰: **에이전트 1개 · 보안·외부 API 예산·하위호환 영역만**. 내 발견·대장·"당한 것" 목록은 주지 않는다.
  에이전트 발견은 내가 프로브로 재현하기 전엔 최대 `중간`. 끝나면 겹침/단독/오탐을 센다.
- 결함이 아니라 결정으로 보이는 것: **숫자만 재서 `판단필요` 로 대장에 올리고, 분석 끝에 한 번에 묻는다.**

## 영역

코드·로직 · 데이터·수치 · 회귀 · 성능 · 빌드·배포물 · 위생 · 하위호환 · 수치 안정성·단위 ·
리소스 누수 · 인코딩 · UI · 보안 · 외부 API 예산(호스트 4곳: LL2·celestrak·api.github.com·arcgis 타일) ·
라이선스·귀속 · 시각 경계 · **측정 도구 자체**(추가) · **개발 환경 규칙**(추가, 가볍게).

## 안전망 기준선 (2026-09-24 실측, 전부 rc=0)

| 안전망 | 건수 | 시간 | 하한 |
|---|---|---|---|
| test_parsing.py | 99 → **100**(스키마 가드 추가) | 1초 | 100 |
| test_cache.py | 92 | 10초 | 92 |
| test_frontend.js | 1,427 | 5초 | 1427 |
| test_docs.js | 13 → **20**(수치 대조표 7) | 0초 | 20 |
| test_layout.js | 52 | 49초 | 52 (CI 에서 Edge 없으면 FAIL) |
| CI tests.yml | 최신 success | 3분 56초 | — |

## 재지 않는 것

실제 exe 장시간 메모리 관찰 · WebView2 전용 렌더 차이(헤드리스 Edge 로 대신) ·
arcgis 타일 요청량(MapLibre 내부 — 이용 조건만 인용).

## 통과 기준

`치명`·`높음` 전부 수정 또는 기각(사유). `중간` 은 대장에 남긴 채 통과 가능(남은 것은 PLAN 으로).
백테스트가 못 잡은 사건은 보고서에 **알려진 사각**으로.

## 감사 비용

LL2 스모크(`python api_client.py`)는 분석 시작 때 1회. 감사 중 `CACHE_SCHEMA` 동결.
변이 3종은 **격리 사본**(scratchpad, `git archive`)에서 — 소스를 망가뜨리는 도구가 내가 읽는 파일과 섞이지 않게.
layout 테스트는 변이 대상에서 뺀다. exe 빌드는 수정 판 끝에 한 번.

## 자기 검토

- 참조 구현(국방)에서 뺀 것 5: 3층 감사·버전 연동 커밋 · `refresh_anchors.py` · 묶음 단위 · 모델 타당성 단계 · 로드맵 충돌.
- 유지: 근거 등급 · 앵커 인용 · 차분 · COVERAGE · 검사기 selftest · 심각도 상한 · 회피 경보 · 백테스트.
- 부작용 검토로 넣은 것 4: 스모크 1회·스키마 동결 · 감사 산출물 개인정보 스캔 · 허용 목록 `사유` 필수 ·
  백테스트 worktree 정리.

## 1단계에서 실제로 생긴 것 (도구 목록)

| 도구 | 재는 것 | 자기 확인 |
|---|---|---|
| `tools/audit_scan.py` | 9개 검사 · 허용 목록(사유 필수) · 대상 하한 · innerHTML **래칫** | `--selftest` 10건 + 검사 무력화 시 selftest FAIL 확인 |
| 건수 하한 5종 | 각 테스트가 0건으로 통과하지 않게 | 하한을 실측+1 로 올려 5종 모두 FAIL 확인 |
| `test_docs.js` 문서 수치 대조표 | TTL·폴링·페이지 상한 7쌍 | 코드 쪽(TTL 6시간)·문서 쪽(10분) 변이 둘 다 FAIL |
| `test_parsing.SchemaShapeGuard` | 모양 변화 ↔ `CACHE_SCHEMA` | 필드 추가 시 FAIL(추가된 필드명 출력) |
| `probes/fault_matrix.py` | 경로 5 × 고장 11 × 캐시 2 = 110칸 · 필드 퍼징 1,224건 | `--selftest` 판정 5종 + 대조군 |
| `check_findings.py` | 대장 형식·심각도 상한·앵커 실재·COVERAGE 누락/유령 | `--selftest` 7건 + 대조군 |
| `backtest.py` | 과거 사건 12건 · **차분 판정** | 첫 판정이 관대해 6/12(거짓 적중 3) → 차분으로 3/12 |
| 변이 3종(wiring·if-js·if-py) | 격리 사본에서 전수 | — |
