# 분석 판 요약 — 2026-09-24

> 정본은 `FINDINGS.md`. 여기는 **순서 · 판단필요 · 영역 판정 · 독립 리뷰 대조 · 도구가 틀린 기록**이다.

## 영역 판정

| 영역 | 판정 | 근거 |
|---|---|---|
| 외부 API 예산 | **발견 6** (F-001·002·004·005·014 + F-006 부수) | 고장 매트릭스 110칸 중 48칸 FAIL · p001·p002·p004·p005 |
| 시각 경계 | **발견 2** (F-003 높음 · F-012) | p003 (시계 주입) |
| 회귀(테스트가 재는가) | **발견 4** (F-018·019·020·021) | if-js 536개 중 생존 26 · if-py 106개 중 생존 26(스모크 전용 9 제외 17) · wiring 59/59 |
| 보안 | PASS + 판단필요 1 · 낮음 1 | p_xss 렌더 45회 · 오염 도달 45 · 날것 0 · 되돌리는 변이로 검증 · F-013·F-015 |
| 하위호환 | 발견 2 (낮음) | F-010·011 |
| 위생 | 발견 2 (낮음) | F-008·009 |
| 측정 도구 자체 | 발견 1 + 이번 감사의 도구가 **11번** 틀림(아래) | F-007 |
| 라이선스·귀속 | 판단필요 1 | F-016 |
| 빌드·배포물 | 판단필요 1 | F-017 · 의존성 고정·산출물 무시는 정상 |
| 인코딩 | PASS | cp949·cp1252 자식 stdio 로 스모크·새 도구 전부 rc=0 |
| 리소스 누수 | PASS (단 지키는 테스트가 없다 → F-018) | 재무장 타이머 넷 모두 `clearInterval` 선행 · `map.on` 은 1회성 설정에만 |
| 수치 안정성·단위 | PASS (훑음) | 날짜변경선 분할·방위 정규화 + 테스트 존재(P27) |
| UI | 새로 재지 않음 | test_layout 52건 초록(P34~P37) |
| 성능 | **재지 않음** | P19·P33 기준선 유지 — 이번 감사 범위 밖으로 둠 |
| 데이터·수치 | PASS | 필드 퍼징 1,224건 · 의도된 탈락(발사장 좌표) 외 0 |
| 코드·로직 | 발견 1 (F-006) | |
| 개발 환경 규칙 | 새 발견 없음 | P48 훅 동작 확인(변이 중 lock 존재) |

## 수정 순서 (심각도순이 아니다 — 선행 조건·공유 코드·비용 순)

| 순 | 발견 | 이유 |
|---|---|---|
| 0 | **F-020** | 테스트만. F-001 을 고칠 자리의 폴백(572·627)을 지키는 단언이 없다 — **고치기 전에 먼저** 둔다 |
| 1 | **F-001 + F-002** | 같은 자리(응답 → 캐시 쓰기)를 고친다: "모양이 틀린 200 / 0건" 을 **오류로 분류해 폴백으로 보낸다**. F-004 의 실패 기록이 이 분류 위에 선다 |
| 2 | **F-006** | 캐시 쓰기 실패를 네트워크 실패와 분리 — F-004 가 "실패"를 셀 때 섞이면 안 된다 |
| 3 | **F-004** | 실패 시각을 기억해 일정 시간 재요청을 막는다(호스트별). 가장 비싼 항목(중) |
| 4 | **F-005** | 강제 갱신 최소 간격 — 3 과 같은 "마지막 요청 시각" 을 재사용 |
| 5 | **F-003** | 독립적·소. 캐시 mtime 의 연도 < 대상 연도+1 이면 한 번 다시 받는다(스키마 변경 불필요) |
| 6 | **F-018** | 테스트만. 3~5 에서 타이머를 안 건드리므로 순서 무관 — 싸서 앞에 둔다 |
| 7 | F-010 · F-011+F-021 · F-012 · F-013 | 소·국소 방어 |
| 8 | F-007 · F-008 · F-009 | 문서·주석 |
| 9 | F-019 | 18줄을 동치/비동치로 판단 — 가장 손이 많이 간다 |

**회피 경보 확인**: 상위 5개 중 F-004 는 구조 변경(실패 기록)이다 — 국소 수정만 고른 것이 아니다.
**`CACHE_SCHEMA`**: 위 수정은 정규화 결과의 모양을 바꾸지 않는다 → 올리지 않는다(`SchemaShapeGuard` 가 확인).

## 판단필요 — 분석 끝에 한 번에 묻는다

| 발견 | 결정 | 판단용 숫자 |
|---|---|---|
| F-014 | 캐시 스키마를 종류별로 나눌까 | 스키마 상향 직후 첫 실행 = LL2 2 + 아카이브 연도당 ≤5 + Celestrak 그룹×2 + GitHub 1 |
| F-015 | CSP 를 넣을까 | 지금 날것 삽입 0건 · 인라인 `onerror` 가 있어 `unsafe-inline` 필요 |
| F-016 | 서드파티 라이선스 전문을 어디에 둘까 | 전문 2개 · 수 KB |
| F-017 | onefile → onedir | exe 15.06MB · 시작 시간 **재지 않음** |

## 독립 리뷰 대조 (에이전트 1개 · 보안·API 예산·하위호환)

| | 건수 | 발견 |
|---|---|---|
| 겹침 | 1 | F-003 |
| 에이전트만 | 8 | F-004(**높음** — 재현) · F-005 · F-006 · F-010 · F-011 · F-012 · F-013 · F-014 · F-015 (에이전트 9번이 둘로 갈림) |
| 나만 | 11 | F-001 · F-002 · F-007 · F-008 · F-009 · F-016 · F-017 · F-018 · F-019 · F-020 · F-021 |
| 오탐 | 0 | 재현한 다섯(F-004·005·006·010 + F-003)이 전부 재현됐다 |

**에이전트만 찾은 `높음` 이 1건(F-004)** → 규칙대로 다음 감사에도 쓴다. 에이전트는 **XSS 전수 오염 검사도 독립으로 돌려** 같은 결론(0건)을 냈다 — 내 p_xss 와 겹침.
참고: 에이전트의 영역(API 예산)이 내 발견의 대부분과 같은 영역이라 겹침이 적은 것은 **서로 다른 축을 봤다**는 뜻이다 — 나는 응답 **모양**(고장 매트릭스), 에이전트는 요청 **빈도**(폴링·재시도).

## 백테스트 — 알려진 사각

차분 판정 **3/12** (P34 `ui-top` · P48 변이 흔적 · P29 IncompleteRead). 못 잡은 9건의 종류:
프론트 로직(Set.includes) · 레이아웃 기하(대비) · CI 환경(TZ·로컬 전용 파일·cp1252 콘솔) ·
문서 개수 · 설정 파일 원자성 · 브릿지 인자 · 스키마 미상향(→ 1단계에서 `SchemaShapeGuard` 로 막음, 단 차분 백테스트로는 재채점 불가).
**정적 스캔·API 매트릭스는 프론트·환경 부류를 원래 못 본다** — 그쪽은 기존 테스트·변이·레이아웃 테스트가 맡는다.
⚠ 도구는 사건을 알고 나서 만들었으므로 3/12 는 상한으로 읽는다.

## 이번 감사에서 도구가 틀린 기록 (전부 관대/공허 방향이면 ⚠)

1. `webbrowser.open` 을 파일 open 으로 오탐 (엄격 — 바로 보임)
2. ⚠ 하한 selftest 가 하한을 꺼도 통과 — 래칫이 대신 잡고 있었다
3. ⚠ heredoc 이 백슬래시를 먹어 치환이 조용히 빠짐 — 세 번(메모리에 이미 있던 함정)
4. ⚠ sed 로 되돌린 상수가 안 돌아옴 — 테스트가 잡아 `git checkout` 으로 복원
5. ⚠ 백테스트가 무관한 FAIL 에 맞아 6/12 → 차분 판정으로 3/12
6. ⚠ XSS 프로브 첫 실행이 예외로 빠져 공허 — 하한이 잡음
7. ⚠ XSS 프로브가 끝에서만 스캔 — 되돌리는 변이를 못 잡음 → 렌더마다 스캔
8. img src 의 `javascript:` 를 실행 가능으로 오판 (엄격)
9. ⚠ 개인정보 스캔이 추적 목록만 봐서 대상 0건으로 초록 → 디스크를 훑게
10. 대장 머리 정규식이 `·` 든 영역명에서 FAIL (엄격)
11. ⚠ if-py 변이가 `*.py` glob 을 못 펼쳐 대상 0개 — 도구가 경고했고 극단값으로 드러남

**관대 방향 8 · 엄격 방향 3.** 전역 규칙("도구는 관대해지는 방향으로 틀린다")이 이번에도 그대로 맞았다.
