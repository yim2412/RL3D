# 개발 노트 후보 — RL3D (캐기 단계, 2026-09-05)

> **노트가 아니라 후보 목록이다.** 재료를 훑어 *"무엇이 어떻게 깨졌고 어떻게
> 알아냈나"* 형태만 뽑았다. 탈락시킨 것도 이유와 함께 남긴다(§4).

## 재료 — 실측 (2026-09-05, **2026-09-14 재측정**)

> **숫자를 갱신할 때 옛 값을 지우지 않는다** — 어느 쪽이 얼마나 늘었는지가 다음 판단의 근거다.

| 항목 | 2026-09-05 | 2026-09-14 | 세는 명령 |
|---|---|---|---|
| 커밋 | 30 | **90** | `git log --oneline \| wc -l` |
| 문서 | 675줄 | **2,953줄** (CHANGELOG 982 · DECISIONS 920 · PLAN 672 · README 223 · CLAUDE 156) | `wc -l *.md docs/*.md` |
| 커밋 본문 | 270줄 | **1,368줄** | `git log --format='%b' \| grep -c .` |

**문서가 본체라는 결론은 그대로다** (675>270 → 2,953>1,368). 다만 **`docs/DECISIONS.md` 920줄이
9/5 표에는 아예 없었다** — 그때 `wc -l *.md` 만 세어 `docs/` 를 빠뜨렸다. 규약 ⓒ 그대로다.

## 재료 — 옛 표 (2026-09-05, 갱신하지 않는다)

| 항목 | 값 | 세는 명령 |
|---|---|---|
| 커밋 | **30** | `git log --oneline \| wc -l` |
| 문서 | **675줄** (PLAN 311 · CHANGELOG 141 · README 135 · CLAUDE 88) | `wc -l *.md` |
| 커밋 본문 | **270줄** | `git log --format='%b' \| grep -c .` |
| 후보 | **34건** | `awk '/^## 2\. 후보 목록/,/^## 3\./' docs/notes/CANDIDATES.md \| grep -cE "^\\\| \*?\*?[A-H][0-9]+"` |

**다섯 프로젝트 중 가장 얇고, 실제로도 얇았다.** 다만 얇은 방식이 피파와 반대다 —
피파는 문서 512 < 커밋 본문 658 이라 `git log` 가 본체였는데, **여기는 문서 675 >
커밋 본문 270** 이라 원래대로 문서가 본체다. 특히 `PLAN.md` 의 Phase 11 표는
**각 항목에 "왜 그렇게 했나"가 한 문단씩 붙어 있어** 사실상 판단 기록이다.

## 근거 표기

**전부 `인용`이다.** 이번 세션에서 다시 잰 것은 없다. 위 재료 표의 네 값만 실측이다.

> 재측정이 가능한 것과 아닌 것을 미리 갈라 둔다. `python tests/test_parsing.py`(34항목)·
> `node tests/test_frontend.js`(102항목)·`python api_client.py` 스모크는 지금도 돌릴 수
> 있다. 반면 **P11-6 의 렌더 성능 수치(8,000개 28ms)는 WebView2 실측이라 다시 재려면
> 앱을 띄워야 한다.** 노트에 쓸 때 필요하면 그때 잰다.

---

## 1. 이 프로젝트에서 두드러진 것 — 한 줄로

**"앱은 정상인데 사용자에게는 고장으로 보이는 상태"가 반복해서 나왔다.** 일곱 번이다.
다른 네 프로젝트에서는 이만큼 한 갈래로 모이지 않았다. 데스크톱 앱이면서 화면이
지도 하나라, **아무것도 안 보이는 것**이 곧 고장의 모양이기 때문으로 보인다.

---

## 2. 후보 목록 — 55건 (2026-09-05 에 34건 · **2026-09-14 에 21건 추가, I~O**)

### A. 고장이 아닌데 고장처럼 보인다

| # | 무엇이 틀렸나 | 어떻게 알아냈나 / 어떻게 했나 | 근거 | 일반성 |
|---|---|---|---|---|
| A1 | **창이 화면 밖에 떠서 앱이 안 뜬 것처럼 보였다.** 보조 모니터에서 창을 옮긴 뒤 그 모니터를 떼면 저장된 좌표가 **어느 화면에도 없는 자리**가 되는데, 그대로 복원했다 | 복원 좌표가 어느 모니터든 **최소 120×40px 이상 걸치는지** 확인하고, 아니면 OS 기본 위치로 넘긴다 | `인용` `v1.5.2` · `d100d53` | **높다.** 창 위치 복원이 있는 모든 앱 |
| A2 | **오프라인이면 지도만 비어 앱 고장으로 오해된다.** 배경 타일만 온라인 전용이라 발사·위성은 캐시로 뜨는데 **지도가 검다** | 좌하단 안내 배지. **타일 연속 3회 실패에서만** 띄운다(한두 개 실패는 정상적으로 흔하다) — `error` 이벤트의 `sourceId` 가 carto/esri 인 것만 집계하고, `data` 이벤트에서 `tile.state === "loaded"` 를 보면 카운터를 되돌린다. 다시 받아지면 스스로 사라진다 | `인용` `v1.5.0` · `P11-7` | **높다.** 부분 오프라인의 표현 |
| A3 | **세 대역이 모두 꺼진 설정을 복원하면 위성이 하나도 안 보인다** | 그 설정은 **무시한다.** 저장된 값이라도 "앱이 고장난 것처럼 되는 상태"면 복원하지 않는다 | `인용` `v1.4.0` | **중~높음.** 설정 복원의 방어 |
| A4 | **관심 위성이 필터에 걸려 지도에서 사라지면 고장으로 보인다** — 사용자가 ⭐ 로 고른 것인데 점이 없다 | **관심 위성은 필터 예외**로 뒀다 | `인용` `v1.4.0` · `P11-3` | **중~높음** |
| A5 | **검색어를 지도에도 걸면, 검색창을 닫은 뒤 왜 위성이 없는지 알 수 없다** | 검색어는 **사이드바 전용**으로 남겼다 — 필터의 "흔적"이 화면에 안 남는 곳에는 필터를 걸지 않는다 | `인용` `P11-3` | **높다.** 보이지 않는 필터의 문제 |
| A6 | **위성 레이어가 꺼진 채로 위성을 검색하면 아무 일도 안 일어난다** | 그 사실을 안내한다 | `인용` `v1.2.0` | **중** |
| A7 | **사용자가 끈 것을 앱이 되돌렸다.** 위성 그룹을 전부 해제하면 기본 그룹으로 폴백됐다 — **빈 리스트가 falsy 라 `groups or DEFAULT` 가 기본값을 반환** | `None`(미지정)과 `[]`(명시적 빈 선택)을 구분. 전체 해제 시 위성 0개가 맞다 | `인용` `6d66170` | **높다.** falsy 빈 컬렉션의 전형 |

### B. 에러 메시지가 거짓말을 했다

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| **B1** | **타임아웃을 "인터넷 연결을 확인하세요"로 안내했다.** `urlopen` 은 타임아웃을 그대로 던지지 않고 **`URLError(reason=timeout)` 으로 감싼다.** `URLError` 검사가 위에 있어 서버가 느릴 뿐인 상황에서도 연결 문제라고 말했고, **`isinstance(e, TimeoutError)` 분기는 한 번도 실행되지 않는 죽은 코드**였다 | **실제 타임아웃을 유발해** 확인한 뒤 `reason` 까지 보도록 고쳤다 | `인용` `v1.5.2` · `d100d53` | **높다.** 예외 래핑 + 순서 의존 + 죽은 분기가 한 자리에 |
| B2 | **실제로 겪는 실패에 전용 문구가 없었다.** 403 이 `서버 응답 오류(403).` 로만 보였다 — Celestrak 은 대형 그룹을 짧은 간격으로 거듭 받으면 막는데, 그게 이 앱이 실제로 겪는 실패다 | 상태코드→문구를 `HTTP_ERROR_MESSAGES` 표 **한 곳**으로 모으고 404·500·502·503·504 도 함께 채웠다 | `인용` `v1.5.2` | **중~높음** |

### C. 검사가 공허하게 통과했다

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| **C1** | **좌표가 전부 `NaN` 인데 검사들이 통과했다.** 프론트 테스트 하네스에서 `satellite.min.js` 를 **별도 `vm` 컨텍스트**에 올렸더니, 라이브러리 내부 `date instanceof Date` 가 **realm 불일치로 false** 가 되어 계산 결과가 전부 `NaN` 이었다. 그런데 `every()` 로 쓴 검사들은 **공허하게 참**이 된다 | 실제 라이브러리를 **앱과 같은 vm 컨텍스트**에 올리도록 고쳤다. **브라우저에선 없는 문제** — 테스트 환경이 실행 환경과 다를 때만 생긴다 | `인용` `214b088` · `P11-8` | **높다.** 빈 집합에 대한 전칭 검사 + 테스트 환경 차이 |
| **C2** | **경계 테스트가 상수로 기대값을 계산해, 그 상수를 0 으로 바꿔도 통과했다** | **경계 테스트는 좌표를 직접 적는다.** 처음엔 `MIN_VISIBLE_W` 로 계산했더니 기대값이 상수를 따라 같이 움직였다 | `인용` `v1.5.2` | **높다.** Argus 의 "배선 테스트는 기본값이 아닌 값으로" 와 같은 뿌리 |
| C3 | **통과 예측(`computePasses`)이 한 번도 검증된 적이 없었다.** 테스트가 유틸·분류·설정만 덮고 있어서 **정작 틀려도 화면으로는 알 수 없는 계산 로직이 비어 있었다** | 2차 확장에서 계산 로직까지 넣어 62 → **102항목** | `인용` `214b088` | **높다** |
| C4 | **정답표가 없는 계산을 어떻게 재나** — 통과 예측은 외부 정답이 없다 | **물리 불변식으로 검증한다**: 경사 51.6° 위성은 남극(−82°)에서 통과 **0건** · 통과 간격은 궤도 주기의 **정수배 근처** · 하루 **2~12회** · `minEl` 상향 시 **단조 감소** · 최대고도 90° 이하 · 통과 구간 **비겹침** | `인용` `214b088` · `P11-8` | **높다.** 골든이 불가능한 대상의 검증법 |
| C5 | **테스트를 도입할 때 실효성을 뮤테이션으로 확인했다.** 대역 경계 2000→2500, 타일 임계 3→2 가 **각각 정확히 1건**만 실패 | 2차 확장에서도 5종(날짜변경선 끊기 제거 2건 · 고도↔방위 혼동 6건 · 통과 병합 실패 2건 · 타임라인 부등호 반전 1건 · 정렬 반전 1건) 전부 검출 | `인용` `P11-8` · `214b088` | **중~높음** |
| C6 | **모듈 스코프 `let` 은 vm 컨텍스트 객체에 안 보인다** — 테스트가 상태를 읽지도 넣지도 못한다 | 같은 스코프에 접근자를 이어붙여 주입/조회(`STATE_KEYS`). **로더는 `harness.js` 한 곳에만** 둬서 나중에 ES 모듈이 되면 그 파일만 바꾸면 되게 했다 | `인용` `P11-8` | **중** |

### D. 방어 구멍 (테스트가 잡아낸 것)

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| D1 | **1건 실패가 전체를 죽였다.** `results` 안에 `null` 이 섞이면 `AttributeError` 가 `except` 를 빠져나가 **앱까지 전파**됐다 | 회귀 테스트를 도입하다 나왔다. dict 가 아닌 항목은 건너뛰고 `AttributeError`/`KeyError` 도 잡도록 | `인용` `cc059ee` | **높다** |
| D2 | **줄 하나가 그 뒤 전부를 날렸다.** `_parse_tle` 이 3줄 고정 스텝이라 **헤더·잡음 줄이 하나만 있어도 그 뒤를 전부 못 읽었다** | 한 줄씩 밀며 재동기화. **골든이 그대로 통과 → 실제 데이터 결과는 불변**이라는 것으로 안전을 확인했다 | `인용` `cc059ee` | **높다.** 고정 스텝 파서의 전형 |
| D3 | **같은 발사가 두 번 들어왔다.** LL2 가 막 발사된 건을 `upcoming`·`previous` **양쪽에** 내보낸다 | id 기준 중복 제거(previous 우선 = 결과 확정본). 실측 100 → 99건. **마커·통계·티커 모두에 중복으로 잡히던 것이 함께 해소** | `인용` `de248e2` | **중~높음** |
| D4 | **id 타입이 섞인다.** LL2 id 는 문자열, NORAD 는 숫자로 온다 | 즐겨찾기는 **항상 문자열로** 보관 | `인용` `P11-2` | **중** |

### E. 측정이 계획을 뒤집었다

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| **E1** | **로드맵에 "렌더 성능이 문제일 것"이라 적어 둔 항목의 전제부터 틀렸다.** 갱신은 매 프레임이 아니라 **초당 1회**였고, 실제 앱에서 **8,000개도 동기 28ms · 화면 반영 33ms**(177개 0.7ms · 2,000개 7.4ms) | 측정 **30분**으로 구현 자체가 불필요해졌다. `satrec` 을 깊은 복사해 위성마다 별개 객체로 둔 뒤에도 같은 수치라 **SGP4 캐시 효과도 아니었다** | `인용` `P11-6` · `638f330` | **높다** |
| **E2** | **재는 환경이 제품과 다르면 반대 결론이 난다.** Node 단독 벤치는 5,000개 **86ms** 로 **더 느리게** 나온다 | **판단 근거는 실제 앱(WebView2) 수치를 썼다.** Node 수치를 믿었으면 "최적화가 필요하다"가 됐다 | `인용` `P11-6` | **높다.** Argus 의 "재는 대상이 제품보다 좁으면 결론이 기운다"와 같은 자리 |
| E3 | **제약의 정체가 딴 데 있었다.** 위성 개수 캡을 성능 때문이라고 믿었는데, 실제 제약은 **Celestrak 403**(Starlink 전체 10,776개·1.7MB 를 짧은 간격으로 거듭 받으면 막힌다). 그리고 **GEO 는 실제 568개인데 200개로 자르고 있었다** | 캡을 올렸다(GEO 무제한 · Starlink 2,000). **상한은 지도에 그릴 개수만 줄이고 다운로드는 줄이지 않는다**는 것도 주석에 남겼다 | `인용` `v1.5.1` | **높다** |
| E4 | **그래서 원칙을 하나 남겼다** — *"로드맵에 «성능/구조가 문제일 것»이라고 적어 둔 항목은 **착수 전에 측정부터** 한다."* 그 측정이 대신 **캡 상향이라는 실제 개선**을 찾아냈다 | `인용` `PLAN.md` Phase 11 종료 메모 | **높다** |

### F. 공개 저장소에 남의 것이 올라갔다

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| **F1** | **테스트 픽스처에 제3자 연락처가 들어 있었다.** `tests/fixtures/ll2_*.json` 의 `vidURLs` 에 유튜브 설명문이 통째로 — **채널 운영자의 SNS 계정·사업용 이메일**과 "무단 사용 시 삭제될 수 있다"는 저작권 문구까지 | **파싱에 쓰이지도 않는 필드였다** — `_parse_launches` 는 중계 링크에서 `title`·`url` 만 남기고 `description` 은 버린다(캐시 비대 방지). 그래서 지워도 **골든값이 안 바뀌고 17/17 그대로 통과**했다 | `인용` `edd029c` | **높다.** 피파에서 같은 종류가 나왔다 — **두 프로젝트째** |
| **F2** | **생성 쪽을 안 고치면 다음 갱신에 그대로 돌아온다.** 기존 픽스처 14건을 비우는 것만으로는 부족해서 `capture_fixtures.py` 도 함께 고쳤다 | 피파는 생성 스크립트가 없어 **수동 익명화 규칙을 docstring 에 적는 것**으로 대신했다 — 같은 문제에 대한 두 가지 답 | `인용` `edd029c` | **높다** |

### G. 만든 것을 되돌리고, 안 만들기로 한 것

| # | 무엇 | 왜 | 근거 | 일반성 |
|---|---|---|---|---|
| G1 | **발사 T-10분 토스트 알림을 전부 제거했다**(구현 후 롤백) | 실사용에서 불필요하다고 판단. `winotify` 의존성까지 걷어냈고, **로드맵에 `P8-8 ❌` 로 남겨 롤백 사실과 이유를 기록**했다 | `인용` `b4a5374` | **중~높음.** 지운 것을 왜 지웠는지 남긴 자리 |
| G2 | **딥링크·한/영 토글·앱 개명을 도입하지 않기로 결정하고 그 사실을 적었다** | 딥링크는 데스크톱 앱이라 실익이 없고, 나머지는 혼자 쓰는 동안 이득이 없다 | `인용` `PLAN.md` 11절 · `P11-4` | **중** |
| G3 | **발사 실시간 텔레메트리는 무료 API 부재로 범위 밖** — "나중에" 표에 도입 조건과 함께 남겼다 | `인용` `CLAUDE.md` · `PLAN.md` 4-3 | **중** |

### H. 구조·빌드·환경

| # | 무엇 | 어떻게 | 근거 | 일반성 |
|---|---|---|---|---|
| H1 | **ES 모듈을 쓸 수 없다.** `main.py` 가 `url=` 에 파일 경로를 주고 `http_server` 없이 `webview.start()` 해서 페이지가 **`file://` 로 열린다** | 클래식 스크립트라 **최상위 `let`·`function` 이 파일 간에 공유**되고, `index.html` 의 `<script>` 순서가 곧 의존 관계다. 로드맵에 적어 둔 "확인 필요"의 답이 이것이었다 | `인용` `P11-5` | **중~높음** |
| H2 | **1,737줄 단일 파일을 8개로 자르며 «잘못 자르지 않았음»을 어떻게 확인했나** | 라인 범위 매핑 스크립트로 **"모든 줄이 정확히 한 번씩" 배치**됐음을 검증 → `node --check` 8개 + 이어붙인 결과 → **선언 개수 대조**(함수 107 · 최상위 바인딩 50, 원본과 동일) | `인용` `P11-5` · `6f948c8` | **높다.** 기계적 리팩터의 검증법 |
| H3 | **`build.bat` 은 ASCII·BOM 금지.** cmd 는 BOM 을 명령으로 잘못 읽고, 한글 경로(`C:\Users\준\`)는 코드페이지에 따라 깨진다 | `activate.bat` 대신 `.venv\Scripts\python.exe` 를 직접 부르는 것도 같은 이유. **파일 상단 주석에 이유가 적혀 있고 실제로 그래서 살아남았다** | `인용` `f3511c9` · `CLAUDE.md` | **중~높음** |
| H4 | **쓰지 않는 옛 캐시가 남아 있었다** — 그룹별 TLE 캐시로 바꾸기 전의 `tle.json` 36KB | 시작할 때 한 번 지운다 | `인용` `v1.5.2` | **중** |
| H5 | **네트워크 어댑터를 건드리지 않고 오프라인을 검증했다** — 타일 URL 을 도달 불가 호스트로 잠시 바꿔 실제 렌더를 캡처로 확인 | `인용` `P11-7` | **중~높음** |
| H6 | **마우스 자동화 없이 창 복원을 검증했다** — `settings.json` 을 미리 써 두고 재실행 → `PrintWindow` 캡처로 복원 결과 확인. **초기 로드만으로는 `moveend` 가 안 나서 카메라가 저장되지 않는데, 그건 정상 동작**이라 저장 경로는 vm 테스트로 따로 검증했다 | `인용` `PLAN.md` P11 검증 메모 | **중~높음** |

---

### I. 변이 실험이 거짓 음성을 냈다 `실측 2026-09-13~14`

> [노트 02](02-checks-that-passed-vacuously.md) 는 **변이를 도구로** 써서 공허한 검사를
> 찾았다. 이번엔 **그 도구 자체가 "안 잡혔다"고 잘못 말한** 경우들이다. 매번 이유가 달랐다.

| # | 무엇이 틀렸나 | 어떻게 알아냈나 / 어떻게 했나 | 근거 | 일반성 |
|---|---|---|---|---|
| **I1** | **방어가 둘이라 하나를 뜯어도 안 잡혔다.** `costPerKg` 에 앞단 falsy 검사(`!cost \|\| !leoKg`)와 결과 검사(`!isFinite(v)`)를 겹쳐 뒀더니, **둘 중 하나를 지우는 변이가 나머지에 막혀 전부 통과**했다 — 방어를 뜯어도 테스트가 조용했다 | **둘 다 지우는 변이**를 해 보고서야 알았다(그때만 실패 1건). 방어를 **한 줄로** 합쳤고, 지금은 그 줄을 지우면 단언 넷이 깨진다 | `실측` `v1.27.0` · P15-2 | **높다.** 중복 방어는 "안전해 보이는" 코드의 기본형이다 |
| **I2** | **같은 일이 이틀 뒤 또 났다.** `freshnessText` 의 `isFinite` 검사가 `agoText` 의 것과 겹쳐, 지우는 변이가 통과했다 | 한 곳으로 줄였다. `agoText` 가 못 읽는 날짜에 `""` 를 주므로 `\|\| null` 로 이어받는다 | `실측` `v1.31.0` · P15-7 | **높다.** 두 번 났다는 것이 이 갈래의 근거다 |
| **I3** | **골든을 갱신하기 전에 변이를 돌리면 아무것도 안 잰다.** 파싱에 필드를 더한 뒤 *그 필드를 지우는* 변이를 했는데 골든 회귀가 **통과**했다 — 아직 갱신 전이라 **필드를 빼면 옛 골든과 도로 같아진다** | 갱신 후 다시 돌리니 `FAILED (failures=2)`. **순서가 곧 검증력**이다 | `실측` `v1.28.0` · P15-6 | **높다.** 골든/스냅샷 회귀를 쓰는 모든 곳 |
| **I4** | **변이가 적용조차 안 됐는데 "안 잡혔다"고 읽었다.** `sed` 패턴에 없는 괄호를 넣어 치환이 실패했고, 결과만 보고 테스트 구멍으로 오해했다 | **치환 후 해당 줄을 출력해** 적용 여부를 먼저 확인한다. 제대로 하니 `failures=4` | `실측` `v1.29.0` · P15-5 | **중~높음.** 자동화로 변이를 넣을 때의 전형 |

### J. 후보를 고르는 기준이 네 번 늘어났다 `실측·인용 2026-09-12~14`

> "이미 받아오는데 안 쓰는 데이터"를 기능으로 바꾸는 일을 네 Phase 동안 했다.
> **매번 다른 방식으로 속았고, 그때마다 재는 것이 하나씩 늘었다.**

| # | 무엇이 틀렸나 | 무엇을 재게 됐나 | 근거 | 일반성 |
|---|---|---|---|---|
| **J1** | **표본을 한쪽만 보고 "전부 비어 있다"고 했다.** `probability`·`weather_concerns` 가 56건 전부 비었다고 적고 "죽은 UI"로 기각했는데, 그 표본이 **예정 발사 50건 + 픽스처 6건**이었다. 캐시 100건(예정 50·**지난 50**)으로 다시 세니 14건·9건이 있고 **전부 지난 발사**다 — 발사 뒤에 채워지는 필드였다 | ① **채움률을 예정/지난 갈라서** 잰다 | `인용` P13-4 | **높다.** 상태에 따라 채워지는 필드의 전형 |
| **J2** | **픽스처 7행이 역사적 물체라 채워져 보였다.** SATCAT 궤도 제원을 후보로 뽑았는데, 실제 쓸 그룹(`visual` 159행)에서는 `ORBIT_TYPE` 이 **전부 빈값**, `ORBIT_CENTER` 가 **전부 `EA`** 였다 — 정보량 0 | ② 채움률만이 아니라 **서로 다른 값이 몇 개인지**(정보량)를 본다. 뒤에 `rocket.apogee` 를 이 기준으로 기각했다(채움 42/100인데 **값이 넷뿐**: 200·500·20·40000) | `인용`+`실측` P14-3 · P15 후보 | **높다** |
| **J3** | **채움률이 높아도 분포가 한쪽에 쏠려 있었다.** 발사 비용 65/100 인데 **미국 49/55(89%) 대 중국 2/21(10%)** — 합계·순위 막대를 내면 어떤 집계도 *"중국 발사는 공짜"* 로 읽힌다 | ③ **분포 쏠림**을 본다. 집계 화면을 **안 만들기로** 했다(모수를 밝혀도 막대 길이가 먼저 눈에 들어온다) | `실측` `v1.27.0` · P15-2 | **높다.** 국가·기관별 데이터의 기본 함정 |
| **J4** | **"값이 있다"와 "말할 게 있다"가 달랐다.** 미션 참여 기관 59/100 중 **28건이 발사 제공자 자신**이었다(Starlink 자체 발사) — 그대로 두면 *"SpaceX 가 SpaceX 를 위해"* 다 | ④ **자기 자신·중복을 뺀 뒤** 다시 센다. 빼니 31/100 으로 반토막 | `실측` `v1.29.0` · P15-5 | **중~높음** |
| **J5** | **새 경고가 기존 경고와 겹쳤다.** 30일 넘게 안 바뀐 예정 발사 23/50 을 경고로 만들려다, 겹침을 세 보니 **22건은 이미 `net_precision` 경고가 떠 있었다.** 새로 알려 주는 건 **1건** | ⑤ **기존 화면이 이미 같은 말을 하는지** 센다. 경고를 접고 사실 한 줄로 낮췄다 | `실측` `v1.31.0` · P15-7 | **높다.** 경고를 더하기 전의 일반 규칙 |

### K. 거르려고 쓴 관용구가 안 걸렀다 `실측 2026-09-13`

> 둘 다 **"이 관용구가 막아 줄 것"** 이라고 믿은 자리다. 둘 다 안 막았고, 둘 다 조용했다.

| # | 무엇이 틀렸나 | 어떻게 알아냈나 / 어떻게 했나 | 근거 | 일반성 |
|---|---|---|---|---|
| **K1** | **`.filter(x => x[1])` 가 아무것도 안 걸렀다.** 제원 표를 falsy 로 거르는데, 포맷터 `fmtQty(0, "kg")` 가 **`"0 kg"` 이라는 truthy 문자열**을 낸다 — 화면에 `LEO 탑재량 0 kg` 이 떴다(GSLV Mk. II). 탑재량 0 인 로켓은 없다 | 포맷터에서 **물리량의 0 을 `null` 로** 돌린다. **횟수의 0 은 그대로** 둔다 — `실패 0` 은 43종 중 25종이고 좋은 소식이다. 두 종류의 0 을 가르는 것이 핵심 | `실측` `v1.27.0` · P15-2 | **높다.** 거르는 쪽과 포맷하는 쪽이 따로 있으면 늘 생긴다 |
| **K2** | **`x or {}` 가 문자열을 그대로 통과시켰다.** `None`·`{}`·`""` 만 막고 **비어 있지 않은 문자열**은 통과시켜, 다음 줄 `.get()` 이 `AttributeError` 로 터진다 — **발사 한 건이 아니라 페이지 전체가** 날아갔다 | **방어 테스트가 찾았다**(`launch_service_provider: "문자열"`). `_dict()` 로 아홉 자리를 한 번에 고쳤고, 변이로 되돌리니 `errors=6` | `실측` `v1.30.0` · P15-3 | **높다.** [A7](#) 의 `groups or DEFAULT` 와 같은 갈래 — `or` 는 타입이 아니라 truthiness 를 본다 |

### L. 잔해를 세는 grep 이 살아 있는 것을 죽었다고 했다 `실측 2026-09-13`

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| **L1** | **"죽은 코드" 22개가 전부 살아 있었다.** 호출부 없는 JS 함수 **14개**와 안 쓰는 CSS 클래스 **7개**가 나왔는데, 되짚으니 **22개 모두 쓰이고 있었다.** 함수는 `addEventListener("click", fn)`·`.filter(fn)`·`.map(fn)` 처럼 **이름만 넘기는 자리**, CSS 는 `m-${d.outcome}`·`tk-${...}`·`reason-${kind}` 처럼 **문자열을 이어 만드는** 클래스였다 | 후보를 내기 전에 **한 개씩 되짚었다.** `grep "name("` 로 세면 이 둘이 전부 죽은 것으로 나온다 | `실측` Phase 15 후보 절 | **높다.** 콜백을 값으로 넘기는 언어 + 동적 클래스명을 쓰는 모든 곳 |

### M. 기각 근거도 후보 목록처럼 낡는다 `인용·실측 2026-09-12~13`

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| **M1** | **한 번 기각한 분리를 다시 재니 근거가 뒤집혀 있었다.** *"30커밋 6회·중앙값 22줄이라 아낄 게 별로 없다"* 로 기각(P12-18)했는데, 하루 뒤 **30커밋 12회·811줄**, 기능 셋을 더 넣고는 **15회·940줄·902줄짜리 리포 최대 JS** 였다 | 같은 명령을 **다시 돌려** 셌다. 여섯 파일로 나눴다(P14-4) | `인용`+`실측` P12-18 → P14-4 | **높다.** "기각"에 날짜를 안 붙이면 영구 결론이 된다 |
| **M2** | **가장 비싼 파일이 후보 목록에 아예 없었다** | 후보를 믿지 않고 **churn 을 다시 측정**해서 찾았다(`sats.js` → 네 파일) | `인용` P12-23 | **중~높음** |
| **M3** | **churn 을 횟수만 세면 거짓말을 한다** — 자주 바뀌는 것과 크게 바뀌는 것이 다르다 | 횟수와 **변경 줄 수**를 같이 센다 | `인용` P12-19 근거 재측정 | **높다** |
| **M4** | **"싸다"고 적어 둔 판단이 실측에서 뒤집혔다**(P12-8 재진입) | 착수 전에 다시 쟀다 | `인용` `P12-8` | **중** |

### N. 조용히 죽어 있었다 `인용·실측 2026-09-12~13`

> [노트 01](01-not-broken-but-it-looked-broken.md) 의 **반대쪽**이다 — 그쪽은 *멀쩡한데
> 고장으로 보인* 것이고, 이쪽은 *고장인데 고장으로 안 보인* 것이다.

| # | 무엇이 틀렸나 | 어떻게 알아냈나 | 근거 | 일반성 |
|---|---|---|---|---|
| **N1** | **"오늘 밤" 탭이 도입 이래 줄곧 죽어 있었다.** `Set` 에 `.includes()` 를 불러 예외가 났는데, **테스트는 관측지가 없는 경로만** 덮고 있었다 — 그쪽은 안내를 내고 끝나 예외가 안 난다. 화면에는 "탭이 안 바뀐다"로만 보였다 | **정기 점검에서 기능을 실제로 켜 보고** 나왔다. 조건 분기는 **그 분기로 실제로 들어가는 상태**를 만들어 재야 한다 | `인용` `v1.22.1` | **높다** |
| **N2** | **CI 가 다섯 커밋 동안 빨간불이었고 아무도 안 봤다.** 로컬 530 통과 / CI 526 통과·4 실패 — 시간대 테스트가 "현지 ≠ UTC"를 재는데 **CI 러너는 UTC** 라 둘이 같아졌다 | 테스트 맨 위에서 `TZ` 를 고정. **푸시한 뒤 `gh run list` 한 줄을 본다** | `인용` 2026-09-12 | **높다.** 로컬 초록이 증거가 아닌 전형 |
| **N3** | **오늘 만든 기능 넷이 아카이브에서 영원히 안 보이고 있었다.** 지난 연도 캐시는 **TTL 이 없어 영구**라 새 필드가 안 채워지는데, `launches.json` 은 TTL 15분이라 **저절로 나아 개발 중에는 보이지도 않았다** | 세션 끝에 **캐시 파일을 직접 열어** 보고 찾았다. `CACHE_SCHEMA` 를 도입 | `인용` `v1.22.0` | **높다.** 계층마다 수명이 다른 캐시의 전형 |
| **N4** | **11개 버전이 릴리스된 적이 없었다** — 태그만 있고 릴리스가 없었다 | 목록을 세어 보고 알았다 | `인용` P12-22 | **낮~중** |

### O. 스키마를 여섯 번 올려 API 한도를 태웠다 `실측 2026-09-13~14`

| # | 무엇이 틀렸나 | 어떻게 알아냈나 / 어떻게 했나 | 근거 | 일반성 |
|---|---|---|---|---|
| **O1** | **한 세션에 `CACHE_SCHEMA` 를 3→9 로 여섯 번 올려 LL2 시간당 한도(429)를 태웠다.** 스키마가 바뀌면 `launches.json` 뿐 아니라 **연도 아카이브까지 통째로** 다시 받는다(연도당 최대 5페이지). 그 뒤로는 **실제 캐시로 검증할 수 없었다** | 폴백은 설계대로 동작했다 — 화면에 `⚠ 요청이 많아 잠시 제한됐습니다(시간당 한도)… (저장된 데이터 표시)` 가 뜨고 지도·데이터는 그대로였다. 검증은 **받아 둔 원본 응답을 `api_parsing` 으로 파싱해 `harness.js` 로 렌더**해서 이었다(네트워크 0, 실제 API 데이터). 규칙은 `CLAUDE.md` 로: **여러 기능을 잇달아 넣는 날은 스키마를 마지막에 한 번만** | `실측` `v1.31.0` | **중~높음.** 캐시 무효화가 상류 API 비용을 부르는 구조 전반 |

## 3. 고른 것 — 2편 → **둘 다 썼다 (2026-09-05)**

> [`README.md`](README.md) 에 목록이 있다.
>
> | | 노트 | 후보 |
> |---|---|---|
> | 01 | [고장이 아닌데 고장으로 보였다](01-not-broken-but-it-looked-broken.md) | A1~A7 |
> | 02 | [공허하게 통과한 검사들](02-checks-that-passed-vacuously.md) | C1+C2+C3+C4 + B1·B2 |
>
> **쓰면서 실제로 돌려 확인했다** — `python tests/test_parsing.py` **34/34**,
> `node tests/test_frontend.js` **102/102**. 노트가 인용한 항목 수가 지금도 맞다.
>
> 그리고 **노트에 적은 코드 조각을 실제 소스와 대조해 두 곳을 고쳤다.** 기억으로
> 재구성한 함수 이름(`is_visible` → `_rect_visible`)과, 예외 분기의 "고치기 전" 모양을
> 지어낸 것. **후자는 지금 소스의 `_is_timeout` docstring 이 경위를 그대로 들고 있어서**
> 재구성 대신 그것을 인용했다. `인용` 표기를 지킨다는 것은 이런 자리를 말한다.

### 노트 후보 1 — 고장이 아닌데 고장으로 보였다 (A 묶음 7건)

> 창이 화면 밖에 떠서, 지도 배경만 안 떠서, 필터에 다 걸려서, 설정을 그대로
> 복원해서 — **앱은 정상인데 사용자에게 도달한 것은 "고장"이었다.** 일곱 번 나왔다.

**왜 이것인가.** 다섯 프로젝트 중 이 갈래가 한 프로젝트에 이만큼 모인 곳이 없다.
그리고 **고친 방식이 전부 다르다** — 복원값을 검사하거나(A1), 상태를 설명하는 배지를
띄우거나(A2), 저장된 설정을 무시하거나(A3), 필터에 예외를 두거나(A4), **애초에 그
자리에 필터를 걸지 않거나**(A5). 마지막 것이 특히 좋다: *"검색어를 지도에도 걸면
검색창을 닫은 뒤 왜 위성이 없는지 알 수 없다"* — **필터의 흔적이 화면에 남지 않는
곳에는 필터를 걸지 않는다**는 규칙이 사례에서 나왔다.

A7 은 반대 방향이라 대비가 된다 — 사용자가 명시적으로 전부 끈 것을 앱이 **되돌렸다**
(빈 리스트가 falsy 라서). "아무것도 안 보이면 고장"이라는 걱정이 코드에서 이렇게도
나타난다.

⚠️ 이 노트는 **자랑으로 기울기 쉽다.** 일곱 건 중 다섯이 "이렇게 잘 막았다"는 이야기라
노트의 성격(실패와 발견)에서 벗어난다. **A1·A7 처럼 실제로 겪은 것을 앞에 두고**,
나머지는 "그래서 다음부터 미리 막은 것"으로 배치해야 한다.

### 노트 후보 2 — 공허하게 통과한 검사들 (C1+C2+C3, 보조 C4·B1)

> 좌표가 전부 `NaN` 인데 검사가 통과했다. 상수를 0 으로 바꿨는데 경계 테스트가
> 통과했다. 그리고 애초에 **한 번도 검증된 적 없는 계산**이 있었다.

**왜 이것인가.** 세 건의 메커니즘이 전부 다르고, 셋 다 **테스트가 초록인 채로**
일어났다.

| | 왜 통과했나 |
|---|---|
| C1 | 대상이 빈/`NaN` 이라 `every()` 가 **공허하게 참** — 별도 realm 의 `instanceof` 실패가 원인 |
| C2 | 기대값을 상수로 계산해 **상수가 바뀌면 기대값도 같이 움직인다** |
| C3 | 그 계산에 **테스트가 아예 없었다** — "화면으로는 알 수 없는" 영역이 통째로 |

그리고 **C4 가 답이다** — 정답표가 없는 계산은 물리 불변식으로 잰다(경사 51.6° 위성은
남극에서 통과 0건, 간격은 주기의 정수배, `minEl` 상향 시 단조 감소…). 이건 노트로
쓸 값이 있다. *"골든을 만들 수 없으면 검증할 수 없다"* 가 아니었다.

**B1 을 곁들인다** — `isinstance(e, TimeoutError)` 가 한 번도 실행되지 않는 죽은
분기였던 것. 테스트가 아니라 제품 코드에서 같은 일이 일어난 사례다.

### 세 번째는 쓰지 않는다

**E 묶음(측정이 계획을 뒤집었다)** 이 단독으로 강하다. 특히 **E2**(Node 벤치는
5,000개 86ms 인데 실제 앱은 8,000개 28ms — 재는 환경이 다르면 반대 결론)는 일반성이
높다. 다만 **Argus 노트 01·03 이 이미 "재는 쪽이 틀렸다"를 두 편으로 썼다.**
세 번째 telling 이 되므로 **허브 주제에 사례로 올리고 노트는 안 쓴다.**

**F 묶음(픽스처에 남의 연락처)** 은 **피파와 두 프로젝트째**라 허브 새 주제 조건이
찬다(§5). 노트로는 피파 때와 같은 이유로 미룬다 — 발견 경로가 한 줄이다.

---

## 3.5 다음에 쓸 것 — 2026-09-14 추가분에서

> **아직 안 골랐다.** 한 세션에 여러 편을 몰아 쓰면 "두 프로젝트 이상"이라는
> 허브 주제 판정을 할 사이가 없다(개발노트 README ⓔ). 순서만 적어 둔다.

| 순위 | 묶음 | 왜 이것인가 |
|---|---|---|
| **1** | **I (변이가 거짓 음성을 냈다) 4건** | [노트 02](02-checks-that-passed-vacuously.md) 가 **변이를 도구로** 썼으니 이건 정확히 그 뒷면이다 — *도구가 "괜찮다"고 잘못 말한* 네 경우. 이유가 매번 다르고(중복 방어 2회 · 골든 갱신 순서 · 치환 미적용) **중복 방어는 이틀 간격으로 두 번** 났다. 02 를 읽은 사람이 바로 다음에 읽을 글이다 |
| **2** | **J (기준이 네 번 늘어났다) 5건** | 한 갈래의 일을 네 Phase 동안 하며 **속는 방식이 매번 달랐고, 잴 것이 하나씩 늘었다**(채움률 → 정보량 → 분포 쏠림 → 중복 제거 → 기존 화면과의 겹침). 결론이 아니라 **기준이 자라는 과정**이라 이 프로젝트에서만 나올 수 있는 글이다 |
| 3 | **K (관용구가 안 걸렀다) 2건** | `"0 kg"` 이 truthy · `x or {}` 가 문자열 통과. **기존 A7**(`groups or DEFAULT`)과 같은 갈래라 셋을 묶으면 두꺼워진다 |
| 4 | **M+L (근거가 낡는다 / 잔해 탐지 오탐)** | M 은 *기각에 날짜를 안 붙이면 영구 결론이 된다*, L 은 *grep 이 살아 있는 것을 죽었다고 한다* — **둘 다 "내가 적어 둔 것을 믿으면 안 된다"** 로 묶인다 |
| 5 | **N (조용히 죽어 있었다) 4건** | [노트 01](01-not-broken-but-it-looked-broken.md) 의 반대쪽이라 대비가 좋은데, **01 을 고쳐 쓰는 모양이 될 위험**이 있다. 쓴다면 01 을 다시 읽고 시작한다 |
| — | **O (스키마로 한도 태움) 1건** | 단독으로는 얇다. 허브의 *"캐시 무효화가 상류 비용을 부른다"* 류 주제가 생기면 그때 |

**허브 후보**: **I** 와 **J** 는 둘 다 *"검증·측정 자체를 검증한다"* 는 한 갈래다.
다른 프로젝트에서 같은 모양이 나오면 그때 허브 주제로 올린다 — 지금은 **한 프로젝트
이야기**라 올리지 않는다(ⓔ).

## 4. 탈락 — 다시 검토하지 않기 위해

| 후보 | 왜 뺐나 |
|---|---|
| D1·D2·D3·D4 | 좋은 버그들이지만 **각각 한 문단**이다. D2(고정 스텝 파서가 줄 하나에 전부 무너짐)만 일반성이 높아 노트 2 의 예시로 쓸 수 있다 |
| C5·C6 | 방법론 메모다. 노트 2 안의 각주로 |
| E1~E4 | **강하다. 다음 순번 1순위.** 다만 Argus 와 주제가 겹쳐 이번엔 허브 사례로만 |
| F1·F2 | **다음 순번 2순위.** 피파 F1 과 묶으면 한 편이 된다 — 두 저장소가 **같은 문제에 다른 답**(생성 스크립트 수정 vs docstring 규칙)을 냈다는 대비가 있다 |
| G1·G2·G3 | *"왜 안 만들었나"* 는 좋은 기록이지만 **실패담이 아니다** — 피파에서도 같은 이유로 뺐다 |
| H1·H3·H4 | 환경 제약 메모. `CLAUDE.md` 자리이지 노트 자리가 아니다 |
| H2 | **아깝다.** "모든 줄이 정확히 한 번씩 + 선언 개수 대조"는 기계적 리팩터의 좋은 검증법인데, **아무것도 깨지지 않은 이야기**라 노트의 형태(무엇이 어떻게 깨졌나)에 안 맞는다 |
| H5·H6 | 검증 방법 메모. 노트 1·2 의 각주로 |
| B2 | 노트 2 의 B1 옆에 한 줄로 |

---

## 5. 허브에 보낼 것

### 새 주제 조건이 찬 것 — 「공개 저장소에 남의 것이 올라간다」

| | 사례 |
|---|---|
| **피파** | 테스트 픽스처가 실응답 그대로라 `ouid`(영구 식별자) · `matchId`(하나만 있어도 참가자 조회 가능) · **상대 구단주명 4명**이 공개돼 있었다. 파일명도 `matchId` 라 **내용만 고쳐서는 소용이 없었다** |
| **RL3D** | 픽스처의 `vidURLs` 에 유튜브 설명문이 통째로 — **채널 운영자의 SNS·사업용 이메일**과 저작권 문구. **파싱에 쓰이지도 않는 필드**라 지워도 골든이 안 바뀌었다 |

**둘 다 회귀 테스트 픽스처다.** 실제 응답을 그대로 두는 것이 골든 테스트의 가치인데,
그 실제 응답에 **제3자 정보가 섞여 있다.** 그리고 답이 갈렸다 — RL3D 는 **생성
스크립트를 고쳤고**(안 고치면 다음 갱신에 돌아온다), 피파는 생성 스크립트가 없어
**익명화 규칙을 docstring 에 적었다.**

### 기존 주제에 붙는 것

| 주제 | RL3D 사례 |
|---|---|
| **통과는 증거가 아니다** | **C1** — 좌표가 전부 `NaN` 인데 `every()` 검사가 공허하게 통과. 별도 vm realm 의 `instanceof` 실패가 원인이고 **브라우저에선 없는 문제** · **C2** — 기대값을 상수로 계산해 그 상수를 0 으로 바꿔도 통과 |
| **한 번도 돌지 않은 것이 오래 살아남는다** | **B1** — `isinstance(e, TimeoutError)` 분기가 한 번도 실행되지 않는 죽은 코드였고, 그동안 사용자는 "인터넷 연결을 확인하세요"를 봤다 |
| **기능이 아니라 측정이 틀렸다** | **E1·E2** — "렌더 성능이 문제일 것"의 전제부터 틀렸고(초당 1회 갱신), Node 벤치를 믿었으면 반대 결론이 났다 |
| **화면이 본문과 다른 말을 한다** | **A 묶음** — 앱은 정상인데 화면은 고장. 특히 **A5**(필터의 흔적이 안 남는 곳에는 필터를 걸지 않는다) |

---

## 6. 재료는 다 훑었다

| 재료 | 상태 |
|---|---|
| `PLAN.md` 311줄 (특히 Phase 11 표) | ✅ |
| `CHANGELOG.md` 141줄 (v1.0.0~v1.5.2 전부) | ✅ |
| `CLAUDE.md` 88줄 · `README.md` 135줄 | ✅ |
| `git log` 커밋 30개 · 본문 270줄 | ✅ 본문 있는 것 전부 |
| `tests/` · `web/js/` | ⏸️ 노트를 쓸 때 재현 명령 확인용 |

### 2026-09-14 재측정 — 그 뒤 재료

| 재료 | 상태 |
|---|---|
| `git log` 커밋 **52개**(2026-09-06~14) · 본문 **998줄** | ✅ 제목 전부 · 본문은 Phase 12~15 |
| `PLAN.md` 672줄 (Phase 12~15 절 · ⚠ 블록) | ✅ |
| `CHANGELOG.md` 982줄 (v1.6.0~v1.31.0) | ✅ |
| `docs/DECISIONS.md` 920줄 | ⏸️ **아직 안 훑었다** — 9/5 표에서 빠져 있던 재료라 다음 캐기에서 본다 |
| `CLAUDE.md` 156줄 · `README.md` 223줄 | ✅ |

> **`DECISIONS.md` 920줄이 남아 있다.** 지금 후보(55건)는 그것을 빼고 나온 수다 —
> 훑으면 더 늘 수 있다.
